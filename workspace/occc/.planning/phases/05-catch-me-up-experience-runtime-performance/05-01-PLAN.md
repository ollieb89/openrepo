---
phase: 05-catch-me-up-experience-runtime-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/lib/sync/intent.ts, src/lib/sync/vector-store.ts, src/lib/sync/types/intent.ts]
autonomous: true
requirements: [CMEU-01, PERF-03]
must_haves:
  truths:
    - "User queries are parsed into structured IntentMetadata including date ranges"
    - "Vector searches are pre-filtered by date range in the SQL layer"
    - "Active project records receive a +0.3 relevance boost"
  artifacts:
    - path: "src/lib/sync/intent.ts"
      provides: "NLP temporal parsing via chrono-node"
    - path: "src/lib/sync/vector-store.ts"
      provides: "Filtered vector retrieval logic"
  key_links:
    - from: "src/lib/sync/intent.ts"
      to: "chrono-node"
      via: "import"
    - from: "src/lib/sync/vector-store.ts"
      to: "vector_cache table"
      via: "WHERE clause filtering on created_at"
---

<objective>
Implement the "Intent & Temporal Engine" to parse natural language queries and perform high-performance, filtered vector retrieval.

Purpose: Improve accuracy and speed by narrowing the search space before vector similarity calculations.
Output: Intent parsing utility and updated vector store with SQL filtering support.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catch-me-up-experience-runtime-performance/05-CONTEXT.md
@src/lib/sync/vector-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install chrono-node and implement Intent Parser</name>
  <files>package.json, src/lib/sync/types/intent.ts, src/lib/sync/intent.ts</files>
  <action>
    1. Run `npm install chrono-node`.
    2. Create `src/lib/sync/types/intent.ts` defining `IntentMetadata` interface per CONTEXT.md (query, timeRange, boostedProjectId, limit).
    3. Create `src/lib/sync/intent.ts` implementing `parseIntent(query: string, activeProjectId?: string): IntentMetadata`.
    4. Use `chrono.parse()` to extract date ranges. Default to "Last 7 Days" if no temporal context found.
    5. Ensure `boostedProjectId` is correctly mapped from the active project.
  </action>
  <verify>Run a standalone test script that calls `parseIntent` with strings like "last week", "since Monday", and "yesterday" and verifies correct ISO dates in the response.</verify>
  <done>Natural language temporal queries correctly resolve to start/end timestamps.</done>
</task>

<task type="auto">
  <name>Task 2: Filtered Vector Retrieval with Project Boost</name>
  <files>src/lib/sync/vector-store.ts</files>
  <action>
    1. Update `src/lib/sync/vector-store.ts` to add `searchContext(intent: IntentMetadata)` function.
    2. Implement SQL pre-filtering in the query:
       - Filter `vector_cache` by `created_at` using `intent.timeRange`.
       - Filter by `entity_type` if implied (though catch-up usually searches both).
    3. Implement the +0.3 relevance boost logic for records where `metadata->projectId` matches `intent.boostedProjectId`.
    4. Ensure the function returns a ranked list of `VectorRecord` with their adjusted scores.
  </action>
  <verify>Unit test `searchContext` with mock records spanning different dates and projects; verify that date filtering and project boosting work as expected.</verify>
  <done>Vector search is constrained by time and prioritized by active project context.</done>
</task>

</tasks>

<verification>
Check that `chrono-node` is in package.json and that `searchContext` returns correctly filtered and boosted results for a given intent.
</verification>

<success_criteria>
- NLP queries correctly resolve to date ranges.
- Search results exclude records outside the parsed date range.
- Records in the active project appear higher in results due to the +0.3 boost.
</success_criteria>

<output>
After completion, create `.planning/phases/05-catch-me-up-experience-runtime-performance/05-01-SUMMARY.md`
</output>
