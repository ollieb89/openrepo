---
wave: 3
depends_on: ["00-02-PLAN.md"]
files_modified:
  - packages/platform/services/billing.py
  - packages/platform/routes/webhooks.py
  - packages/platform/services/usage.py
  - packages/platform/services/subscription.py
  - packages/platform/routes/billing.py
  - packages/platform/tests/test_billing.py
autonomous: true
---

# Plan: Phase 0, Wave 0.3 - Billing Integration

<context>
**Phase:** 0 - Shared Infrastructure
**Wave:** 0.3
**Goal:** Stripe subscriptions and usage metering. Needed before any customer-facing deployment.
</context>

<requirements>
- Stripe SDK integration + webhook handler with signature verification.
- Subscription model: 3 tiers (Starter $500, Growth $1K, Scale $2K).
- Extract existing `pricing_logic.py` and move to Stripe Products.
- Usage metering per org per billing period.
- Subscription lifecycle logic (create, upgrade, downgrade, cancel with Stripe proration).
- Billing dashboard API endpoints.
- Gate G0.3: Stripe test-mode subscription lifecycle works end-to-end.
</requirements>

<tasks>

<task type="code" gate="blocking">
  <name>Task 0.3.1: Stripe Integration & Webhooks</name>
  <action>
    - Add `stripe` to dependencies.
    - Create `packages/platform/services/billing.py` with Stripe client setup.
    - Create `packages/platform/routes/webhooks.py` to handle Stripe events (signature verification mandatory).
  </action>
  <verify>
    <automated>grep -q "stripe" pyproject.toml && test -f packages/platform/routes/webhooks.py</automated>
    <manual>Review webhook signature validation logic.</manual>
  </verify>
  <done>Stripe client and webhooks route are setup.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.3.2: Subscription Tier Migration</name>
  <action>
    - Review existing `pricing_logic.py`.
    - Create a script or migration to bootstrap Stripe Products and Prices for the 3 tiers (Starter, Growth, Scale).
    - Discard the old in-memory tracking in favor of Stripe definitions.
  </action>
  <verify>
    <automated>test -f packages/platform/services/billing.py</automated>
    <manual>Confirm script accurately maps 3 tiers to Stripe API calls.</manual>
  </verify>
  <done>Subscription tiers migrated to Stripe.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.3.3: Usage Metering Service</name>
  <action>
    - Create `packages/platform/services/usage.py`.
    - Implement logic to report tracked actions to Stripe Usage Records API per-org.
  </action>
  <verify>
    <automated>test -f packages/platform/services/usage.py</automated>
    <manual>Review usage reporting batching or real-time submission code.</manual>
  </verify>
  <done>Usage metering service built.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.3.4: Subscription Lifecycle & APIs</name>
  <action>
    - Create `packages/platform/services/subscription.py` handling create, upgrade, downgrade, cancel operations.
    - Create `packages/platform/routes/billing.py` to expose endpoints for current plan, usage, invoices, and managing payment methods.
  </action>
  <verify>
    <automated>test -f packages/platform/routes/billing.py</automated>
    <manual>Review endpoints for correct tenant isolation via JWT context.</manual>
  </verify>
  <done>Lifecycle logic and billing APIs built.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.3.5: Integration Tests (Gate G0.3)</name>
  <action>
    - Create `packages/platform/tests/test_billing.py`.
    - Write tests using Stripe Test mode (or mocked Stripe responses) to verify full lifecycle: subscribe → use → meter → invoice.
  </action>
  <verify>
    <automated>pytest packages/platform/tests/test_billing.py</automated>
    <manual>Verify test covers all edge cases.</manual>
  </verify>
  <done>Billing integration proven via tests.</done>
</task>

</tasks>

<must_haves>
- Stripe Webhook signature verification is strictly enforced.
- No local pricing logic remains; Stripe acts as the source of truth for Products/Prices.
- Usage events are correctly attributed to the Tenant's Stripe Customer ID.
</must_haves>
