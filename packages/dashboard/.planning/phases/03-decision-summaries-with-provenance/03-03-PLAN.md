---
phase: 03-decision-summaries-with-provenance
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - "src/app/decisions/page.tsx"
  - "src/components/decisions/DecisionCard.tsx"
  - "src/app/api/decisions/route.ts"
  - "src/app/api/decisions/[id]/route.ts"
  - "src/app/api/decisions/[id]/re-summarize/route.ts"
autonomous: false
requirements:
  - SUMM-03

must_haves:
  truths:
    - "User can view a chronological 'Decision Log' of all extracted resolutions."
    - "Decision cards display citation snippets for instant verifiability."
    - "Cards support deep linking to Slack via 'slack://' protocol."
    - "Users can 'Hide' a decision globally or trigger a re-summarization with custom hints."
  artifacts:
    - path: "src/app/decisions/page.tsx"
      provides: "Main UI for the Decision Log"
    - path: "src/components/decisions/DecisionCard.tsx"
      provides: "Interactive card with expand/collapse and correction controls"
  key_links:
    - from: "src/components/decisions/DecisionCard.tsx"
      to: "/api/decisions/[id]/re-summarize"
      via: "POST request with user-provided hint"
      pattern: "fetch.*re-summarize"
---

<objective>
Build the user-facing Decision Log UI and implement the human-in-the-loop correction loop for AI-generated summaries.

Purpose: Provide transparency and control over the summarization engine, allowing users to verify summaries against citations and correct errors.
Output: A functional Decision Log dashboard and API endpoints for hiding and regenerating summaries.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-decision-summaries-with-provenance/03-02-SUMMARY.md
@.planning/phases/03-decision-summaries-with-provenance/03-CONTEXT.md
@src/lib/types/decisions.ts
@src/lib/sync/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Decision Log Feed and Card UI</name>
  <files>src/app/decisions/page.tsx, src/components/decisions/DecisionCard.tsx, src/app/api/decisions/route.ts</files>
  <action>
    - Create `src/app/api/decisions/route.ts` to return all non-hidden decisions sorted by date.
    - Create `src/app/decisions/page.tsx` as a scrollable chronological feed.
    - Create `src/components/decisions/DecisionCard.tsx` following the Research pattern:
      - Collapsed state: Headline (Outcome) + Metadata (Project, Date).
      - Expanded state: Participants, Next Steps, and the **Citation Snippet**.
      - Implement `slack://` deep links and web fallbacks for provenance.
      - Add "Source Restricted" badge logic: if the user's session doesn't have access to the channel, hide the link but show the summary.
  </action>
  <verify>Visit /decisions, expand a card, click the Slack link, verify citation visibility.</verify>
  <done>User can browse and verify decisions in a high-density, interactive feed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Correction and Feedback Controls</name>
  <files>src/app/api/decisions/[id]/route.ts, src/app/api/decisions/[id]/re-summarize/route.ts, src/components/decisions/DecisionCard.tsx</files>
  <action>
    - Create `DELETE /api/decisions/[id]` to set `isHidden: true` globally for that decision.
    - Create `POST /api/decisions/[id]/re-summarize` accepting an optional `hint` string.
    - Logic for re-summarize:
      1. Fetch the original `ThreadRecord`.
      2. Call the summarizer with the user's hint injected into the prompt.
      3. Overwrite the existing decision and return the new one.
    - Add "❌" (Hide) and "🔄" (Re-summarize) buttons to the `DecisionCard`.
    - Implement the "Re-summarize with Hints" pop-over for user input.
  </action>
  <verify>Click 'Hide' on a decision and verify it disappears from the feed. Trigger 'Re-summarize' with a hint and verify the outcome changes.</verify>
  <done>Human-in-the-loop correction mechanisms are fully functional.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Decisions into Catch Me Up Surface</name>
  <files>src/components/common/ContextCard.tsx, src/app/page.tsx</files>
  <action>
    - Create a reusable `ContextCard` component for decisions.
    - Update the Dashboard (Main Page) or a placeholder "Catch Me Up" surface to display the 3 most recent decisions as context cards.
    - Ensure these cards also link back to the full Decision Log or the source thread.
  </action>
  <verify>Check the main dashboard for a 'Recent Decisions' section.</verify>
  <done>Decisions are surfaced in the primary project context as intended.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final UI Verification</name>
  <files>none</files>
  <action>Manually verify the Decision Log UI and Correction Loop.</action>
  <verify>Follow the instructions in the what-built and how-to-verify sections.</verify>
  <done>User confirms the UI works as expected.</done>
  <what-built>Complete Decision Log UI and Correction Loop</what-built>
  <how-to-verify>
    1. Navigate to `/decisions`.
    2. Expand a decision card and verify the citation snippet matches the Slack link context.
    3. Click "Hide" on a garbage summary and ensure it's removed.
    4. Click "Re-summarize" with a hint like "Focus on the deadline" and verify the new summary reflects it.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Chronological feed loads data from the local store.
- Hide and Re-summarize actions persist state correctly.
- Citation snippets are accurate and link to Slack.
</verification>

<success_criteria>
- Users have a single source of truth for project decisions.
- AI hallucinations can be globally corrected or hidden by any user.
- The UI maintains privacy boundaries by badgeing restricted links.
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-summaries-with-provenance/03-03-SUMMARY.md`
</output>
