# Plan 54-02: Confidence Scorer and Config Schema

## Goal

Define the ConfidenceScorer protocol and add escalation threshold configuration to openclaw.json schema.

## Context

From 54-CONTEXT.md:
- Confidence scoring interface needed for autonomy decisions
- Escalation threshold config to be added to openclaw.json
- Builds on Phase 52 suggestion system for confidence concepts

## Tasks

### 1. Define ConfidenceScorer protocol
- [ ] Create `packages/orchestration/src/openclaw/autonomy/confidence.py`
- [ ] Define `ConfidenceScorer` Protocol with `score(context: dict) -> float` method
- [ ] Define `ConfidenceFactors` dataclass (complexity, ambiguity, past_success, time_estimate)
- [ ] Add confidence score range validation (0.0 to 1.0)
- [ ] Create `ThresholdBasedScorer` implementation (initial default)

### 2. Create confidence calculation utilities
- [ ] Implement `calculate_complexity_score(task_description: str) -> float`
- [ ] Implement `estimate_time_factor(hours_estimate: float) -> float`
- [ ] Implement `past_success_factor(project: str, task_type: str) -> float` (stub for now)
- [ ] Create `aggregate_confidence(factors: ConfidenceFactors) -> float` function

### 3. Add escalation threshold config to openclaw.json schema
- [ ] Update `OPENCLAW_JSON_SCHEMA` in config_validator.py
- [ ] Add `autonomy` section with:
  - `escalation_threshold` (float, default 0.6)
  - `confidence_calculator` (enum: "threshold", "adaptive", default "threshold")
  - `max_retries` (int, default 1)
  - `blocked_timeout_minutes` (int, default 30)
- [ ] Add schema validation tests for new fields
- [ ] Update `openclaw.json.example` with new autonomy section

### 4. Create config access functions
- [ ] Add `get_autonomy_config()` to project_config.py
- [ ] Add `get_escalation_threshold() -> float` helper
- [ ] Add `get_confidence_calculator_type() -> str` helper
- [ ] Ensure env var override: `OPENCLAW_ESCALATION_THRESHOLD`

### 5. Wire config defaults into autonomy package
- [ ] Import config functions in autonomy module
- [ ] Create `AutonomyConfig` dataclass for runtime use
- [ ] Add config validation on autonomy package init
- [ ] Add `load_autonomy_config()` function

## Verification

- [ ] Schema validation test: Valid autonomy config passes validation
- [ ] Schema rejection test: Invalid threshold (1.5) fails validation
- [ ] Protocol test: Custom ConfidenceScorer implementation works
- [ ] Config access test: `get_escalation_threshold()` returns correct value
- [ ] Env override test: `OPENCLAW_ESCALATION_THRESHOLD=0.8` overrides config
- [ ] Confidence calculation test: Known inputs produce expected scores
- [ ] Integration test: AutonomyConfig loads from project config correctly

## Dependencies

- Plan 54-01 (core interfaces) — uses AutonomyContext
- Phase 46 schema validation infrastructure
- Phase 47 env var precedence patterns

## Notes

- Keep confidence calculator pluggable — "adaptive" is placeholder for future ML
- Threshold 0.6 is starting point, may tune based on usage
- Past success factor can be stub (returns 0.5) for now — real implementation later
- Follow existing config patterns from phase 45-47

## Time Estimate

1.5-2 hours
