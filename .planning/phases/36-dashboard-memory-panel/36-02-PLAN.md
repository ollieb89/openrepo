---
phase: 36-dashboard-memory-panel
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - workspace/occc/src/app/memory/page.tsx
  - workspace/occc/src/components/memory/MemoryPanel.tsx
  - workspace/occc/src/components/memory/MemoryTable.tsx
  - workspace/occc/src/components/memory/MemoryRow.tsx
  - workspace/occc/src/components/memory/MemoryFilters.tsx
  - workspace/occc/src/components/memory/MemoryStatBar.tsx
autonomous: true
requirements:
  - DSH-11
  - DSH-14

must_haves:
  truths:
    - "Navigating to /memory shows project-scoped memory items in a sortable table"
    - "Switching projects in the project selector refreshes the memory view"
    - "Each memory item row displays Type, Category, Agent, and Created columns"
    - "Clicking a row expands an accordion with full content and metadata"
    - "Expanded content is capped at ~300 characters with a show-more toggle"
    - "Filter dropdowns for Category, Agent source, and Type filter the table"
    - "Stats bar shows total count and per-agent breakdown"
    - "Pagination shows ~25 items per page with page numbers"
  artifacts:
    - path: "workspace/occc/src/app/memory/page.tsx"
      provides: "Next.js route for /memory"
      contains: "use client"
    - path: "workspace/occc/src/components/memory/MemoryPanel.tsx"
      provides: "Top-level layout orchestrating all sub-components"
      contains: "MemoryPanel"
    - path: "workspace/occc/src/components/memory/MemoryTable.tsx"
      provides: "Table with sortable columns and accordion rows"
      contains: "MemoryTable"
    - path: "workspace/occc/src/components/memory/MemoryRow.tsx"
      provides: "Individual row with expand/collapse"
      contains: "MemoryRow"
    - path: "workspace/occc/src/components/memory/MemoryFilters.tsx"
      provides: "Category/Agent/Type dropdown filters"
      contains: "MemoryFilters"
    - path: "workspace/occc/src/components/memory/MemoryStatBar.tsx"
      provides: "Item count and per-agent breakdown"
      contains: "MemoryStatBar"
  key_links:
    - from: "workspace/occc/src/app/memory/page.tsx"
      to: "workspace/occc/src/components/memory/MemoryPanel.tsx"
      via: "import and render"
      pattern: "import.*MemoryPanel"
    - from: "workspace/occc/src/components/memory/MemoryPanel.tsx"
      to: "workspace/occc/src/lib/hooks/useMemory.ts"
      via: "useMemory hook call"
      pattern: "useMemory\\("
    - from: "workspace/occc/src/components/memory/MemoryPanel.tsx"
      to: "workspace/occc/src/context/ProjectContext"
      via: "useProject hook for projectId"
      pattern: "useProject\\("
---

<objective>
Build the browse UI for the /memory page: page route, panel layout, sortable table with accordion rows, filter dropdowns, stats bar, and pagination.

Purpose: Implement the core browsing experience where operators can view project-scoped memory items, sort by columns, filter by category/agent/type, and expand rows to see full content and metadata. This plan covers the read-only table display; search and delete interactions are in Plan 03.

Output: A fully navigable /memory page with sortable table, accordion rows, filters, stats bar, and pagination.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dashboard-memory-panel/36-RESEARCH.md
@.planning/phases/36-dashboard-memory-panel/36-01-SUMMARY.md
@workspace/occc/src/lib/types/memory.ts
@workspace/occc/src/lib/hooks/useMemory.ts
@workspace/occc/src/context/ProjectContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page route + MemoryPanel + MemoryStatBar + MemoryFilters</name>
  <files>
    workspace/occc/src/app/memory/page.tsx
    workspace/occc/src/components/memory/MemoryPanel.tsx
    workspace/occc/src/components/memory/MemoryStatBar.tsx
    workspace/occc/src/components/memory/MemoryFilters.tsx
  </files>
  <action>
1. Create `workspace/occc/src/app/memory/page.tsx`:
   - `'use client';` directive (follows existing dashboard pattern — all pages are client components)
   - Import and render `<MemoryPanel />` as the sole child
   - No additional logic in the page file itself

2. Create `workspace/occc/src/components/memory/MemoryPanel.tsx`:
   - Import `useMemory` hook and `useProject` from context
   - Call `useMemory(projectId, searchQuery)` where `searchQuery` comes from local state (null in browse mode)
   - Page hierarchy (top to bottom per locked decision):
     a. Search bar placeholder area (just a div — Plan 03 will wire search)
     b. `<MemoryStatBar items={items} />`
     c. `<MemoryFilters ... />` with state for category, agentType, type filters
     d. `<MemoryTable items={filteredItems} ... />`
     e. Pagination controls at bottom
   - Client-side filtering: apply selected category/agent/type filters to `items` before passing to table
   - Client-side pagination: `PAGE_SIZE = 25`, slice filtered items by current page
   - Default sort: newest first (sort items by `created_at` descending)
   - Sortable columns state: `sortField` (Type|Category|Agent|Created) and `sortDirection` (asc|desc)
   - Loading state: show a skeleton/spinner while `isLoading` is true
   - Error state: show a user-friendly error message if `error` is truthy
   - Empty state: "No memory items found" message when items array is empty

3. Create `workspace/occc/src/components/memory/MemoryStatBar.tsx`:
   - Props: `{ items: MemoryItem[] }`
   - Compute stats from items (client-side, no extra API call):
     - `total` = items.length
     - `byAgent` = reduce items to `Record<string, number>` keyed by `agent_type ?? 'unknown'`
   - Render: `Memory | {total} items | l2_pm: {n} · l3_code: {n} · l3_test: {n}`
   - Use muted text color (gray-500/gray-400 dark), compact single-line layout
   - Only show agent types that have >0 items

4. Create `workspace/occc/src/components/memory/MemoryFilters.tsx`:
   - Props: `{ items: MemoryItem[]; category: string | null; agentType: string | null; type: string | null; onCategoryChange, onAgentTypeChange, onTypeChange }`
   - Dynamically compute available filter values from the `items` array (not static hardcoded lists)
   - Three `<select>` dropdowns in a horizontal row: Category, Agent Source, Type
   - Each dropdown has "All" as default option (null value)
   - Styled with Tailwind: `bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-md px-3 py-1.5 text-sm`
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Navigate to http://localhost:6987/memory — page should render with stats bar and filter dropdowns</manual>
  </verify>
  <done>Memory page route exists; MemoryPanel orchestrates data flow from useMemory hook; stats bar shows per-agent counts; filter dropdowns populated dynamically from data</done>
</task>

<task type="auto">
  <name>Task 2: MemoryTable + MemoryRow with sortable columns and accordion</name>
  <files>
    workspace/occc/src/components/memory/MemoryTable.tsx
    workspace/occc/src/components/memory/MemoryRow.tsx
  </files>
  <action>
1. Create `workspace/occc/src/components/memory/MemoryTable.tsx`:
   - Props: `{ items: MemoryItem[]; sortField: string; sortDirection: 'asc' | 'desc'; onSort: (field: string) => void; expandedId: string | null; onToggleExpand: (id: string) => void; selectedIds: Set<string>; onToggleSelect: (id: string) => void; onSelectAll: () => void; onDeleteItem: (id: string) => void }`
   - Table header with 5 columns: checkbox (for bulk select), Type, Category, Agent, Created
   - Clickable column headers (Type, Category, Agent, Created) toggle sort — show sort arrow indicator (up/down chevron) on active sort column
   - Checkbox column header: "select all on current page" toggle
   - Map over items and render `<MemoryRow>` for each
   - Tailwind table styling: `min-w-full divide-y divide-gray-200 dark:divide-gray-700`
   - Empty state handled by parent (MemoryPanel)

2. Create `workspace/occc/src/components/memory/MemoryRow.tsx`:
   - Props: `{ item: MemoryItem; isExpanded: boolean; onToggle: () => void; isSelected: boolean; onToggleSelect: () => void; onDelete: () => void }`
   - Collapsed row: checkbox, type badge, category badge, agent_type badge, formatted created_at
   - Badges: small colored pills — use different bg colors per agent_type (e.g., blue for l2_pm, green for l3_code, purple for l3_test)
   - Created_at: format as relative time if <24h ("2h ago"), otherwise as short date ("Feb 24")
   - Click anywhere on the row (except checkbox) toggles expand/collapse
   - Expanded row: `<tr>` with `<td colSpan={5}>` containing:
     a. Full content text — capped at ~300 characters initially, with a "Show more" / "Show less" toggle button
     b. Additional metadata not in columns (user_id, id, any extra fields from the catch-all)
     c. Delete button (red, styled with `bg-red-600 text-white px-3 py-1 rounded text-sm`) — calls `onDelete()`
   - Expanded row background: `bg-gray-50 dark:bg-gray-800/50` to visually separate from collapsed rows
   - Hover state on collapsed rows: `hover:bg-gray-50 dark:hover:bg-gray-700/50 cursor-pointer`
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Memory page table renders rows; clicking a row expands accordion with content and metadata; columns are sortable</manual>
  </verify>
  <done>Sortable table with Type/Category/Agent/Created columns; accordion rows expand to show full content (300-char cap with show-more toggle), metadata, and delete button; checkbox selection for bulk operations</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — all components compile
2. Navigating to /memory shows a table with memory items from the selected project
3. Clicking column headers changes sort order
4. Filter dropdowns narrow the visible items
5. Stats bar shows total count and per-agent breakdown
6. Clicking a row expands accordion with full content and metadata
7. Pagination shows 25 items per page with page number controls
</verification>

<success_criteria>
- /memory page renders with project-scoped memory items
- Table columns sortable by Type, Category, Agent, Created
- Accordion rows expand inline with full content (300-char cap + show-more) and metadata
- Filters narrow by Category, Agent source, Type
- Stats bar shows total + per-agent counts
- Pagination at 25 items/page
- Loading, error, and empty states handled
</success_criteria>

<output>
After completion, create `.planning/phases/36-dashboard-memory-panel/36-02-SUMMARY.md`
</output>
