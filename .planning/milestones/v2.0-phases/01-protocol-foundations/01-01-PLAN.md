---
phase: 01-protocol-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openclaw/src/acp/server-translator.ts
  - openclaw/src/gateway/server-http.ts
  - openclaw/src/gateway/server.impl.ts
  - openclaw/src/gateway/server-runtime-state.ts
  - openclaw/src/acp/server-translator.test.ts
  - openclaw/src/gateway/server-http.test.ts
  - openclaw/test/e2e/server.acp.test.ts
autonomous: true
requirements: [HYB-01, HYB-02, HYB-04]

must_haves:
  truths:
    - "Gateway accepts WebSocket upgrades on /v1/acp"
    - "ACP clients can connect and initialize sessions without the 'openclaw acp' bridge"
    - "ACP messages are dispatched to internal Gateway handlers with minimal latency"
  artifacts:
    - path: "openclaw/src/acp/server-translator.ts"
      provides: "Internal ACP-to-Gateway translation logic"
    - path: "openclaw/src/gateway/server-http.ts"
      provides: "/v1/acp endpoint routing"
  key_links:
    - from: "openclaw/src/gateway/server-http.ts"
      to: "openclaw/src/acp/server-translator.ts"
      via: "WebSocket connection handling"
---

<objective>
Implement a native ACP (Agent Client Protocol) WebSocket endpoint in the Gateway server to enable high-performance, low-latency communication between IDEs/tools and OpenClaw agents.
</objective>

<execution_context>
@/home/ob/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@openclaw/src/gateway/server.impl.ts
@openclaw/src/gateway/server-http.ts
@openclaw/src/acp/translator.ts
@openclaw/src/acp/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServerAcpAgent for in-process dispatching</name>
  <files>openclaw/src/acp/server-translator.ts, openclaw/src/acp/server-translator.test.ts</files>
  <action>
    1. Create a new `ServerAcpAgent` (or refactor `AcpGatewayAgent`) to handle ACP messages by dispatching them directly to the Gateway's internal request handlers.
    2. Reuse translation logic from `openclaw/src/acp/translator.ts`.
    3. Implement a dispatcher that calls `GatewayRequestHandlers` (coreGatewayHandlers) directly.
    4. Create `openclaw/src/acp/server-translator.test.ts` to verify that ACP 'prompt' messages correctly trigger internal Gateway calls.
  </action>
  <verify>
    <automated>pnpm test openclaw/src/acp/server-translator.test.ts</automated>
  </verify>
  <done>Server-side ACP translator exists, can dispatch messages, and is verified by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Register /v1/acp endpoint in Gateway</name>
  <files>openclaw/src/gateway/server-http.ts, openclaw/src/gateway/server-runtime-state.ts, openclaw/src/gateway/server-http.test.ts</files>
  <action>
    1. Update `attachGatewayUpgradeHandler` in `server-http.ts` to recognize the `/v1/acp` path.
    2. In `createGatewayRuntimeState`, initialize the ACP WebSocket server logic.
    3. Ensure authentication is applied using `authorizeGatewayConnect`.
    4. Create `openclaw/src/gateway/server-http.test.ts` with a test case that attempts a WebSocket upgrade to `/v1/acp` and confirms a 101 Switching Protocols response.
  </action>
  <verify>
    <automated>pnpm test openclaw/src/gateway/server-http.test.ts --grep "/v1/acp"</automated>
  </verify>
  <done>Gateway accepts WebSocket connections on /v1/acp and is verified by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ACP translator to WebSocket events</name>
  <files>openclaw/src/gateway/server.impl.ts, openclaw/test/e2e/server.acp.test.ts</files>
  <action>
    1. Connect the `/v1/acp` handler to the `ServerAcpAgent` in `server.impl.ts`.
    2. Map WebSocket messages to translator methods and vice versa.
    3. Create `openclaw/test/e2e/server.acp.test.ts` which uses a real WebSocket client to connect to the Gateway, perform an ACP 'initialize', and send a test prompt.
  </action>
  <verify>
    <automated>pnpm vitest openclaw/test/e2e/server.acp.test.ts</automated>
  </verify>
  <done>End-to-end communication works via the native ACP endpoint and is verified by E2E tests.</done>
</task>

</tasks>

<success_criteria>
- Gateway has a functional /v1/acp endpoint.
- Standard ACP clients can connect and perform an 'initialize' handshake.
- Latency is reduced by removing the CLI bridge overhead.
- All new functionality is covered by automated unit and E2E tests.
</success_criteria>
