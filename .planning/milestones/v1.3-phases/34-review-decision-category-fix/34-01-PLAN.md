---
phase: 34-review-decision-category-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestration/snapshot.py
  - tests/test_l2_review_memorization.py
  - tests/test_spawn_memory.py
autonomous: true
requirements: [MEM-02, RET-02]

must_haves:
  truths:
    - "_memorize_review_decision() sends category='review_decision' in the memU POST payload"
    - "_format_memory_context() routes items with category=='review_decision' to 'Past Review Outcomes' section"
    - "Items without a category field still route to 'Past Work Context' section (backward compatibility)"
  artifacts:
    - path: "orchestration/snapshot.py"
      provides: "category field in review decision memorize payload"
      contains: '"category": "review_decision"'
    - path: "tests/test_l2_review_memorization.py"
      provides: "payload category field assertion test"
      contains: "test_memorize_review_decision_sends_category_field"
    - path: "tests/test_spawn_memory.py"
      provides: "round-trip routing test for category field"
      contains: "test_review_decision_category_routes_to_review_section"
  key_links:
    - from: "orchestration/snapshot.py"
      to: "memU /memorize endpoint"
      via: "httpx.Client POST with category field in payload"
      pattern: '"category".*"review_decision"'
    - from: "skills/spawn_specialist/spawn.py"
      to: "SOUL template"
      via: "_format_memory_context() routing on item.get('category')"
      pattern: 'item\.get\("category"'
---

<objective>
Add the missing `category: "review_decision"` field to the `_memorize_review_decision()` payload in snapshot.py, and add tests that verify the payload construction and round-trip routing into the correct SOUL section.

Purpose: Close the integration gap where review decisions stored without a category field rely on the fragile `agent_type == "l2_pm"` fallback instead of the primary `category == "review_decision"` routing path in `_format_memory_context()`.

Output: One-line fix in snapshot.py, new payload test in test_l2_review_memorization.py, round-trip routing test in test_spawn_memory.py, backward-compat guard test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-review-decision-category-fix/34-RESEARCH.md
@.planning/phases/30-l2-review-decision-memorization/30-01-SUMMARY.md
@.planning/phases/30-l2-review-decision-memorization/30-02-SUMMARY.md
@orchestration/snapshot.py
@tests/test_l2_review_memorization.py
@tests/test_spawn_memory.py
@skills/spawn_specialist/spawn.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category field to review decision payload and write payload test</name>
  <files>orchestration/snapshot.py, tests/test_l2_review_memorization.py</files>
  <action>
In `orchestration/snapshot.py`, find the `_memorize_review_decision()` function's `payload` dict (around lines 74-81). Add `"category": "review_decision"` as a top-level field in the dict, alongside the existing `"resource_url"`, `"modality"`, and `"user"` fields. Use a plain string literal per user decision — no constants, no enums. Do NOT modify any other memorize functions.

The payload dict should look like:
```python
payload = {
    "resource_url": content,
    "modality": "conversation",
    "category": "review_decision",
    "user": {
        "user_id": project_id,
        "agent_type": AgentType.L2_PM.value,
    },
}
```

Then in `tests/test_l2_review_memorization.py`, add a new test `test_memorize_review_decision_sends_category_field` that:
1. Uses the existing `fake_thread_factory` pattern from `test_diff_summary_truncated_to_500` (lines 333-366) to intercept the POST payload from inside the daemon thread
2. Calls `_memorize_review_decision(project_id="proj", task_id="T-001", verdict="merge", reasoning="all tests pass")`
3. Asserts `payload_captured[0]["category"] == "review_decision"`
4. Asserts the category is at the top level of the dict (not nested under `"user"`)

Follow the existing `@patch("orchestration.project_config.get_memu_config", return_value=_MEMU_CFG_ENABLED)` decorator pattern used by other tests in that file.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_l2_review_memorization.py -v -k category</automated>
    <manual>Inspect snapshot.py payload dict to confirm category field is at top level</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>_memorize_review_decision() sends category="review_decision" at top level of memU payload; new test passes confirming the field is present and correctly positioned</done>
</task>

<task type="auto">
  <name>Task 2: Add round-trip routing test and backward-compat guard</name>
  <files>tests/test_spawn_memory.py</files>
  <action>
In `tests/test_spawn_memory.py`, add two new test functions:

1. `test_review_decision_category_routes_to_review_section` — Simulates a memory item returned by memU that was stored with the category field (as Task 1 now produces). Constructs a list with one dict: `{"resource_url": "# L2 Review Decision: task T-001\nVerdict: merge\nReasoning: all good", "category": "review_decision"}`. Passes it to `_format_memory_context()`. Asserts:
   - `"## Past Review Outcomes"` is in the result
   - `"## Past Work Context"` is NOT in the result (no work items)
   - `"Verdict: merge"` appears in the result

2. `test_item_without_category_routes_to_work_context` — Backward compatibility guard (required by user decision). Constructs a list with one dict: `{"resource_url": "Legacy memory item with no category"}` (no `category` key at all). Passes to `_format_memory_context()`. Asserts:
   - `"## Past Work Context"` is in the result
   - `"## Past Review Outcomes"` is NOT in the result
   - `"Legacy memory item"` appears in the result

Check if `test_format_work_only_no_review_section` already covers the backward-compat case adequately. If it does (items without category routing to work context), add a brief comment noting coverage overlap but still add the explicit test for documentation value per user decision.

Import `_format_memory_context` at the top if not already imported — it should already be imported given the existing tests in this file.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_spawn_memory.py -v -k "category_routes or without_category"</automated>
    <manual>Verify both new tests target the correct SOUL section routing</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Round-trip test confirms category="review_decision" items route to "Past Review Outcomes"; backward-compat test confirms items without category route to "Past Work Context"; full test suite green</done>
</task>

</tasks>

<verification>
Full test suite must pass with zero regressions:
```bash
python3 -m pytest tests/ -v
```

Specific phase verification:
```bash
python3 -m pytest tests/test_l2_review_memorization.py tests/test_spawn_memory.py -v
```

Confirm the one-line fix in snapshot.py:
```bash
grep -n '"category"' orchestration/snapshot.py
```
</verification>

<success_criteria>
1. `orchestration/snapshot.py` contains `"category": "review_decision"` in the `_memorize_review_decision()` payload dict
2. `test_memorize_review_decision_sends_category_field` passes — proving MEM-02 payload correctness
3. `test_review_decision_category_routes_to_review_section` passes — proving RET-02 round-trip routing
4. `test_item_without_category_routes_to_work_context` passes — proving backward compatibility
5. Full test suite green (all existing tests + 3 new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/34-review-decision-category-fix/34-01-SUMMARY.md`
</output>
