---
phase: 37-category-field-e2e-fix
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - skills/spawn_specialist/spawn.py
  - tests/test_spawn_memory.py
  - tests/test_l2_review_memorization.py
autonomous: true
requirements: [MEM-02, RET-02]

must_haves:
  truths:
    - "Items with category='review_decision' route to 'Past Review Outcomes' section via primary path"
    - "Items with category='task_outcome' route to 'Task Outcomes' section"
    - "Items without category fall back to agent_type routing (backward compat)"
    - "Output section order is: review decisions, then task outcomes, then uncategorized work context"
  artifacts:
    - path: "skills/spawn_specialist/spawn.py"
      provides: "CATEGORY_SECTION_MAP and three-bucket _format_memory_context()"
      contains: "CATEGORY_SECTION_MAP"
    - path: "tests/test_spawn_memory.py"
      provides: "Tests for category-primary routing, task_outcome section, ordering"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "memu-py retrieved items"
      via: "item.get('category') primary routing check"
      pattern: "CATEGORY_SECTION_MAP"
---

<objective>
Upgrade `_format_memory_context()` to support three-bucket routing with `CATEGORY_SECTION_MAP`, add the "Task Outcomes" section, enforce review-first output ordering, and write comprehensive tests.

Purpose: Plan 01 wired category through storage. This plan completes the retrieval side: when items come back from memU with a `category` field, the formatter routes them to the correct SOUL section via the primary path (not the agent_type fallback). This makes MEM-02 and RET-02 fully end-to-end.

Output: Updated formatter with three sections and correct ordering, plus tests proving all routing paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-category-field-e2e-fix/37-CONTEXT.md
@.planning/phases/37-category-field-e2e-fix/37-RESEARCH.md
@.planning/phases/37-category-field-e2e-fix/37-01-SUMMARY.md
@skills/spawn_specialist/spawn.py
@tests/test_spawn_memory.py
@tests/test_l2_review_memorization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CATEGORY_SECTION_MAP and upgrade _format_memory_context() to three-bucket routing</name>
  <files>
    skills/spawn_specialist/spawn.py
  </files>
  <action>
In `skills/spawn_specialist/spawn.py`, near `_format_memory_context()` (around line 201):

1. Add a module-level constant ABOVE the function:
   ```python
   CATEGORY_SECTION_MAP = {
       "review_decision": "Past Review Outcomes",
       "task_outcome": "Task Outcomes",
   }
   ```
   Per locked decision: hard-coded dict mapping, category determines section.

2. Refactor `_format_memory_context()` to use three buckets instead of two:
   - `work_bullets` (uncategorized/fallback — "Past Work Context")
   - `review_bullets` ("Past Review Outcomes")
   - `task_bullets` (NEW — "Task Outcomes")

3. Replace the existing dual-check routing logic (lines 237-239) with:
   ```python
   category = item.get("category", "")
   agent_type = item.get("agent_type", "")

   # Primary routing: category field (new items with category stored)
   if category in CATEGORY_SECTION_MAP:
       bucket_name = CATEGORY_SECTION_MAP[category]
   # Fallback: agent_type for legacy items without category
   elif agent_type == "l2_pm":
       bucket_name = "Past Review Outcomes"
   else:
       bucket_name = "Past Work Context"
   ```
   Then append the bullet to the appropriate bucket list based on `bucket_name`.

4. Change the output section assembly to enforce the locked decision ordering — review decisions FIRST, then task outcomes, then work context (uncategorized). Current code outputs work_bullets first, review_bullets second — this must be reversed:
   ```python
   sections = []
   if review_bullets:
       sections.append("## Past Review Outcomes\n\n" + "\n".join(review_bullets))
   if task_bullets:
       sections.append("## Task Outcomes\n\n" + "\n".join(task_bullets))
   if work_bullets:
       sections.append("## Past Work Context\n\n" + "\n".join(work_bullets))
   ```

5. Update the function docstring to document:
   - Three sections instead of two
   - CATEGORY_SECTION_MAP as primary routing
   - agent_type as fallback for legacy items
   - Output ordering: review -> task outcomes -> work context

WHY ordering change: CONTEXT.md locked decision says "Category-grouped output ordering: review decisions -> task outcomes -> uncategorized/fallback". The current code puts work context first — this must flip.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_spawn_memory.py -v -x</automated>
    <manual>Inspect _format_memory_context() has CATEGORY_SECTION_MAP, three buckets, and review-first output ordering.</manual>
  </verify>
  <done>_format_memory_context() routes items via CATEGORY_SECTION_MAP primary path with agent_type fallback. Three sections: Past Review Outcomes, Task Outcomes, Past Work Context. Output order is review-first per locked decision.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for category-primary routing, task_outcome section, ordering, and backward compat</name>
  <files>
    tests/test_spawn_memory.py
    tests/test_l2_review_memorization.py
  </files>
  <action>
In `tests/test_spawn_memory.py`:

1. **Update existing tests** that assert section output ordering. The current tests expect "## Past Work Context" before "## Past Review Outcomes" — the new ordering is reversed (review first). Read all existing tests and update any that assert ordering or parse the full output string.

2. **Add test `test_category_primary_routing_review_decision`**: Input items with `category="review_decision"` (no `agent_type` field). Assert they appear under "## Past Review Outcomes". This proves the primary path fires without the fallback.

3. **Add test `test_category_primary_routing_task_outcome`**: Input items with `category="task_outcome"`. Assert they appear under "## Task Outcomes" section heading. This is the NEW section that did not exist before.

4. **Add test `test_category_routing_with_mixed_categories`**: Input a mix of `category="review_decision"`, `category="task_outcome"`, and no-category items. Assert:
   - Three sections present in output
   - "## Past Review Outcomes" appears before "## Task Outcomes" which appears before "## Past Work Context"
   - Each item is in the correct section

5. **Add test `test_legacy_items_without_category_still_route_correctly`**: Input items with `agent_type="l2_pm"` but no `category` field. Assert they route to "## Past Review Outcomes" via fallback. This is the backward-compat guard (per locked decision: existing agent_type fallback remains for memories without a category).

6. **Add test `test_task_outcome_category_budget_shared`**: Input enough `task_outcome` items to approach the budget. Assert budget is shared across all three sections (same MEMORY_CONTEXT_BUDGET constant).

In `tests/test_l2_review_memorization.py`:

7. Review existing tests. No changes expected — these test the snapshot.py payload (which already has `category: "review_decision"` from Phase 34). Only update if the existing tests import or reference anything that changed.

Run full test suite to confirm zero regressions: `python3 -m pytest tests/ -v`
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_spawn_memory.py tests/test_l2_review_memorization.py -v -x</automated>
  </verify>
  <done>All routing paths tested: category-primary for review_decision and task_outcome, agent_type fallback for legacy items, mixed-category ordering, budget sharing. Existing tests updated for new ordering. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_spawn_memory.py -v` — all tests pass including new category routing tests
2. `python3 -m pytest tests/test_l2_review_memorization.py -v` — no regressions
3. `python3 -m pytest tests/ -v` — full suite passes
4. CATEGORY_SECTION_MAP exists as module-level constant in spawn.py
5. Output ordering is review -> task outcomes -> work context (inspect test assertions)
</verification>

<success_criteria>
- CATEGORY_SECTION_MAP defines routing for review_decision and task_outcome
- _format_memory_context() has three buckets with primary category routing and agent_type fallback
- Output sections ordered: Past Review Outcomes, Task Outcomes, Past Work Context
- New tests cover: category-primary routing (both values), mixed categories, fallback routing, budget sharing
- Existing tests updated for review-first ordering
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/37-category-field-e2e-fix/37-02-SUMMARY.md`
</output>
