---
phase: 16-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestration/snapshot.py
  - orchestration/config.py
  - orchestration/monitor.py
  - orchestration/soul_renderer.py
  - agents/_templates/soul-default.md
autonomous: true
requirements:
  - CFG-02
  - CFG-04
  - CFG-06

must_haves:
  truths:
    - "capture_semantic_snapshot() and cleanup_old_snapshots() require explicit project_id with no default fallback"
    - "create_staging_branch() delegates to _detect_default_branch() instead of duplicating inline detection"
    - "soul-default.md template body consumes $project_name and renders it correctly"
    - "STATE_FILE and SNAPSHOT_DIR constants no longer exist in config.py"
    - "monitor.py does not import STATE_FILE"
  artifacts:
    - path: "orchestration/snapshot.py"
      provides: "project_id-threaded snapshot functions and delegated branch detection"
      contains: "def capture_semantic_snapshot.*project_id: str"
    - path: "orchestration/config.py"
      provides: "Lock and poll constants only — no deprecated paths"
      contains: "LOCK_TIMEOUT"
    - path: "agents/_templates/soul-default.md"
      provides: "Default SOUL template consuming all build_variables() outputs"
      contains: "$project_name"
  key_links:
    - from: "orchestration/snapshot.py:capture_semantic_snapshot"
      to: "orchestration/project_config.py:get_snapshot_dir"
      via: "explicit project_id parameter pass-through"
      pattern: "get_snapshot_dir\\(project_id\\)"
    - from: "orchestration/snapshot.py:create_staging_branch"
      to: "orchestration/snapshot.py:_detect_default_branch"
      via: "function call replacing inline detection"
      pattern: "_detect_default_branch\\(workspace\\)"
---

<objective>
Fix the 3 cross-phase wiring issues identified by the v1.1 milestone audit: thread project_id through snapshot functions (CFG-02), replace duplicate inline branch detection with delegation to existing helper (CFG-06), add $project_name consumption to soul template (CFG-04), and remove deprecated constants from config.py.

Purpose: Complete the integration wiring left incomplete by Phases 11 and 12 so that all per-project path APIs are correctly consumed and the SOUL template renders all declared variables.
Output: Corrected orchestration layer with no deprecated constants and full variable coverage.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-fixes/16-RESEARCH.md
@.planning/phases/11-config-decoupling-foundation/11-01-SUMMARY.md
@.planning/phases/12-soul-templating/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread project_id through snapshot functions and delegate branch detection</name>
  <files>orchestration/snapshot.py</files>
  <action>
Two changes in snapshot.py:

**CFG-02 — project_id threading:**
1. Add `project_id: str` as a required positional parameter to `capture_semantic_snapshot()` (currently ~line 196). New signature: `def capture_semantic_snapshot(task_id: str, workspace_path: str, project_id: str) -> Tuple[Path, Dict[str, Any]]:`
2. Inside the function, change `get_snapshot_dir()` call to `get_snapshot_dir(project_id)`.
3. Add `project_id: str` as a required positional parameter to `cleanup_old_snapshots()` (currently ~line 485). Place it before the defaulted `max_snapshots` param. New signature: `def cleanup_old_snapshots(workspace_path: str, project_id: str, max_snapshots: int = 100) -> Dict[str, Any]:`
4. Inside the function, change `get_snapshot_dir()` call to `get_snapshot_dir(project_id)`.
5. Before modifying, grep all .py files for `capture_semantic_snapshot` and `cleanup_old_snapshots` to find all call sites. Update every caller to pass `project_id` explicitly. Per research, no external callers currently exist — but verify with grep first.

**CFG-06 — branch detection delegation:**
6. In `create_staging_branch()`, find the inline branch detection block (starts with `# Detect default branch (main or master)` comment, ~lines 129-154, ending with `except Exception: pass`). Replace the ENTIRE block (from the comment through the except/pass) with a single line: `default_branch = _detect_default_branch(workspace)`. Do NOT enhance `_detect_default_branch()` — call it as-is per user decision.

**Backward compatibility:** project_id is required with no default — any caller missing it fails immediately with a clear TypeError. This is intentional per locked decision.
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "
import inspect
from orchestration.snapshot import capture_semantic_snapshot, cleanup_old_snapshots, create_staging_branch
sig1 = inspect.signature(capture_semantic_snapshot)
sig2 = inspect.signature(cleanup_old_snapshots)
assert 'project_id' in sig1.parameters, 'capture_semantic_snapshot missing project_id'
assert sig1.parameters['project_id'].default is inspect.Parameter.empty, 'project_id must not have default'
assert 'project_id' in sig2.parameters, 'cleanup_old_snapshots missing project_id'
assert sig2.parameters['project_id'].default is inspect.Parameter.empty, 'project_id must not have default'
src = inspect.getsource(create_staging_branch)
assert '_detect_default_branch' in src, 'create_staging_branch must call _detect_default_branch'
assert 'symbolic-ref' not in src, 'create_staging_branch must not have inline symbolic-ref detection'
print('PASS: snapshot.py changes verified')
"</automated>
  </verify>
  <done>capture_semantic_snapshot() and cleanup_old_snapshots() require explicit project_id parameter with no default. create_staging_branch() delegates branch detection to _detect_default_branch() with no inline duplicate. All callers updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add $project_name consumption to soul-default.md and audit build_variables()</name>
  <files>agents/_templates/soul-default.md, orchestration/soul_renderer.py</files>
  <action>
**CFG-04 — template variable consumption:**
1. Open `agents/_templates/soul-default.md`.
2. In the `## HIERARCHY` section, add a new bullet line between "Subordinates" and "Scope": `- **Project:** $project_name`
3. Audit `build_variables()` in `orchestration/soul_renderer.py` to check all 8 declared variables:
   - `$project_name` — will be consumed after step 2 above
   - `$project_id` — determine if it should appear in the default template or is intentionally reserved for override files only. Research indicates it is likely intentional (overrides can use it, default template doesn't need it). If intentionally reserved, add a comment in `build_variables()` documenting this: `# project_id: available for override files; not consumed by soul-default.md`
   - `$agent_name`, `$tier` — consumed in title line by render_soul() directly (not in template body) — correct
   - `$workspace`, `$tech_stack_frontend`, `$tech_stack_backend`, `$tech_stack_infra` — consumed in template body — correct
4. Check for any dead code or unused variables in `build_variables()` beyond the $project_name fix. Clean up if found.

**Do NOT change the template engine** (string.Template.safe_substitute per Phase 12 decision).
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "
from pathlib import Path
import string

template_path = Path('agents/_templates/soul-default.md')
body = template_path.read_text()
assert '\$project_name' in body, 'soul-default.md must contain \$project_name'

# Verify it renders correctly
tmpl = string.Template(body)
result = tmpl.safe_substitute(project_name='TestProject', workspace='/workspace', tech_stack_frontend='React', tech_stack_backend='Python', tech_stack_infra='Docker')
assert 'TestProject' in result, '\$project_name must render to actual value'
assert '\$project_name' not in result, '\$project_name must not remain unresolved'
print('PASS: soul-default.md variable consumption verified')
"</automated>
  </verify>
  <done>$project_name appears in soul-default.md HIERARCHY section and renders correctly via safe_substitute. All build_variables() outputs are accounted for (consumed or documented as intentionally reserved).</done>
</task>

<task type="auto">
  <name>Task 3: Remove deprecated constants and clean up unused imports</name>
  <files>orchestration/config.py, orchestration/monitor.py</files>
  <action>
**Deprecated constant removal:**
1. Open `orchestration/config.py`. Delete the `STATE_FILE` block (the DEPRECATED comment + the `STATE_FILE = Path(...)` assignment, ~4 lines). Delete the `SNAPSHOT_DIR` block (the DEPRECATED comment + the `SNAPSHOT_DIR = Path(...)` assignment, ~4 lines).
2. After deletion, check if `import os` and `from pathlib import Path` are still used by remaining code. They should NOT be (only `LOCK_TIMEOUT`, `LOCK_RETRY_ATTEMPTS`, `POLL_INTERVAL` remain, which are plain literals). Remove the unused imports.
3. The final config.py should contain only:
   ```
   # Lock configuration
   LOCK_TIMEOUT = 5  # seconds
   LOCK_RETRY_ATTEMPTS = 3

   # Polling configuration
   POLL_INTERVAL = 1.0  # seconds
   ```

**monitor.py import cleanup:**
4. Open `orchestration/monitor.py`. Find the import line `from .config import STATE_FILE, POLL_INTERVAL` (and the direct-execution variant `from orchestration.config import STATE_FILE, POLL_INTERVAL`). Remove `STATE_FILE` from both import statements, keeping `POLL_INTERVAL`.
5. Grep monitor.py for any other `STATE_FILE` references — remove all.

**Broad unused import audit:**
6. For each file in `orchestration/*.py`, check for imports that are no longer used after these changes. Specifically audit: `config.py`, `monitor.py`, `snapshot.py`, `soul_renderer.py`, `project_config.py`, `state_engine.py`, `init.py`. Remove any unused imports found.
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "
from orchestration import config
assert not hasattr(config, 'STATE_FILE'), 'STATE_FILE must not exist in config'
assert not hasattr(config, 'SNAPSHOT_DIR'), 'SNAPSHOT_DIR must not exist in config'
assert hasattr(config, 'LOCK_TIMEOUT'), 'LOCK_TIMEOUT must still exist'
assert hasattr(config, 'POLL_INTERVAL'), 'POLL_INTERVAL must still exist'

# Verify config.py source has no import os or from pathlib
import inspect
src = inspect.getsource(config)
assert 'import os' not in src, 'config.py should not import os'
assert 'from pathlib' not in src, 'config.py should not import pathlib'

# Verify monitor.py does not reference STATE_FILE
from orchestration import monitor
msrc = inspect.getsource(monitor)
assert 'STATE_FILE' not in msrc, 'monitor.py must not reference STATE_FILE'
print('PASS: deprecated constants removed, imports cleaned')
"</automated>
  </verify>
  <done>STATE_FILE and SNAPSHOT_DIR deleted from config.py. Unused os/pathlib imports removed from config.py. STATE_FILE removed from monitor.py imports. No unused imports remain in orchestration layer.</done>
</task>

</tasks>

<verification>
All three checks must pass in sequence:
1. `python3 -c "from orchestration.snapshot import capture_semantic_snapshot; import inspect; sig=inspect.signature(capture_semantic_snapshot); assert 'project_id' in sig.parameters"` — CFG-02
2. `python3 -c "from orchestration.snapshot import create_staging_branch; import inspect; assert '_detect_default_branch' in inspect.getsource(create_staging_branch)"` — CFG-06
3. `python3 -c "from pathlib import Path; assert '\$project_name' in Path('agents/_templates/soul-default.md').read_text()"` — CFG-04
4. `python3 -c "from orchestration import config; assert not hasattr(config, 'STATE_FILE')"` — constant cleanup
</verification>

<success_criteria>
- capture_semantic_snapshot() and cleanup_old_snapshots() have required project_id parameter
- create_staging_branch() calls _detect_default_branch() with no inline duplicate logic
- soul-default.md contains and renders $project_name
- config.py contains only LOCK_TIMEOUT, LOCK_RETRY_ATTEMPTS, POLL_INTERVAL
- monitor.py does not import or reference STATE_FILE
- No unused imports in orchestration layer
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-fixes/16-01-SUMMARY.md`
</output>
