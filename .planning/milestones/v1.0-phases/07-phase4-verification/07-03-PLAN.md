---
phase: 07-phase4-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/occc/src/app/api/swarm/route.ts
  - workspace/occc/src/app/api/swarm/stream/route.ts
autonomous: true
requirements: [DSH-02]
gap_closure: true

must_haves:
  truths:
    - "SSE stream at /api/swarm/stream emits a data: event with full swarm state immediately on connection"
    - "SSE stream emits updated swarm state data: events when workspace-state.json changes"
    - "Keepalive comments continue between state updates"
  artifacts:
    - path: "workspace/occc/src/app/api/swarm/stream/route.ts"
      provides: "SSE endpoint that emits actual swarm state as data: events"
      contains: "getSwarmState"
    - path: "workspace/occc/src/app/api/swarm/route.ts"
      provides: "Exported getSwarmState function reusable by stream endpoint"
      exports: ["getSwarmState", "GET"]
  key_links:
    - from: "workspace/occc/src/app/api/swarm/stream/route.ts"
      to: "workspace/occc/src/app/api/swarm/route.ts"
      via: "import { getSwarmState }"
      pattern: "getSwarmState"
    - from: "workspace/occc/src/app/api/swarm/stream/route.ts"
      to: "workspace/.openclaw/workspace-state.json"
      via: "fs.stat mtime polling triggering getSwarmState()"
      pattern: "getSwarmState\\(\\)"
---

<objective>
Fix the SSE stream endpoint at `/api/swarm/stream` so it emits actual `data:` events with full swarm state, closing the UAT gap where only keepalive comments were sent.

Purpose: The dashboard's real-time update mechanism depends on this SSE stream emitting state payloads. Currently it connects but never sends data, breaking live refresh.
Output: Working SSE stream that sends initial state on connect and updated state on file change.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase4-verification/07-UAT.md
@workspace/occc/src/app/api/swarm/route.ts
@workspace/occc/src/app/api/swarm/stream/route.ts
@workspace/occc/src/app/api/logs/[agent]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export getSwarmState and fix SSE stream to emit real data</name>
  <files>
    workspace/occc/src/app/api/swarm/route.ts
    workspace/occc/src/app/api/swarm/stream/route.ts
  </files>
  <action>
Two changes in a single task since they are tightly coupled:

**1. Export getSwarmState from route.ts**

In `workspace/occc/src/app/api/swarm/route.ts`, add `export` keyword to the `getSwarmState` function declaration (line 39). Change:
```typescript
async function getSwarmState()
```
to:
```typescript
export async function getSwarmState()
```
No other changes to this file. The GET handler and all other code remain unchanged.

**2. Rewrite stream/route.ts to emit actual state data**

Replace the entire contents of `workspace/occc/src/app/api/swarm/stream/route.ts` with a corrected implementation that follows the pattern from the working `/api/logs/[agent]/route.ts` endpoint. Specifically:

- Import `getSwarmState` from `@/app/api/swarm/route` (the parent route)
- Import `promises as fs` from `fs` for mtime checking
- In the `start(controller)` callback:
  1. Immediately call `getSwarmState()` and send the result as the FIRST `data:` event (like the logs endpoint sends `{connected: true}` immediately)
  2. Store the initial mtime from `fs.stat()` of the state file
- In the polling interval (keep 1-second interval):
  1. Check mtime via `fs.stat()`
  2. When mtime changes, call `getSwarmState()` and emit the FULL state object as a `data:` event (NOT just `{updated: true}`)
  3. Update stored mtime
- Keep the keepalive mechanism (`: keepalive\n\n` every 30 seconds) matching the logs endpoint pattern
- Keep the cleanup/abort handling pattern from the current implementation
- Use `JSON.stringify()` for the data payload, same as the logs endpoint does with `JSON.stringify(entry)`

The state file path constant: `'/home/ollie/.openclaw/workspace/.openclaw/workspace-state.json'` with env var override via `process.env.STATE_FILE || process.env.SWARM_STATE_FILE`.

Key behavioral differences from current broken code:
- MUST send initial data event before any polling (current code skips first cycle)
- MUST send full state object from getSwarmState() on change (current code sends bare `{updated: true}`)
- Keepalive every 30s using a counter or separate interval (current 30-tick counter at 1s interval is fine, but align with logs endpoint style)
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      1. Start dashboard: cd workspace/occc && bun run dev
      2. Run: curl -N http://localhost:6987/api/swarm/stream
      3. Expect FIRST line to be: data: {"agents":[...],"metrics":{...},"state":{...},"lastUpdated":"..."}
      4. Wait — should see keepalive comments every 30s
      5. Touch the state file: touch workspace/.openclaw/workspace-state.json
      6. Expect another data: event with full state JSON within 1-2 seconds
    </manual>
  </verify>
  <done>
    - curl to /api/swarm/stream immediately receives a data: event with full swarm state JSON (agents, metrics, state, lastUpdated)
    - Touching workspace-state.json triggers another data: event with full state within 2 seconds
    - Keepalive comments sent every 30 seconds between updates
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors from the export change or new imports)
2. `curl -N http://localhost:6987/api/swarm/stream` emits at least one `data:` line with JSON containing `agents` key within 2 seconds of connection
3. After `touch workspace/.openclaw/workspace-state.json`, another `data:` event appears in the stream
</verification>

<success_criteria>
- SSE stream emits initial full swarm state as first data: event on connection
- SSE stream emits updated full swarm state on workspace-state.json mtime change
- Keepalive comments sent between state updates
- TypeScript compiles cleanly
- UAT test 3 ("SSE Stream Connects") would now pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase4-verification/07-03-SUMMARY.md`
</output>
