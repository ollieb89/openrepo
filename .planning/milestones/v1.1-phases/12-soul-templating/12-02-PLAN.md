---
phase: 12-soul-templating
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - orchestration/soul_renderer.py
  - scripts/verify_soul_golden.py
autonomous: true
requirements:
  - CFG-04
  - CFG-05

must_haves:
  truths:
    - "A standalone golden baseline verification script confirms zero diff between rendered PumplAI SOUL and the v1.0 hardcoded file"
    - "The soul renderer can be invoked from the command line with --project flag"
    - "The renderer writes the output file to the agent directory when --write flag is provided"
    - "A project without a soul-override.md gets a complete SOUL.md from the default template alone"
  artifacts:
    - path: "scripts/verify_soul_golden.py"
      provides: "Golden baseline verification script"
      contains: "render_soul"
    - path: "orchestration/soul_renderer.py"
      provides: "CLI entry point via __main__ block"
      contains: "if __name__"
  key_links:
    - from: "scripts/verify_soul_golden.py"
      to: "orchestration/soul_renderer.py"
      via: "import render_soul"
      pattern: "from orchestration.soul_renderer import render_soul"
    - from: "orchestration/soul_renderer.py"
      to: "agents/<l2_pm_id>/agent/SOUL.md"
      via: "--write flag writes rendered output to agent directory"
      pattern: "write.*agent.*SOUL"
---

<objective>
Add CLI entry point to the soul renderer and create the golden baseline verification script.

Purpose: Provide a command-line interface for rendering SOUL files (used by Phase 14's `openclaw project init`) and a verification script that proves the PumplAI golden baseline is reproducible. Also validate that the renderer works for projects without overrides (new project scenario).

Output: CLI-enabled `orchestration/soul_renderer.py`, `scripts/verify_soul_golden.py` verification script.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-soul-templating/12-RESEARCH.md
@.planning/phases/12-soul-templating/12-01-SUMMARY.md
@orchestration/soul_renderer.py
@agents/pumplai_pm/agent/SOUL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI entry point to soul_renderer.py and write_soul() function</name>
  <files>orchestration/soul_renderer.py</files>
  <action>
Add to the existing `orchestration/soul_renderer.py`:

1. **`write_soul(project_id: str, output_path: Optional[Path] = None) -> Path`** — Renders SOUL.md and writes it to disk. If `output_path` is None, derive the default output path from the project config: `agents/<l2_pm_id>/agent/SOUL.md` (using `config["agents"]["l2_pm"]`). Creates parent directories if needed. Returns the path written to.

2. **`if __name__ == "__main__":` block** with `argparse`:
   - `--project` (required): Project ID to render SOUL for
   - `--write` (flag, optional): Write the rendered SOUL.md to the agent directory (default: print to stdout)
   - `--output` (optional): Custom output path (overrides default agent directory path). Only used with `--write`.

   Usage examples:
   ```
   python3 orchestration/soul_renderer.py --project pumplai          # Print to stdout
   python3 orchestration/soul_renderer.py --project pumplai --write  # Write to agents/pumplai_pm/agent/SOUL.md
   python3 orchestration/soul_renderer.py --project pumplai --write --output /tmp/SOUL.md
   ```

Use `argparse` (consistent with existing monitor.py/spawn.py pattern — locked decision from STATE.md).
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 orchestration/soul_renderer.py --project pumplai | diff - agents/pumplai_pm/agent/SOUL.md</automated>
    <manual>Running with --write should create/overwrite the SOUL.md file in the agent directory</manual>
  </verify>
  <done>CLI entry point works: `--project pumplai` prints golden-matching SOUL to stdout; `--write` writes to `agents/pumplai_pm/agent/SOUL.md`; `--output` writes to custom path. All using `argparse`.</done>
</task>

<task type="auto">
  <name>Task 2: Create golden baseline verification script and test new-project scenario</name>
  <files>scripts/verify_soul_golden.py</files>
  <action>
Create `scripts/verify_soul_golden.py` — a standalone verification script that:

1. Renders the PumplAI SOUL via `render_soul("pumplai")`
2. Reads the golden baseline from `agents/pumplai_pm/agent/SOUL.md`
3. Compares them byte-for-byte
4. If match: prints `PASS: golden baseline diff is empty` and exits 0
5. If mismatch: prints `FAIL: non-empty diff:` followed by `difflib.unified_diff()` output and exits 1

Additionally, the script should include a second check for the "new project without override" scenario:
6. Call `render_soul()` with variables for a hypothetical project (no override file). Verify the result:
   - Contains the title line `# Soul: ...`
   - Contains all three default template sections (HIERARCHY, CORE GOVERNANCE, BEHAVIORAL PROTOCOLS)
   - Does NOT contain any `$` variable references (all substituted or left as empty strings via `safe_substitute`)

Use `sys.path.insert(0, ...)` to make imports work when running from the project root. Use `difflib` for diff output (per RESEARCH.md recommendation).

The script must be runnable as:
```
cd ~/.openclaw && python3 scripts/verify_soul_golden.py
```
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 scripts/verify_soul_golden.py</automated>
  </verify>
  <done>`scripts/verify_soul_golden.py` runs successfully, printing `PASS: golden baseline diff is empty` and verifying both the PumplAI golden baseline (exact match) and the new-project-without-override scenario (complete SOUL from defaults).</done>
</task>

</tasks>

<verification>
1. `python3 orchestration/soul_renderer.py --project pumplai | diff - agents/pumplai_pm/agent/SOUL.md` produces no output (zero diff)
2. `python3 scripts/verify_soul_golden.py` exits 0 with PASS message
3. `python3 orchestration/soul_renderer.py --project pumplai --write --output /tmp/test-soul.md && diff /tmp/test-soul.md agents/pumplai_pm/agent/SOUL.md` produces no output
</verification>

<success_criteria>
- CLI entry point works with `--project`, `--write`, `--output` flags
- Golden baseline verification script passes (exit 0)
- New-project scenario produces a complete SOUL from defaults
- `write_soul()` function creates the file at the correct agent directory path
</success_criteria>

<output>
After completion, create `.planning/phases/12-soul-templating/12-02-SUMMARY.md`
</output>
