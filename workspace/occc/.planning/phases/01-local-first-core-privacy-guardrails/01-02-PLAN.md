---
phase: 01-local-first-core-privacy-guardrails
plan: 02
type: execute
wave: 2
depends_on: [01-01]
requirements: [PRIV-02]
files_modified:
  - src/lib/privacy/audit-log.ts
  - src/app/api/privacy/events/route.ts
  - src/app/api/privacy/settings/route.ts
  - src/components/privacy/PrivacyCenter.tsx
  - src/components/common/InferenceBadge.tsx
  - src/lib/hooks/usePrivacy.ts
  - src/app/settings/privacy/page.tsx
  - src/app/page.tsx
  - tests/privacy/privacy-guard.test.ts
autonomous: true
must_haves:
  truths:
    - "Users can manage remote inference consent per project in a dedicated Privacy Center."
    - "Responses that used remote inference show an explicit badge and short reason."
    - "When remote is denied in low-confidence cases, users receive local-only output with a clear improvement note."
    - "Remote inference usage is visible in a filterable audit log."
  artifacts:
    - path: "src/components/privacy/PrivacyCenter.tsx"
      provides: "Privacy control surface with project-scoped consent and revoke action."
    - path: "src/components/common/InferenceBadge.tsx"
      provides: "Inline remote/local provenance badge and reason display."
    - path: "src/lib/privacy/audit-log.ts"
      provides: "Remote inference event persistence and filtering support."
    - path: "src/app/api/privacy/events/route.ts"
      provides: "Audit event retrieval for Privacy Center."
    - path: "src/app/api/privacy/settings/route.ts"
      provides: "Privacy settings update/read API."
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/common/InferenceBadge.tsx"
      via: "response rendering includes provenance badge"
      pattern: "badge rendered with mode and reason"
    - from: "src/components/privacy/PrivacyCenter.tsx"
      to: "src/app/api/privacy/events/route.ts"
      via: "fetch filterable remote-use events"
      pattern: "privacy events query/filter path"
    - from: "src/components/privacy/PrivacyCenter.tsx"
      to: "src/app/api/privacy/settings/route.ts"
      via: "consent toggle and revoke actions"
      pattern: "project-scoped consent mutation"
---

# Phase 01-02 Plan: Privacy UX, Auditability, and Deny-Path Messaging

## Objective
Deliver user-visible trust controls and evidence surfaces, including explicit messaging when users deny remote inference.

## Context
- `.planning/phases/01-local-first-core-privacy-guardrails/01-CONTEXT.md`
- `.planning/phases/01-local-first-core-privacy-guardrails/01-RESEARCH.md`
- `.planning/phases/01-local-first-core-privacy-guardrails/01-01-PLAN.md`

<tasks>
  <task id="P01-02-T01" wave="2" type="auto">
    <name>Add Privacy Center, inline badges, filterable audit log, and explicit deny-path local-only message</name>
    <title>Add Privacy Center, inline badges, filterable audit log, and explicit deny-path local-only message</title>
    <requirements>PRIV-02</requirements>
    <depends_on>P01-01-T01</depends_on>
    <files>
      <file>src/lib/privacy/audit-log.ts</file>
      <file>src/app/api/privacy/events/route.ts</file>
      <file>src/app/api/privacy/settings/route.ts</file>
      <file>src/components/privacy/PrivacyCenter.tsx</file>
      <file>src/components/common/InferenceBadge.tsx</file>
      <file>src/lib/hooks/usePrivacy.ts</file>
      <file>src/app/settings/privacy/page.tsx</file>
      <file>src/app/page.tsx</file>
      <file>tests/privacy/privacy-guard.test.ts</file>
    </files>
    <action>Implement dedicated Privacy Center UI with project-scoped opt-in toggle and one-click revoke action. Add inline inference badges to generated response surfaces that show remote/local mode and short reason when remote path was used. Persist remote inference events in a filterable audit log (project, mode, reason, connector, time range) and expose retrieval API for Privacy Center. Implement explicit deny-path behavior from locked decision: when remote inference is denied (user consent absent/revoked), return local-only output plus a user-visible note that remote inference could improve output quality.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/privacy/privacy-guard.test.ts</automated>
      <manual>In UI, deny remote for one project and trigger a low-confidence flow; verify response stays local-only and shows a note that remote could improve output. Then opt in and verify response shows remote badge + reason and event appears in Privacy Center audit log.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Users can view/change project-scoped remote consent, inspect remote-use events, and always see explicit local-only deny messaging or remote-use badge/reason in responses.</done>
  </task>
</tasks>

## Verification Criteria
- Deny-path note appears whenever remote is not permitted in low-confidence scenarios.
- Remote-used responses show explicit badge and reason with corresponding audit log events.

## Must Haves
<must_haves>
  <must_have id="MH-03" requirement="PRIV-02">Privacy Center provides one-click revoke and current consent state per project/workspace.</must_have>
  <must_have id="MH-04" requirement="PRIV-02">Any response produced through remote inference displays an explicit badge and short reason.</must_have>
  <must_have id="MH-05" requirement="PRIV-02">When remote inference is denied, system returns local-only result with explicit note that remote could improve output.</must_have>
</must_haves>
