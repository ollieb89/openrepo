---
phase: 39-graceful-sentinel
plan: "04"
type: execute
wave: 1
depends_on: ["39-03"]
files_modified:
  - skills/spawn_specialist/pool.py
  - tests/test_recovery_scan.py
autonomous: true
gap_closure: true
requirements: [REL-04, REL-05, REL-06, REL-07, REL-08]

must_haves:
  truths:
    - "Pool startup automatically calls run_recovery_scan() before processing any new spawn — no manual intervention required"
    - "spawn_task() awaits run_recovery_scan() before spawn_and_monitor(), ensuring orphaned tasks are handled first"
  artifacts:
    - path: "skills/spawn_specialist/pool.py"
      provides: "run_recovery_scan() call in spawn_task() before spawn_and_monitor()"
      contains: "await pool.run_recovery_scan()"
    - path: "tests/test_recovery_scan.py"
      provides: "Integration test proving spawn_task wires recovery scan"
  key_links:
    - from: "skills/spawn_specialist/pool.py::spawn_task()"
      to: "skills/spawn_specialist/pool.py::L3ContainerPool.run_recovery_scan()"
      via: "await pool.run_recovery_scan() called after pool creation, before spawn_and_monitor()"
      pattern: "await pool\\.run_recovery_scan\\(\\)"
---

<objective>
Wire the orphaned `run_recovery_scan()` method into the pool startup path so recovery actually runs automatically.

Purpose: `run_recovery_scan()` is fully implemented and tested (83 lines, 10 passing tests) but never called from any startup path — REL-06 success criterion 2 is not met. This gap closure adds the call in `spawn_task()` before `spawn_and_monitor()`.

Output: `spawn_task()` calls `await pool.run_recovery_scan()` before spawning, plus a test proving the wiring.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-graceful-sentinel/39-03-SUMMARY.md
@.planning/phases/39-graceful-sentinel/39-VERIFICATION.md
@skills/spawn_specialist/pool.py
@tests/test_recovery_scan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run_recovery_scan() into spawn_task() and add integration test</name>
  <files>skills/spawn_specialist/pool.py, tests/test_recovery_scan.py</files>
  <action>
In `skills/spawn_specialist/pool.py`, function `spawn_task()` (starts around line 1055):

1. After the pool is created at line 1098 (`pool = L3ContainerPool(max_concurrent=max_concurrent, project_id=project_id)`), set the pool config so `run_recovery_scan()` has access to `recovery_policy`:

```python
pool._pool_config = pool_cfg if 'pool_cfg' in dir() else {"recovery_policy": "mark_failed"}
```

Wait — `pool_cfg` is already computed at line 1093 in the try block. But it's only set when get_pool_config succeeds. Refactor slightly: after line 1098, assign the full config dict:

```python
try:
    pool_cfg = get_pool_config(project_id)
    max_concurrent = pool_cfg["max_concurrent"]
except Exception:
    pool_cfg = _POOL_DEFAULTS.copy()
    max_concurrent = _POOL_DEFAULTS["max_concurrent"]

pool = L3ContainerPool(max_concurrent=max_concurrent, project_id=project_id)
pool._pool_config = pool_cfg
```

The existing code already computes `pool_cfg` in the try block but doesn't save it on the except path. Fix the except block to also set `pool_cfg` (currently it only sets `max_concurrent`). Then assign `pool._pool_config = pool_cfg` after pool creation.

2. Add the recovery scan call before `spawn_and_monitor()`:

```python
await pool.run_recovery_scan()
```

This goes between the pool creation and the `return await pool.spawn_and_monitor(...)` call.

3. In `tests/test_recovery_scan.py`, add a new test `test_spawn_task_calls_recovery_scan` that:
   - Patches `L3ContainerPool.run_recovery_scan` as an AsyncMock
   - Patches `L3ContainerPool.spawn_and_monitor` as an AsyncMock returning a success dict
   - Patches `get_pool_config` to return valid config
   - Patches `get_workspace_path` to return "/tmp/test"
   - Patches `get_active_project_id` to return "test-project"
   - Calls `spawn_task("task-1", "code", "test description")`
   - Asserts `run_recovery_scan` was called exactly once
   - Asserts `run_recovery_scan` was called BEFORE `spawn_and_monitor` (use mock call order)

Keep the test pure — no Docker daemon dependency. Follow the existing test patterns in the file (sys.path.insert, _make_pool helper, AsyncMock usage).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_recovery_scan.py -v -x</automated>
    <manual>grep -n "run_recovery_scan" skills/spawn_specialist/pool.py to confirm the call exists in spawn_task()</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>
    - spawn_task() calls await pool.run_recovery_scan() before spawn_and_monitor()
    - pool._pool_config is set on the pool instance before run_recovery_scan() is called
    - New test proves run_recovery_scan is called exactly once and before spawn_and_monitor
    - All 10 existing recovery scan tests still pass
    - All existing pool tests still pass
  </done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_recovery_scan.py -v` — all tests pass (10 existing + 1 new)
2. `grep -n "await pool.run_recovery_scan" skills/spawn_specialist/pool.py` — shows call in spawn_task()
3. `python3 -m pytest tests/ -v -x` — full test suite passes
</verification>

<success_criteria>
- run_recovery_scan() is called automatically during pool startup via spawn_task()
- REL-06 success criterion 2 is satisfied: recovery scan runs without manual intervention
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/39-graceful-sentinel/39-04-SUMMARY.md`
</output>
