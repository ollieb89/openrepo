---
phase: 07-risk-drift-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/risk/types.ts
  - src/lib/risk/extraction.ts
  - src/lib/risk/entity-resolver.ts
  - src/lib/risk/comparison.ts
  - src/lib/risk/pipeline.ts
autonomous: true
requirements:
  - REAS-06
  - REAS-07
  - REAS-08
user_setup:
  - service: openai
    why: "Phase 7 research recommends GPT-4o-mini for high-quality NLI comparison logic."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard"

must_haves:
  truths:
    - "New Slack decisions trigger an audit against vector-linked tracker items."
    - "A Slack decision older than 30 days is ignored by the pipeline."
    - "Contradictions are identified with a reason, confidence score, and severity."
    - "Fuzzy matching maps Slack display names to GitHub/Linear assignees."
  artifacts:
    - path: "src/lib/risk/types.ts"
      provides: "Zod schemas for ProjectState and AuditResult"
    - path: "src/lib/risk/pipeline.ts"
      provides: "The Drift Guard orchestrator implementing the RAR pattern"
    - path: "src/lib/risk/extraction.ts"
      provides: "LLM-based structured extraction from raw text/JSON"
  key_links:
    - from: "src/lib/risk/pipeline.ts"
      to: "src/lib/sync/vector-store.ts"
      via: "similarity search for RAR linking"
    - from: "src/lib/risk/comparison.ts"
      to: "deep-object-diff"
      via: "structural mismatch detection"
---

<objective>
Implement the core "Drift Guard" audit pipeline for identifying contradictions between Slack decisions and tracker states (GitHub/Linear).

Purpose: Establish the automated infrastructure to detect when project "intent" deviates from "reality".
Output: A set of services (Extraction, Comparison, Resolver) orchestrated by a central pipeline that implements the Retrieval-Augmented Reconciliation (RAR) pattern.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-risk-drift-intelligence/07-RESEARCH.md
@.planning/phases/07-risk-drift-intelligence/07-CONTEXT.md
@src/lib/sync/vector-store.ts
@src/lib/sync/graph.ts
@src/lib/ollama.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Foundation & Entity Resolution</name>
  <files>package.json, src/lib/risk/types.ts, src/lib/risk/entity-resolver.ts</files>
  <action>
    1. Install dependencies: `npm install fuzzball deep-object-diff date-fns p-limit openai`.
    2. Create `src/lib/risk/types.ts`:
       - Define `ProjectStateSchema` (Zod) with fields: `assignee` (string), `due_date` (string/ISO), `priority` (low/medium/high), `status` (string), `scope` (summary string).
       - Define `AuditResultSchema` (Zod) with fields: `isContradiction` (boolean), `confidence` (0-1), `reason` (string), `severity` (low/medium/high), `driftedFields` (array of field names).
    3. Create `src/lib/risk/entity-resolver.ts`:
       - Implement `EntityResolver` class.
       - Use `fuzzball` for `resolveUser(slackName, trackerUsers)` and `resolveLabel(slackLabel, trackerLabels)`.
       - Target a similarity threshold of > 85% for auto-matching.
  </action>
  <verify>
    Run a vitest/bun test checking:
    1. `ProjectStateSchema.parse()` validates correctly.
    2. `resolveUser("Bob Smith", ["bsmith", "alice"])` returns "bsmith".
  </verify>
  <done>Dependencies installed and core types/entity resolution logic functional.</done>
</task>

<task type="auto">
  <name>Task 2: Extraction & Comparison Engine</name>
  <files>src/lib/risk/extraction.ts, src/lib/risk/comparison.ts</files>
  <action>
    1. Create `src/lib/risk/extraction.ts`:
       - Implement `ExtractionService`.
       - Use `ollama.generate` (or `openai` if configured) with a system prompt to extract structured `ProjectState` from Slack messages and Tracker items.
       - Use `zod-to-json-schema` or similar prompt strategy to ensure valid output.
    2. Create `src/lib/risk/comparison.ts`:
       - Implement `ComparisonEngine`.
       - Logic: 
         - Use `deep-object-diff` to find structural mismatches in `ProjectState`.
         - If mismatches found, or if fields are "subjective" (scope/priority), run an NLI prompt: "Compare DECISION vs TRACKER_STATE. Does the tracker state contradict the decision?".
         - Return structured `AuditResult`.
  </action>
  <verify>
    1. `ExtractionService` correctly extracts a date and assignee from a mock Slack string.
    2. `ComparisonEngine` flags a contradiction when `due_date` differs between two states.
  </verify>
  <done>Extraction and Comparison services are ready to process data pairs.</done>
</task>

<task type="auto">
  <name>Task 3: Audit Pipeline Orchestrator (RAR)</name>
  <files>src/lib/risk/pipeline.ts</files>
  <action>
    1. Create `src/lib/risk/pipeline.ts`:
       - Implement `AuditPipeline` orchestrator.
       - Implement `processTrigger(source: 'slack' | 'tracker', data: any)`:
         - **Filter**: If Slack, ignore if message is > 30 days old.
         - **Link (RAR)**: Use `VectorStore.search` to find the most relevant "Tracker" item for a Slack message (or vice versa).
         - **Execute**: Run `ExtractionService` on both, then `ComparisonEngine`.
         - **Normalize**: Use `EntityResolver` to handle identity mapping before comparison.
    2. Add `AuditPipeline.runProjectSweep(projectId)` for manual full audits.
  </action>
  <verify>
    1. Mock a Slack message trigger.
    2. Assert `VectorStore.search` is called to find candidates.
    3. Assert `ComparisonEngine` is called with the linked pair.
  </verify>
  <done>The full audit pipeline is implemented and can be triggered by external events.</done>
</task>

</tasks>

<verification>
### Integrated Verification
1. Define a mock scenario: Slack says "Ship by Friday" (Normalized to 2024-05-24), GitHub issue says "2024-06-01".
2. Run `AuditPipeline.processTrigger` with the Slack message.
3. Verify that the system:
   - Finds the GitHub issue via vector search.
   - Identifies the date contradiction.
   - Returns a `confidence > 0.8` result with the reason "Slack decision specifies May 24th, but GitHub issue is set to June 1st".
</verification>

<success_criteria>
- [ ] Audit pipeline successfully links disparate records via Vector Search (RAR).
- [ ] Extraction engine produces valid `ProjectState` objects from unstructured text.
- [ ] Comparison engine identifies date/assignee/status contradictions with reasoning.
- [ ] 30-day TTL constraint is enforced for Slack decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-risk-drift-intelligence/07-01-SUMMARY.md`
</output>
