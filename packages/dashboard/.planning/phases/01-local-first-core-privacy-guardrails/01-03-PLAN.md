---
phase: 01-local-first-core-privacy-guardrails
plan: 03
type: execute
wave: 2
depends_on: [01-01]
requirements: [PRIV-01, PRIV-03]
files_modified:
  - src/lib/privacy/minimization.ts
  - src/lib/openclaw.ts
  - src/lib/types.ts
  - src/lib/types/privacy.ts
  - tests/privacy/minimization.test.ts
autonomous: true
must_haves:
  truths:
    - "Persisted records retain only allowlisted metadata/provenance needed for retrieval and trust."
    - "Raw content/body fields are stripped or rejected in persistence paths by default."
    - "Provenance defaults include source links, timestamps, and connector labeling."
  artifacts:
    - path: "src/lib/privacy/minimization.ts"
      provides: "Allowlist-based persistence and field stripping/rejection logic."
    - path: "src/lib/openclaw.ts"
      provides: "Integration of minimization in data write paths."
    - path: "src/lib/types/privacy.ts"
      provides: "Privacy metadata/provenance type contracts."
    - path: "tests/privacy/minimization.test.ts"
      provides: "Regression coverage for metadata-only persistence behavior."
  key_links:
    - from: "src/lib/openclaw.ts"
      to: "src/lib/privacy/minimization.ts"
      via: "write path invokes allowlist/minimization helper"
      pattern: "payload sanitization before persistence"
    - from: "src/lib/privacy/minimization.ts"
      to: "src/lib/types/privacy.ts"
      via: "enforced metadata and provenance schema"
      pattern: "typed allowlist contract"
---

# Phase 01-03 Plan: Metadata Minimization and Provenance Defaults

## Objective
Enforce allowlist-based persistence so only minimum metadata/provenance is retained for retrieval and trust signals.

## Context
- `.planning/phases/01-local-first-core-privacy-guardrails/01-CONTEXT.md`
- `.planning/phases/01-local-first-core-privacy-guardrails/01-RESEARCH.md`
- `.planning/phases/01-local-first-core-privacy-guardrails/01-01-PLAN.md`

<tasks>
  <task id="P01-03-T01" wave="2" type="auto">
    <name>Enforce metadata-minimization contracts for storage and provenance fields</name>
    <title>Enforce metadata-minimization contracts for storage and provenance fields</title>
    <requirements>PRIV-01,PRIV-03</requirements>
    <depends_on>P01-01-T01</depends_on>
    <files>
      <file>src/lib/privacy/minimization.ts</file>
      <file>src/lib/openclaw.ts</file>
      <file>src/lib/types.ts</file>
      <file>src/lib/types/privacy.ts</file>
      <file>tests/privacy/minimization.test.ts</file>
    </files>
    <action>Create explicit allowlist-based persistence helpers that keep only minimum retrieval/linking metadata (source id, thread id, timestamp, connector, entity type, provenance pointers). Strip or reject raw content/body fields by default in write paths. Update shared types so persisted records always carry provenance fields (source links, timestamps, connector label) needed for user trust signals. Do not add raw-content debug retention in this phase.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/privacy/minimization.test.ts</automated>
      <manual>Run ingestion/persistence path with sample payload including raw body text and confirm stored artifact contains only allowlisted metadata + provenance pointers.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>All persistence paths enforce metadata-only storage by contract, and provenance defaults are present for retrieval/linking without unnecessary raw retention.</done>
  </task>
</tasks>

## Verification Criteria
- Storage writes drop or reject raw content/body fields.
- Persisted records keep only allowlisted metadata with provenance defaults.

## Must Haves
<must_haves>
  <must_have id="MH-06" requirement="PRIV-03">Persisted data is restricted to minimum metadata and provenance pointers required for retrieval/linking; raw content is not retained by default.</must_have>
</must_haves>
