---
phase: 01-environment-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [openclaw.json]
autonomous: true
requirements: [SET-01, SET-02]
must_haves:
  truths:
    - "Ubuntu host has Docker 27+ and Nvidia Container Toolkit installed"
    - "openclaw.json contains required gateway and lane queue settings"
  artifacts:
    - path: "openclaw.json"
      provides: "Swarm configuration"
      contains: ""gateway""
  key_links:
    - from: "openclaw.json"
      to: "Gateway Port"
      via: "port: 18789"
---

<objective>
Establish the physical prerequisites and core configuration for the OpenClaw swarm.

Purpose: Ensure the host environment is capable of running isolated, GPU-accelerated agent containers and that the root configuration aligns with the Grand Architect Protocol.
Output: Verified host environment and aligned openclaw.json.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_1_RESEARCH.md
@openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Host Prerequisites (SET-01)</name>
  <files></files>
  <action>
    Verify the installation of critical host dependencies:
    1. Check Docker version (expect 27+).
    2. Check Bun version.
    3. Check Nvidia Container Toolkit presence via `nvidia-smi` and a test container run.
    4. Verify Nvidia drivers are correctly loaded.
  </action>
  <verify>
    docker --version && bun --version && nvidia-smi && docker run --rm --runtime=nvidia --gpus all nvidia/cuda:12.0-base nvidia-smi
  </verify>
  <done>All prerequisite software is installed and functional on the Ubuntu 24.04 host.</done>
</task>

<task type="auto">
  <name>Task 2: Align openclaw.json for GAP (SET-02)</name>
  <files>openclaw.json</files>
  <action>
    Update `openclaw.json` to ensure alignment with the Grand Architect Protocol:
    1. Ensure `gateway` section uses port 18789, mode "local", and bind "loopback".
    2. Ensure `agents.defaults` has `maxConcurrent: 4` and `subagents.maxConcurrent: 8`.
    3. Verify `pumplai_pm` agent is correctly defined with the expected workspace.
  </action>
  <verify>
    cat openclaw.json | jq '.gateway.port, .agents.defaults.maxConcurrent'
  </verify>
  <done>`openclaw.json` is configured with the standard swarm parameters.</done>
</task>

</tasks>

<verification>
Run `openclaw check` (if available) or verify the configuration via `jq`.
Confirm Docker and Nvidia capabilities are active.
</verification>

<success_criteria>
- Host environment passes all health checks.
- `openclaw.json` is valid JSON and contains the required GAP settings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-substrate/01-01-SUMMARY.md`
</output>
