---
phase: 04-auto-link-suggest-review-loop
plan: 02
type: execute
wave: 1
depends_on: ["04-01-PLAN.md"]
files_modified: [src/lib/sync/indexer.ts, src/lib/sync/engine.ts, src/lib/sync/summarizer.ts]
autonomous: true
requirements: [LINK-01]
---

<objective>
Implement the Issue Indexer to vectorize GitHub/Linear issue metadata and Decision Summaries. This provides the semantic foundation for the Relevance Engine.

Purpose: Enable semantic search between conversations and work items.
Output: Background tasks that populate the vector_cache table with embeddings.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/sync/vector-store.ts
@src/lib/ollama.ts
@src/lib/sync/engine.ts
@src/lib/sync/summarizer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Central Indexer Service</name>
  <files>src/lib/sync/indexer.ts</files>
  <action>
    Create a new indexer service that provides:
    - `indexEntity(id: string, type: 'decision' | 'issue', content: string, metadata: any)`: Generates an embedding using `generateEmbedding` (from ollama.ts) and stores it in the `vector_cache` table (via vector-store.ts).
    - Handle upserts: Update existing embeddings if the content has changed (use a hash or timestamp if needed, or just overwrite).
    - Ensure it uses the 'mxbai-embed-large' model as specified in 04-01.
  </action>
  <verify>
    Create a small test script or unit test to verify that calling `indexEntity` populates the `vector_cache` table with a 1024-dimension vector.
  </verify>
  <done>
    `indexEntity` successfully saves embeddings to the database for both 'decision' and 'issue' types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Indexer into Sync and Summarization Pipelines</name>
  <files>src/lib/sync/engine.ts, src/lib/sync/summarizer.ts</files>
  <action>
    Wired the indexer into the background processing loops:
    1. In `src/lib/sync/engine.ts`: After `adapter.upsertRecords` for trackers (GitHub/Linear), call `indexEntity` for each new/updated issue.
    2. In `src/lib/sync/summarizer.ts`: After a decision summary is successfully created and persisted, call `indexEntity` with the summary content.
    
    Ensure these background tasks are non-blocking and catch errors to prevent sync failures.
  </action>
  <verify>
    Run a mock sync or trigger summarization and check the `vector_cache` table to confirm entries are appearing for both issues and decisions.
  </verify>
  <done>
    New issues and new decision summaries are automatically vectorized upon creation/ingestion.
  </done>
</task>

</tasks>

<verification>
Check `vector_cache` table in `nexus-sync.db`:
`sqlite3 workspace/.openclaw/nexus-sync.db "SELECT entity_type, COUNT(*) FROM vector_cache GROUP BY entity_type;"`
Verify both 'issue' and 'decision' counts increase after sync/summarization.
</verification>

<success_criteria>
- Issues from trackers are vectorized and stored in `vector_cache`.
- Decision summaries from Slack threads are vectorized and stored in `vector_cache`.
- Indexing happens automatically in the background without interrupting the main sync flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auto-link-suggest-review-loop/04-02-SUMMARY.md`
</output>
