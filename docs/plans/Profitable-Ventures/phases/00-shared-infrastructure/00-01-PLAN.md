---
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docker-compose.yml
  - .github/workflows/ci.yml
  - config/settings.py
  - .env.example
  - .pre-commit-config.yaml
  - Makefile
autonomous: true
---

# Plan: Phase 0, Wave 0.1 - Project Bootstrap

<context>
**Phase:** 0 - Shared Infrastructure
**Wave:** 0.1
**Goal:** Stand up the monorepo, CI, and local dev environment. Everything else depends on this.
**Context:** Managed DB + VMs for staging/prod, but local uses Docker Compose. We are setting up a `uv` workspace with FastAPI + SQLAlchemy + Alembic in `packages/platform/`.
</context>

<requirements>
- `pyproject.toml` with `uv` workspace
- PostgreSQL Docker Compose for local dev (multi-tenant from day one)
- GitHub Actions CI scaffold (ruff, pytest, pyright, build)
- Environment config structure (`config/settings.py` using pydantic-settings)
- Pre-commit hooks
- Gate G0.1: `make dev && make test` passes with empty test suite. Docker Compose `up` gives healthy PG.
</requirements>

<tasks>

<task type="code" gate="blocking">
  <name>Task 0.1.1: Initialize Python monorepo with `uv` workspace</name>
  <action>
    - Create `pyproject.toml` configuring a `uv` workspace.
    - Create directory structure: `packages/platform/`.
    - Setup base FastAPI, SQLAlchemy, and Alembic dependencies.
    - Create a `Makefile` with `dev` and `test` targets.
  </action>
  <verify>
    <automated>test -f pyproject.toml && test -d packages/platform</automated>
    <manual>Review pyproject.toml for correct uv workspace setup.</manual>
  </verify>
  <done>pyproject.toml and packages/platform directory exist.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.1.2: PostgreSQL Docker Compose for local dev</name>
  <action>
    - Create `docker-compose.yml` with `pg` (PostgreSQL 16) and `pgadmin` services.
    - Ensure data persistence via volumes in `.docker/pgdata`.
  </action>
  <verify>
    <automated>test -f docker-compose.yml</automated>
    <manual>Review docker-compose.yml for pg and pgadmin services.</manual>
  </verify>
  <done>docker-compose.yml exists and defines correct services.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.1.3: GitHub Actions CI scaffold</name>
  <action>
    - Create `.github/workflows/ci.yml`.
    - Configure jobs for: lint (ruff), test (pytest), type-check (pyright).
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml</automated>
    <manual>Review CI yaml for correctness.</manual>
  </verify>
  <done>CI scaffold exists.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.1.4: Environment config structure</name>
  <action>
    - Create `packages/platform/config/settings.py` using `pydantic-settings`.
    - Define core settings (DB URL, Environment).
    - Create `.env.example` with all required vars.
  </action>
  <verify>
    <automated>test -f packages/platform/config/settings.py && test -f .env.example</automated>
    <manual>Review settings.py for pydantic integration.</manual>
  </verify>
  <done>Config structure is in place.</done>
</task>

<task type="code" gate="blocking">
  <name>Task 0.1.5: Pre-commit hooks</name>
  <action>
    - Create `.pre-commit-config.yaml`.
    - Configure ruff, pyright, and secret-scanner hooks.
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml</automated>
    <manual>Review pre-commit yaml for tools.</manual>
  </verify>
  <done>Pre-commit config exists.</done>
</task>

<task type="verify" gate="blocking">
  <name>Task 0.1.6: Gate G0.1 Verification</name>
  <action>
    - Execute `make test` to ensure tests run (even if empty).
    - Bring up Docker Compose to ensure DB starts successfully.
  </action>
  <verify>
    <automated>make test</automated>
    <manual>Verify `docker-compose up -d` works locally.</manual>
  </verify>
  <done>Gate G0.1 criteria are met.</done>
</task>

</tasks>

<must_haves>
- Monorepo structure properly instantiated
- Local PostgreSQL instance is accessible
- Make commands for dev and test work out of the box
</must_haves>
