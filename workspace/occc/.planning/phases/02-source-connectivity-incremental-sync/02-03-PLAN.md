---
phase: 02-source-connectivity-incremental-sync
plan: 03
type: execute
wave: 2
depends_on: [02-01]
requirements: [INTG-02, INTG-03]
files_modified:
  - src/lib/connectors/tracker.ts
  - src/lib/connectors/tracker-github.ts
  - src/lib/connectors/tracker-linear.ts
  - src/app/api/connectors/tracker/route.ts
  - src/app/api/connectors/tracker/sync/route.ts
  - src/components/connectors/TrackerConnectorCard.tsx
  - tests/connectors/tracker-adapter.test.ts
autonomous: true
must_haves:
  truths:
    - "User can connect one tracker provider (GitHub Issues or Linear) and sync issue metadata."
    - "Tracker incremental sync captures changed records (status updates, reopen/close, comments/updates) after first import."
    - "Tracker connector health transitions follow shared auth/rate-limit/error semantics."
  artifacts:
    - path: "src/lib/connectors/tracker.ts"
      provides: "Provider-agnostic tracker adapter interface and config validation."
    - path: "src/lib/connectors/tracker-github.ts"
      provides: "GitHub Issues incremental adapter using update-time cursor semantics."
    - path: "src/lib/connectors/tracker-linear.ts"
      provides: "Linear incremental adapter using GraphQL pagination filtered by updatedAt."
    - path: "src/components/connectors/TrackerConnectorCard.tsx"
      provides: "UI flow for provider choice, connect state, and sync status/actions."
  key_links:
    - from: "src/app/api/connectors/tracker/sync/route.ts"
      to: "src/lib/sync/engine.ts"
      via: "tracker sync routes execute through shared incremental runner"
      pattern: "engine delegation"
    - from: "src/components/connectors/TrackerConnectorCard.tsx"
      to: "src/app/api/connectors/tracker/route.ts"
      via: "provider connect config drives tracker adapter selection"
      pattern: "provider-specific configuration"
---

# Phase 02-03 Plan: Tracker Connectivity (GitHub/Linear) and Incremental Metadata Sync

## Objective
Deliver one-tracker connectivity (GitHub Issues or Linear) with metadata ingestion and changed-record incremental behavior aligned with resumable sync guarantees.

## Context
- `.planning/phases/02-source-connectivity-incremental-sync/02-CONTEXT.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-RESEARCH.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-01-PLAN.md`

<tasks>
  <task id="P02-03-T01" wave="2" type="auto">
    <name>Implement tracker adapter layer and provider-specific incremental sync for GitHub and Linear</name>
    <title>Implement tracker adapter layer and provider-specific incremental sync for GitHub and Linear</title>
    <requirements>INTG-02,INTG-03</requirements>
    <depends_on>P02-01-T01,P02-01-T02</depends_on>
    <files>
      <file>src/lib/connectors/tracker.ts</file>
      <file>src/lib/connectors/tracker-github.ts</file>
      <file>src/lib/connectors/tracker-linear.ts</file>
      <file>src/app/api/connectors/tracker/sync/route.ts</file>
      <file>tests/connectors/tracker-adapter.test.ts</file>
    </files>
    <action>Create tracker adapter abstraction with two concrete implementations: GitHub Issues and Linear. For GitHub, use update-time incremental filters (not creation-time only) so edits and reopen/close transitions are captured. For Linear, use paginated GraphQL queries filtered by `updatedAt` checkpoint. Both adapters must emit normalized change records and consume the shared checkpoint + health classifier utilities.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/tracker-adapter.test.ts tests/connectors/sync-engine.test.ts</automated>
      <manual>Connect tracker provider, run initial sync, modify an existing issue, rerun sync, and confirm modified issue is re-ingested as changed (not skipped).</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Tracker connector supports one configured provider and captures both new and changed issue metadata incrementally.</done>
  </task>

  <task id="P02-03-T02" wave="2" type="auto">
    <name>Add tracker connector API and UI for provider selection, connect state, and sync control</name>
    <title>Add tracker connector API and UI for provider selection, connect state, and sync control</title>
    <requirements>INTG-02</requirements>
    <depends_on>P02-03-T01</depends_on>
    <files>
      <file>src/app/api/connectors/tracker/route.ts</file>
      <file>src/components/connectors/TrackerConnectorCard.tsx</file>
      <file>src/app/settings/connectors/page.tsx</file>
    </files>
    <action>Implement tracker connector configuration APIs and card UI to choose provider (GitHub Issues or Linear), save credentials/config, and trigger manual sync. UI must render normalized health states and explicit re-auth action for `auth_expired`, without collapsing states into generic error labels.</action>
    <verify>
      <automated>npm run lint</automated>
      <manual>On connectors page, configure GitHub or Linear, trigger sync-now, then simulate token expiry and confirm auth_expired state shows reconnect CTA and preserves last successful sync timestamp.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>User can connect one tracker source, run syncs, and act on provider-specific auth/health outcomes through unified state semantics.</done>
  </task>
</tasks>

## Verification Criteria
- Tracker provider can be configured as GitHub or Linear with one active connection.
- Incremental runs reprocess changed issues/metadata updates, not just newly created records.
- Health/error handling produces actionable auth-expired and rate-limit states.

## Must Haves
<must_haves>
  <must_have id="MH-06" requirement="INTG-02">User can connect one project tracker (GitHub Issues or Linear) and ingest issue metadata.</must_have>
  <must_have id="MH-07" requirement="INTG-03">Tracker incremental sync captures net-new and changed records after first import.</must_have>
</must_haves>
