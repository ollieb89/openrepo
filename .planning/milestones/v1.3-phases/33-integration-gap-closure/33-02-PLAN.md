---
phase: 33-integration-gap-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MEM-04
  - RET-02

must_haves:
  truths:
    - "MEM-01 checkbox is [x] in REQUIREMENTS.md — confirmed by existing test evidence"
    - "MEM-03 checkbox is [x] in REQUIREMENTS.md — confirmed by existing test evidence"
    - "MEM-04 status in traceability table reflects completion after Plan 01 fixes"
    - "RET-02 status in traceability table reflects completion after Plan 01 fixes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement satisfaction checkboxes"
      contains: "[x] **MEM-01**"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "tests/test_pool_memorization.py"
      via: "MEM-01 and MEM-03 evidence from passing tests"
      pattern: "MEM-01.*MEM-03"
---

<objective>
Update REQUIREMENTS.md to accurately reflect the satisfied status of MEM-01, MEM-03, MEM-04, and RET-02 after the code fixes in Plan 01 and existing test evidence from prior phases.

Purpose: The v1.3 milestone audit found that MEM-01 and MEM-03 checkboxes were never ticked despite being fully implemented and tested. Plan 01 completes MEM-04 and RET-02. This plan updates all four checkboxes and the traceability table to match reality.

Output: Updated REQUIREMENTS.md with accurate checkboxes and traceability status.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-integration-gap-closure/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MEM-01/MEM-03 evidence and update REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
**Step 1: Run existing tests to confirm MEM-01 and MEM-03 evidence.**
```bash
python3 -m pytest tests/test_pool_memorization.py -v
```
All 5 tests should pass, confirming:
- MEM-01: `_memorize_snapshot_fire_and_forget()` is wired in pool.py (auto-memorize after L3 exit)
- MEM-03: Fire-and-forget pattern is non-blocking (exception handling test passes)

**Step 2: Run spawn memory tests to confirm MEM-04 and RET-02 evidence.**
```bash
python3 -m pytest tests/test_spawn_memory.py -v
```
URL rewrite tests confirm MEM-04 (MEMU_API_URL correctly injected for container use). SOUL tests confirm RET-02 (SOUL injection flow complete).

**Step 3: Update REQUIREMENTS.md checkboxes** — four changes:

1. MEM-01 line: Change `- [x] **MEM-01**:` — this is already `[x]` per current file. Verify it stays `[x]`.

2. MEM-03 line: Change `- [x] **MEM-03**:` — this is already `[x]` per current file. Verify it stays `[x]`.

3. MEM-04 line: Change from `- [ ] **MEM-04**:` to `- [x] **MEM-04**:` — Plan 01 added URL rewrite for Docker DNS + network join.

4. RET-02 line: Change from `- [ ] **RET-02**:` to `- [x] **RET-02**:` — Plan 01 completed entrypoint.sh SOUL_FILE handling + persistent file path.

**Step 4: Update traceability table** — two changes:

| MEM-04 | Phase 33 | Pending | → | MEM-04 | Phase 33 | Complete |
| RET-02 | Phase 33 | Pending | → | RET-02 | Phase 33 | Complete |

**Step 5: Update the "Last updated" line** at the bottom to include today's date and note the phase 33 gap closure.

Note: Per locked decision from CONTEXT.md, stay focused on MEM-01, MEM-03, MEM-04, and RET-02 only — do NOT modify any other checkboxes or traceability entries.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_pool_memorization.py tests/test_spawn_memory.py -v</automated>
    <manual>Verify REQUIREMENTS.md has [x] for MEM-01, MEM-03, MEM-04, RET-02 and traceability table shows Complete for MEM-04 and RET-02</manual>
  </verify>
  <done>REQUIREMENTS.md has accurate checkboxes: MEM-01 [x], MEM-03 [x], MEM-04 [x], RET-02 [x]. Traceability table shows MEM-04 and RET-02 as Complete. Evidence confirmed by passing test suites.</done>
</task>

</tasks>

<verification>
1. `grep "MEM-01" .planning/REQUIREMENTS.md` — shows `[x]`
2. `grep "MEM-03" .planning/REQUIREMENTS.md` — shows `[x]`
3. `grep "MEM-04" .planning/REQUIREMENTS.md` — shows `[x]` in checkbox and `Complete` in traceability
4. `grep "RET-02" .planning/REQUIREMENTS.md` — shows `[x]` in checkbox and `Complete` in traceability
5. `python3 -m pytest tests/test_pool_memorization.py tests/test_spawn_memory.py -v` — all tests pass
</verification>

<success_criteria>
- MEM-01, MEM-03, MEM-04, RET-02 all show [x] in REQUIREMENTS.md
- MEM-04 and RET-02 show "Complete" in traceability table
- All evidence tests pass (pool_memorization + spawn_memory)
- No other checkboxes or traceability entries modified
</success_criteria>

<output>
After completion, create `.planning/phases/33-integration-gap-closure/33-02-SUMMARY.md`
</output>
