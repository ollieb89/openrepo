---
phase: 06-phase3-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_phase3.py
autonomous: true
requirements:
  - HIE-03
  - HIE-04
  - COM-03
  - COM-04

must_haves:
  truths:
    - "verify_phase3.py runs end-to-end and exits 0 when all checks pass"
    - "L3 container spawns successfully with echo runtime and is inspectable"
    - "Docker inspect confirms no-new-privileges, cap_drop ALL, and 4GB memory limit"
    - "Jarvis Protocol state engine creates, updates, and reads tasks with correct locking"
    - "Snapshot directory is writable and capture function is importable"
    - "Any failures discovered during live testing are fixed before script exits 0"
  artifacts:
    - path: "scripts/verify_phase3.py"
      provides: "Consolidated Phase 3 verification harness covering HIE-03, HIE-04, COM-03, COM-04"
      min_lines: 150
  key_links:
    - from: "scripts/verify_phase3.py"
      to: "skills/spawn_specialist/spawn.py"
      via: "spawn_l3_specialist() call with cli_runtime='echo'"
      pattern: "spawn_l3_specialist"
    - from: "scripts/verify_phase3.py"
      to: "orchestration/state_engine.py"
      via: "JarvisState create_task/update_task/read_task"
      pattern: "JarvisState"
    - from: "scripts/verify_phase3.py"
      to: "orchestration/snapshot.py"
      via: "capture_semantic_snapshot import and snapshot dir check"
      pattern: "capture_semantic_snapshot|SNAPSHOT_DIR"
---

<objective>
Build and run a consolidated Phase 3 verification script that exercises all four requirements (HIE-03, HIE-04, COM-03, COM-04) through live end-to-end testing against real Docker infrastructure.

Purpose: Produce repeatable, scriptable evidence that Phase 3 deliverables work as designed. Fix any failures discovered during live testing within this plan.
Output: `scripts/verify_phase3.py` that exits 0 with PASS/FAIL output for each requirement.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-phase3-verification/06-RESEARCH.md
@scripts/verify_phase5_integration.py
@skills/spawn_specialist/spawn.py
@orchestration/state_engine.py
@orchestration/snapshot.py
@orchestration/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify_phase3.py verification script</name>
  <files>scripts/verify_phase3.py</files>
  <action>
Create `scripts/verify_phase3.py` following the established pattern from `scripts/verify_phase5_integration.py` (Colors class, print_section, print_check helpers, find_project_root, structured sections, exit code 0/1).

The script must have four sequential verification sections:

**Section 1: HIE-03 — L3 Container Spawning**
- Import `spawn_l3_specialist` from `skills.spawn_specialist.spawn`
- Spawn a container with `task_id='phase6-hie03-test'`, `skill_hint='code'`, `task_description='Phase 6 HIE-03 verification'`, `workspace_path=str(project_root / 'workspace')`, `requires_gpu=False`, `cli_runtime='echo'`
- Assert the container object is returned (not None)
- Print PASS with container name/id
- Clean up container in finally block: `container.remove(force=True)`

**Section 2: HIE-04 — Physical Isolation Flags**
- Spawn a second container with `task_id='phase6-hie04-test'`, same params as above
- Use `docker.from_env().containers.get(container.id).attrs['HostConfig']` to inspect
- Assert `'no-new-privileges' in attrs['SecurityOpt']`
- Assert `'ALL' in attrs['CapDrop']`
- Assert `attrs['Memory'] == 4 * 1024 * 1024 * 1024` (4GB = 4294967296)
- Also check `attrs['NanoCpus'] == 1_000_000_000` (1 CPU) if present, or `attrs['CpuCount'] == 1`
- Print PASS for each flag verified, with the actual value
- Clean up container in finally block

**Section 3: COM-03 — Jarvis Protocol State Synchronization**
- Import `JarvisState` from `orchestration.state_engine` and `STATE_FILE` from `orchestration.config`
- Create task: `js.create_task('phase6-com03-test', 'code', {'phase': '6', 'req': 'COM-03'})`
- Update task: `js.update_task('phase6-com03-test', 'in_progress', 'Verification run started')`
- Read task: `task = js.read_task('phase6-com03-test')`
- Assert `task['status'] == 'in_progress'`
- Assert activity_log has at least 1 entry containing 'Verification run started'
- Also run `python3 orchestration/monitor.py status` as subprocess and assert returncode == 0
- Print PASS for state create/update/read and monitor status

**Section 4: COM-04 — Semantic Snapshots**
- Import `SNAPSHOT_DIR` from `orchestration.config`
- Assert `Path(SNAPSHOT_DIR).exists()` and `Path(SNAPSHOT_DIR).is_dir()`
- Write a test file `phase6-com04-test.diff` to the snapshots dir with metadata header `# Semantic Snapshot: phase6-com04-test`
- Assert it exists and is readable
- Clean up the test file
- Try importing `capture_semantic_snapshot` from `orchestration.snapshot` — assert import succeeds
- Print PASS for directory exists, writable, and function importable

**Final Summary Section:**
- Print a summary table with requirement ID, description, and PASS/FAIL status
- Print "PHASE 3 VERIFICATION COMPLETE" or "PHASE 3 VERIFICATION INCOMPLETE"
- Exit 0 if all pass, exit 1 if any fail

Use `phase6-*` prefixed task IDs to avoid collision with existing state data (test-001, verify-001, dry-run-001 already exist).

Do NOT use real claude-code/codex runtimes — use `cli_runtime='echo'` for all container spawns.

Always clean up test containers in finally blocks.

Add `import docker` with a try/except that prints a helpful error if docker SDK is not installed.
  </action>
  <verify>
    <automated>python3 /home/ollie/.openclaw/scripts/verify_phase3.py</automated>
    <manual>Review terminal output shows [PASS] for all four requirement sections and exits 0</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>verify_phase3.py exists, runs without errors, exercises all four requirements (HIE-03, HIE-04, COM-03, COM-04) with live Docker operations, and exits 0 with all checks passing</done>
</task>

<task type="auto">
  <name>Task 2: Fix any failures and confirm clean pass</name>
  <files>scripts/verify_phase3.py</files>
  <action>
Run `python3 scripts/verify_phase3.py` and examine the output.

If ALL checks pass (exit 0): This task is a no-op. Move to done.

If ANY check fails (exit 1): Diagnose and fix the failure. The fix may be in:
- `scripts/verify_phase3.py` itself (test logic error)
- `skills/spawn_specialist/spawn.py` (spawn function issue)
- `orchestration/state_engine.py` (state engine issue)
- `orchestration/snapshot.py` (snapshot issue)
- `orchestration/config.py` (path configuration issue)
- Docker image or daemon (rebuild image, restart daemon)

Per CONTEXT.md locked decision: "Fix failures within this phase — verification includes remediation, re-verify, then document the pass." Claude judges whether a fix belongs here or needs a new phase.

After fixing, re-run `python3 scripts/verify_phase3.py` until it exits 0.

If a failure is beyond scope (e.g., Docker daemon down, fundamental architectural issue), document it clearly and create a note for the user rather than silently marking it as passed.
  </action>
  <verify>
    <automated>python3 /home/ollie/.openclaw/scripts/verify_phase3.py && echo "EXIT_CODE_0"</automated>
    <manual>Terminal shows all [PASS] lines and "PHASE 3 VERIFICATION COMPLETE"</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>verify_phase3.py runs cleanly with exit code 0, all four requirements show PASS, no failures remain</done>
</task>

</tasks>

<verification>
- `python3 scripts/verify_phase3.py` exits 0
- Output shows [PASS] for HIE-03 (container spawning), HIE-04 (isolation flags), COM-03 (state engine), COM-04 (snapshots)
- No test containers left running after script completes
- Script follows established pattern from verify_phase5_integration.py
</verification>

<success_criteria>
- scripts/verify_phase3.py exists and is executable
- All four Phase 3 requirements verified through live end-to-end testing
- Script exits 0 with clean PASS output for all checks
- Any failures discovered during testing have been remediated
</success_criteria>

<output>
After completion, create `.planning/phases/06-phase3-verification/06-01-SUMMARY.md`
</output>
