---
phase: 04-monitoring-uplink
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - workspace/occc/src/app/page.tsx
  - workspace/occc/src/app/layout.tsx
  - workspace/occc/src/app/globals.css
  - workspace/occc/src/components/GlobalMetrics.tsx
  - workspace/occc/src/components/AgentHierarchy.tsx
  - workspace/occc/src/components/AgentDetail.tsx
  - workspace/occc/src/components/LogStream.tsx
  - workspace/occc/src/components/StatusToast.tsx
autonomous: true
requirements: [DSH-01, DSH-04]

must_haves:
  truths:
    - "Dashboard renders as mission control 3-panel layout: hierarchy left, detail center, logs right"
    - "Panels stack vertically on screens below 1024px (responsive)"
    - "Clicking an agent in the hierarchy loads its details in the center panel and its logs in the right panel"
    - "Global metrics bar shows agent counts by tier and status counts (active/idle/errored)"
    - "Toast notifications appear when agent state changes (errors, spawn/despawn)"
    - "Log panel supports severity filtering (debug/info/warn/error) and text search"
  artifacts:
    - path: "workspace/occc/src/app/page.tsx"
      provides: "Mission control dashboard layout wiring all components"
      contains: "AgentHierarchy"
    - path: "workspace/occc/src/components/GlobalMetrics.tsx"
      provides: "Top metrics bar with tier counts and status indicators"
      contains: "GlobalMetrics"
    - path: "workspace/occc/src/components/AgentHierarchy.tsx"
      provides: "Left panel: L1/L2/L3 agent tree with status dots and task summaries"
      contains: "AgentHierarchy"
    - path: "workspace/occc/src/components/AgentDetail.tsx"
      provides: "Center panel: tabbed agent detail view (Overview | Tasks | State)"
      contains: "AgentDetail"
    - path: "workspace/occc/src/components/LogStream.tsx"
      provides: "Right panel: filtered log stream with auto-scroll and severity/text filtering"
      contains: "LogStream"
    - path: "workspace/occc/src/components/StatusToast.tsx"
      provides: "Toast notification system for agent state change alerts"
      contains: "useAgentStateMonitor"
  key_links:
    - from: "workspace/occc/src/app/page.tsx"
      to: "workspace/occc/src/hooks/useSwarmState.ts"
      via: "useSwarmState() hook call providing agents and metrics"
      pattern: "useSwarmState"
    - from: "workspace/occc/src/components/LogStream.tsx"
      to: "workspace/occc/src/hooks/useLogStream.ts"
      via: "useLogStream(agentId) hook call for selected agent logs"
      pattern: "useLogStream"
    - from: "workspace/occc/src/components/AgentHierarchy.tsx"
      to: "workspace/occc/src/app/page.tsx"
      via: "onSelectAgent callback prop setting selectedAgent state"
      pattern: "onSelectAgent"
---

<objective>
Build the mission control dashboard UI with all five components: GlobalMetrics bar, AgentHierarchy tree, AgentDetail tabbed panel, LogStream with filtering, and StatusToast notifications. Rewrites the existing mock-based page.tsx into the production 3-panel layout.

Purpose: This is the primary user-facing deliverable -- the actual monitoring interface human operators will use to oversee the swarm.
Output: Complete responsive dashboard UI consuming live data from Plan 01 hooks and Plan 02 log streaming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monitoring-uplink/04-01-SUMMARY.md
@.planning/phases/04-monitoring-uplink/04-02-SUMMARY.md
@.planning/phases/04-monitoring-uplink/04-RESEARCH.md
@workspace/occc/src/app/page.tsx
@workspace/occc/src/app/layout.tsx
@workspace/occc/src/app/globals.css
@workspace/occc/src/hooks/useSwarmState.ts
@workspace/occc/src/hooks/useLogStream.ts
@workspace/occc/src/lib/jarvis.ts
@workspace/occc/src/lib/metrics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mission control layout, GlobalMetrics bar, and AgentHierarchy panel</name>
  <files>
    workspace/occc/src/app/page.tsx
    workspace/occc/src/app/layout.tsx
    workspace/occc/src/app/globals.css
    workspace/occc/src/components/GlobalMetrics.tsx
    workspace/occc/src/components/AgentHierarchy.tsx
  </files>
  <action>
**Rewrite `src/app/page.tsx`** completely (replace existing mock-based content):
- `"use client"` directive
- Import and call `useSwarmState()` hook for live data
- Manage `selectedAgent: string | null` state (null initially)
- Layout structure (per locked decision -- mission control with fixed zones):
  ```
  <div className="flex flex-col h-screen bg-black">
    <GlobalMetrics metrics={metrics} />
    <div className="flex-1 grid grid-cols-1 lg:grid-cols-12 gap-0 overflow-hidden">
      <aside className="lg:col-span-3 ...">  <!-- Hierarchy -->
      <main className="lg:col-span-5 ...">   <!-- Detail -->
      <aside className="lg:col-span-4 ...">  <!-- Logs -->
    </div>
  </div>
  ```
- Pass `onSelectAgent={setSelectedAgent}` to AgentHierarchy
- Pass `agentId={selectedAgent}` to AgentDetail and LogStream
- Show empty state in center and right panels when no agent selected: "Select an agent from the hierarchy" with a subtle icon
- Loading state: Show shimmer skeleton layout (6-8 placeholder cards in hierarchy, skeleton boxes in detail)
- Error state: Full-screen error with retry button
- Import and render `<StatusToast />` component and `<ToastContainer />` from react-toastify

**Update `src/app/layout.tsx`**:
- Import react-toastify CSS: `import 'react-toastify/dist/ReactToastify.css'`
- Add dark theme class to html tag if needed for consistent styling
- Keep existing metadata but update description

**Update `src/app/globals.css`**:
- Add custom scrollbar styles for dark theme (thin scrollbar, slate-800 track, slate-600 thumb)
- Add shimmer/skeleton animation keyframes for loading states
- Preserve existing Tailwind v4 @import and @theme directives
- Add utility class `.panel-border` for consistent border styling between panels

**Create `src/components/GlobalMetrics.tsx`** (top bar, per locked decision):
- Props: `{ metrics: SwarmMetrics }` from lib/metrics.ts
- Fixed height bar at top of dashboard (h-16 to h-20)
- Left section: "OPENCLAW COMMAND" branding with Activity icon and connected status indicator (pulsing green dot)
- Center section: Tier counts displayed as badges: "L1: N", "L2: N", "L3: N" with distinct colors per tier
- Right section: Status counts: "Active: N" (green), "Idle: N" (gray), "Errored: N" (red) -- use colored dots before each count
- Bottom: thin progress-style bar or separator
- Additional metrics: Total tasks, completed tasks, failed tasks as secondary row or tooltip
- Responsive: On mobile, stack into 2 rows (branding + status counts)
- Use Lucide icons: Activity, Server, AlertCircle, CheckCircle2, Clock
- Mission control aesthetic: dark background (slate-950), monospace font for numbers, uppercase labels with tracking-wider

**Create `src/components/AgentHierarchy.tsx`** (left panel, per locked decisions):
- Props: `{ agents: AgentNode[], selectedAgent: string | null, onSelectAgent: (id: string) => void }`
- Group agents by level: L1 (Strategy), L2 (Management), L3 (Execution)
- Each level section has a header: "LEVEL 1: STRATEGY", "LEVEL 2: MANAGEMENT", "LEVEL 3: EXECUTION" with uppercase, tracking-widest, slate-500 text
- Each agent rendered as a compact row (NOT the existing large card):
  - Colored status dot: green=working, blue=idle, red=error, gray=offline (per locked decision)
  - Agent name (bold, slate-200)
  - One-line current task summary truncated with ellipsis (from state.json activity_log per locked decision)
- Selected agent highlighted with left border accent (emerald-500) and slightly lighter background
- Click handler calls `onSelectAgent(agent.id)` (per locked decision -- clicking loads details)
- Vertical connecting line between levels (subtle slate-800 line)
- Scrollable overflow-y-auto for when many agents exist
- Responsive: Full width on mobile, fixed left column on desktop
  </action>
  <verify>
```bash
cd workspace/occc && npx tsc --noEmit
```
No type errors. All component files exist. Page.tsx imports all components without circular dependencies.
  </verify>
  <done>
page.tsx renders mission control 3-panel layout. GlobalMetrics shows tier and status counts at top. AgentHierarchy shows L1/L2/L3 grouped agent list with status dots, names, and task summaries. Clicking an agent sets selectedAgent state, which flows to detail and log panels.
  </done>
</task>

<task type="auto">
  <name>Task 2: AgentDetail, LogStream, and StatusToast components</name>
  <files>
    workspace/occc/src/components/AgentDetail.tsx
    workspace/occc/src/components/LogStream.tsx
    workspace/occc/src/components/StatusToast.tsx
  </files>
  <action>
**Create `src/components/AgentDetail.tsx`** (center panel, Claude's discretion: tabbed view):
- Props: `{ agentId: string, agents: AgentNode[], state: JarvisState }`
- Find the selected agent from agents array by ID
- Three-tab interface using local state: `activeTab: 'overview' | 'tasks' | 'state'`
- Tab bar at top with underline-style active indicator (emerald-500 border-b-2)

**Overview tab:**
- Agent name (large, bold), role/level badge, reports_to relationship
- Status indicator with colored dot and label
- Current task summary (full text, not truncated)
- Last active timestamp (formatted from state.json updated_at)
- Container name if available

**Tasks tab:**
- Filtered list of tasks from state.json where `task.metadata?.agent_id === agentId`
- Each task shows: task_id, status badge (colored), skill_hint, created timestamp
- Expandable: click task to see full activity_log entries
- Activity entries show timestamp, status, and entry text in timeline format
- Empty state: "No tasks assigned to this agent"

**State tab (for debugging):**
- Raw JSON view of the agent's tasks from state.json
- Collapsible/expandable JSON tree using `<pre>` with syntax highlighting via Tailwind colors
- Copy-to-clipboard button for the JSON content
- Note: "Raw Jarvis Protocol state for debugging" subtitle

**Create `src/components/LogStream.tsx`** (right panel, per locked decisions):
- Props: `{ agentId: string }`
- Call `useLogStream(agentId)` hook from Plan 02
- Display connection status indicator: green dot = connected, yellow dot = connecting, red dot = disconnected

**Filtering (per locked decision: severity + text search):**
- Top toolbar with:
  - Severity filter: 4 toggle buttons for debug/info/warn/error -- each toggleable independently, all active by default
  - Text search input: filters logs by message content (case-insensitive substring match)
  - Clear button to reset all logs (`clearLogs()` from hook)
- Filter logs client-side before rendering

**Log rendering:**
- Each log entry: `[timestamp] [LEVEL] message`
- Color code by level: debug=slate-500, info=emerald-400, warn=amber-400, error=red-400
- Monospace font (font-mono), text-xs for compact display
- Timestamps formatted as HH:MM:SS (not full ISO)

**Auto-scroll behavior (Claude's discretion: auto-scroll with scroll-lock detection):**
- Default: auto-scroll enabled, new logs scroll to bottom
- User scrolls up: detect via scroll position check (scrollTop + clientHeight < scrollHeight - threshold), disable auto-scroll
- User scrolls to bottom: re-enable auto-scroll
- "Jump to latest" button appears when auto-scroll is disabled (fixed at bottom of log panel)
- Use `useRef` for scroll container and `useEffect` to manage scroll position

**Create `src/components/StatusToast.tsx`** (per locked decision: toast notifications for state changes):
- Custom hook `useAgentStateMonitor(agents: AgentNode[], state: JarvisState)` that:
  - Tracks previous state via useRef
  - On each render, compares current vs previous agent statuses
  - Detects: agent status changes (idle->working, working->error, etc.), new agents appearing (spawn), agents disappearing (despawn)
  - Fires toast notifications via react-toastify:
    - Error: `toast.error("Agent {name} failed: {last_activity}")` -- red, auto-dismiss 8s
    - Spawn: `toast.info("Agent {name} spawned (L{level})")` -- blue, auto-dismiss 5s
    - Despawn: `toast.warn("Agent {name} stopped")` -- yellow, auto-dismiss 5s
    - Recovery: `toast.success("Agent {name} recovered")` -- green, auto-dismiss 3s
  - Toast position: top-right (per research recommendation)
  - Toasts are brief and dismissible (per locked decision)
- Export the hook for use in page.tsx
- Configure ToastContainer theme: dark, pauseOnHover, closeOnClick, newestOnTop
  </action>
  <verify>
```bash
cd workspace/occc && npx tsc --noEmit
```
No type errors. All 3 component files exist and export their primary components.
  </verify>
  <done>
AgentDetail shows tabbed view (Overview/Tasks/State) for selected agent. LogStream displays filtered, auto-scrolling log entries from useLogStream hook with severity toggles and text search. StatusToast fires dismissible toast notifications on agent state changes (errors, spawns, despawns, recoveries).
  </done>
</task>

</tasks>

<verification>
1. `cd workspace/occc && npx tsc --noEmit` passes
2. `bun run build` succeeds
3. Page renders 3-panel layout at desktop width (>1024px)
4. Page stacks panels vertically at mobile width (<1024px)
5. Clicking agent in hierarchy updates center and right panels
6. Log filtering by severity and text search works
7. Toast notifications appear on state changes
</verification>

<success_criteria>
- Mission control layout matches locked decision: hierarchy left, detail center, logs right
- Responsive: panels stack vertically below lg breakpoint (1024px)
- Agent hierarchy groups by L1/L2/L3 with status dots and task summaries
- Detail panel has 3 tabs (Overview, Tasks, State) per discretionary decision
- Log panel supports severity filtering + text search per locked decision
- Auto-scroll with scroll-lock detection works
- Toast notifications fire for error/spawn/despawn/recovery events
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitoring-uplink/04-03-SUMMARY.md`
</output>
