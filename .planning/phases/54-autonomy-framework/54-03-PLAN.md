# Plan 54-03: Integration Hooks and Event Bus Wiring

## Goal

Wire autonomy system into existing spawn/execute flow and event bus for decoupled communication.

## Context

From 54-CONTEXT.md:
- **Integration approach**: Hook into existing spawn/execute flow at task creation
- **Pool/spawn interaction**: Emit events to existing event bus (decoupled)
- **Persistence**: Store autonomy state in memU

## Tasks

### 1. Create autonomy event types
- [ ] Create `packages/orchestration/src/openclaw/autonomy/events.py`
- [ ] Define `AutonomyEvent` base class with `event_type`, `task_id`, `timestamp`, `payload`
- [ ] Define specific events:
  - `AutonomyStateChanged` (old_state, new_state, reason)
  - `AutonomyConfidenceUpdated` (score, factors)
  - `AutonomyEscalationTriggered` (reason, confidence)
  - `AutonomyRetryAttempted` (attempt_number, max_retries)
- [ ] Add event serialization methods

### 2. Create event bus integration
- [ ] Add `AutonomyEventBus` class wrapping existing event bus
- [ ] Implement `emit(event: AutonomyEvent)` method
- [ ] Implement `subscribe(event_type, handler)` method
- [ ] Add event buffering for high-frequency updates (debounce confidence updates)

### 3. Add spawn flow hooks
- [ ] Create `packages/orchestration/src/openclaw/autonomy/hooks.py`
- [ ] Implement `on_task_spawn(task_id, task_spec) -> AutonomyContext` hook
- [ ] Add hook call in `spawn.py` before container start
- [ ] Ensure hook creates initial autonomy context (state=PLANNING)
- [ ] Add `get_autonomy_context(task_id)` retrieval function

### 4. Create L3 integration interface
- [ ] Create `autonomy_client.py` for L3 container use
- [ ] Implement `report_state_update(task_id, state, confidence)` HTTP client
- [ ] Add `request_escalation(task_id, reason)` method
- [ ] Implement sentinel file writer (JSON format with version)
- [ ] Add retry logic with exponential backoff for HTTP calls

### 5. Wire pool lifecycle events
- [ ] Add `on_task_complete(task_id, result)` hook
- [ ] Add `on_task_failed(task_id, error)` hook
- [ ] Implement automatic state transitions based on pool events
- [ ] Add `on_container_healthy(task_id)` → transition to EXECUTING
- [ ] Add cleanup on task removal (archive autonomy context to memU)

### 6. Create memU persistence integration
- [ ] Add `AutonomyMemoryStore` class
- [ ] Implement `save_context(context)` → memU memorize
- [ ] Implement `load_context(task_id)` → memU retrieve
- [ ] Add memory category: `AUTONOMY_STATE`
- [ ] Add metadata: task_id, project, timestamp for querying

## Verification

- [ ] Event emission test: `AutonomyStateChanged` event emitted on transition
- [ ] Hook test: `on_task_spawn` creates context with PLANNING state
- [ ] Client test: L3 client successfully reports state via HTTP
- [ ] Pool integration test: Task completion updates autonomy state
- [ ] Persistence test: Context saved to memU and retrievable
- [ ] End-to-end test: Spawn → Planning → Executing → Complete flow with events
- [ ] Event bus decoupling test: Autonomy works even if event bus temporarily down

## Dependencies

- Plan 54-01 (state machine, AutonomyContext)
- Plan 54-02 (config loading)
- Phase 50 event bus infrastructure
- Phase 26-38 memU integration patterns

## Notes

- Use existing event bus patterns from phase 50 (Notion sync)
- L3 client should degrade gracefully if autonomy endpoint unavailable
- Sentinal files are local backup if memU unavailable
- Follow spawn.py patterns for hook integration
- Event emission should not block task execution (fire-and-forget)

## Time Estimate

2 hours
