---
phase: 01-protocol-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - openclaw/src/acp/session-mapper.ts
  - openclaw/src/acp/server-translator.ts
  - openclaw/src/acp/session-mapper.test.ts
  - openclaw/src/acp/server-translator.test.ts
autonomous: true
requirements: [HYB-03]

must_haves:
  truths:
    - "IDE clients can resume existing sessions using the sessionKey metadata field"
    - "Workspace isolation and authentication context are correctly applied to native ACP sessions"
  artifacts:
    - path: "openclaw/src/acp/session-mapper.ts"
      provides: "ACP-to-Gateway metadata mapping for orchestration"
  key_links:
    - from: "openclaw/src/acp/session-mapper.ts"
      to: "openclaw/src/acp/server-translator.ts"
      via: "Session initialization context propagation"
---

<objective>
Extend ACP session metadata to support OpenClaw-specific context (session mapping, auth profiles, and workspace isolation) and verify the integration with unit tests.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ACP Metadata for Orchestration</name>
  <files>openclaw/src/acp/session-mapper.ts, openclaw/src/acp/types.ts, openclaw/src/acp/session-mapper.test.ts</files>
  <action>
    1. Update `AcpSessionMeta` and the `parseSessionMeta` function in `session-mapper.ts` to support:
       - `sessionKey`: For explicit session reuse/resumption.
       - `authProfileId`: Targeting a specific credentials profile.
       - `workspaceDir`: Explicitly setting the working directory for the session.
    2. Extract these fields from the ACP `_meta` object in session initialization.
    3. Update `openclaw/src/acp/session-mapper.test.ts` to cover the new metadata fields.
  </action>
  <verify>
    <automated>pnpm test openclaw/src/acp/session-mapper.test.ts</automated>
  </verify>
  <done>Metadata parser correctly handles sessionKey, authProfileId, and workspaceDir, with test coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Apply metadata to Gateway session</name>
  <files>openclaw/src/acp/server-translator.ts, openclaw/src/acp/server-translator.test.ts</files>
  <action>
    1. Update the server-side ACP agent in `server-translator.ts` to propagate the new metadata fields (`sessionKey`, `authProfileId`, `workspaceDir`) when creating or resolving a Gateway session.
    2. Update `openclaw/src/acp/server-translator.test.ts` to verify that these fields reach the underlying Gateway session handler.
  </action>
  <verify>
    <automated>pnpm test openclaw/src/acp/server-translator.test.ts</automated>
  </verify>
  <done>Native ACP sessions apply OpenClaw metadata correctly, verified by unit tests.</done>
</task>

</tasks>

<success_criteria>
- ACP session metadata successfully captures sessionKey, authProfileId, and workspaceDir.
- These fields are propagated to the underlying Gateway session.
- Automated tests verify the complete metadata propagation flow.
</success_criteria>
