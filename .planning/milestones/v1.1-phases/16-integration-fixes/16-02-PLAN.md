---
phase: 16-integration-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - scripts/verify_phase16.py
autonomous: true
requirements:
  - CFG-02
  - CFG-04
  - CFG-06

must_haves:
  truths:
    - "Running scripts/verify_phase16.py exits 0 with all checks passing"
    - "Each of the 3 CFG requirements has a dedicated verification function with PASS/FAIL output"
    - "Deprecated constant removal is verified programmatically"
  artifacts:
    - path: "scripts/verify_phase16.py"
      provides: "Phase 16 verification covering all 3 fixes plus constant cleanup"
      contains: "verify_snapshot_project_id_threading"
  key_links:
    - from: "scripts/verify_phase16.py"
      to: "orchestration/snapshot.py"
      via: "inspect.signature and inspect.getsource checks"
      pattern: "inspect\\.signature|inspect\\.getsource"
    - from: "scripts/verify_phase16.py"
      to: "agents/_templates/soul-default.md"
      via: "template file read and variable substitution check"
      pattern: "soul-default\\.md"
---

<objective>
Create and run the Phase 16 verification script that programmatically confirms all 3 integration fixes (CFG-02, CFG-04, CFG-06) and the deprecated constant removal are correctly implemented.

Purpose: Provide CI-friendly evidence that all fixes are wired correctly, following the established verification script pattern from verify_soul_golden.py.
Output: scripts/verify_phase16.py that exits 0 on success, 1 on any failure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-fixes/16-RESEARCH.md
@.planning/phases/16-integration-fixes/16-01-SUMMARY.md
@scripts/verify_soul_golden.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run Phase 16 verification script</name>
  <files>scripts/verify_phase16.py</files>
  <action>
Create `scripts/verify_phase16.py` following the pattern established by `scripts/verify_soul_golden.py`.

**Structure:**
- Shebang: `#!/usr/bin/env python3`
- Docstring describing purpose
- `sys.path.insert(0, str(Path(__file__).parent.parent))` for imports
- Individual `verify_*()` functions, each returning bool and printing PASS/FAIL
- `main()` that runs all checks and exits 0 on all pass, 1 on any failure

**4 verification functions:**

1. `verify_snapshot_project_id_threading()` — Use `inspect.signature()` to confirm:
   - `capture_semantic_snapshot` has `project_id` parameter with no default
   - `cleanup_old_snapshots` has `project_id` parameter with no default
   - Both functions' source contains `get_snapshot_dir(project_id)` (not bare `get_snapshot_dir()`)

2. `verify_staging_branch_delegates_to_detect()` — Use `inspect.getsource(create_staging_branch)`:
   - Confirm `_detect_default_branch` appears in the source
   - Confirm `symbolic-ref` does NOT appear in the source (no inline duplicate)

3. `verify_template_variable_consumption()` — Read `agents/_templates/soul-default.md`:
   - Confirm `$project_name` appears in the template body
   - Render with `string.Template.safe_substitute()` using test values
   - Confirm the rendered output contains the test value for project_name
   - Confirm no unresolved `$project_name` remains in output

4. `verify_deprecated_constants_removed()` — Import `orchestration.config`:
   - Confirm `hasattr(config, 'STATE_FILE')` is False
   - Confirm `hasattr(config, 'SNAPSHOT_DIR')` is False
   - Confirm `hasattr(config, 'LOCK_TIMEOUT')` is True (not over-deleted)
   - Confirm `hasattr(config, 'POLL_INTERVAL')` is True (not over-deleted)
   - Read config.py source and confirm no `import os` or `from pathlib` lines

**Output format per check:**
```
[PASS] snapshot project_id threading — capture_semantic_snapshot and cleanup_old_snapshots require explicit project_id
[PASS] staging branch detection delegation — create_staging_branch delegates to _detect_default_branch
[FAIL] template variable consumption — $project_name not found in soul-default.md
```

**After writing, run:**
```bash
cd ~/.openclaw && python3 scripts/verify_phase16.py
```

All 4 checks must PASS. If any FAIL, do not mark this task as complete — the failure indicates Plan 01 has a bug.
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 scripts/verify_phase16.py</automated>
  </verify>
  <done>scripts/verify_phase16.py exists, runs without errors, and all 4 checks print PASS with exit code 0.</done>
</task>

</tasks>

<verification>
```bash
cd ~/.openclaw && python3 scripts/verify_phase16.py
echo "Exit code: $?"
```
Expected: All 4 checks print PASS, exit code 0.
</verification>

<success_criteria>
- scripts/verify_phase16.py exists and is executable
- Running it produces 4 PASS lines and exits 0
- Script follows verify_soul_golden.py pattern (functions + main + PASS/FAIL)
- Script is CI-friendly (no interactive prompts, deterministic output)
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-fixes/16-02-SUMMARY.md`
</output>
