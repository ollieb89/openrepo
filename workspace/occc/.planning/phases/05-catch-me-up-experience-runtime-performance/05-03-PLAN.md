---
phase: 05-catch-me-up-experience-runtime-performance
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/app/catch-up/page.tsx, src/components/sync/SummaryStream.tsx, src/components/sync/ClarificationPicker.tsx, src/components/sync/SummaryControls.tsx]
autonomous: true
requirements: [CMEU-01, CMEU-02, CMEU-03, PERF-01]
must_haves:
  truths:
    - "User can query the system from a dedicated Catch-Up page"
    - "The summary streams token-by-token in the UI"
    - "A picker appears when the system is not confident about the query target"
    - "The user can pin a summary to the project context"
  artifacts:
    - path: "src/app/catch-up/page.tsx"
      provides: "The main Catch-Up feature UI"
    - path: "src/components/sync/SummaryStream.tsx"
      provides: "Streaming token renderer"
    - path: "src/components/sync/ClarificationPicker.tsx"
      provides: "Disambiguation UI"
  key_links:
    - from: "src/app/catch-up/page.tsx"
      to: "/api/sync/catch-up"
      via: "fetch with ReadableStream"
    - from: "src/components/sync/SummaryControls.tsx"
      to: "database"
      via: "Pin to Project API call"
---

<objective>
Build the "Catch Me Up" UI to provide a rich, interactive, and streaming experience for natural language project summaries.

Purpose: Surface the power of the RAG engine to the user while allowing for manual disambiguation and result persistence.
Output: Catch-Up page and related streaming/disambiguation components.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catch-me-up-experience-runtime-performance/05-CONTEXT.md
@.planning/phases/05-catch-me-up-experience-runtime-performance/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catch-Up Shell & Streaming UI</name>
  <files>src/app/catch-up/page.tsx, src/components/sync/SummaryStream.tsx</files>
  <action>
    1. Create `src/app/catch-up/page.tsx` with a central search input and a state for the currently active summary stream.
    2. Create `src/components/sync/SummaryStream.tsx` that:
       - Takes a `ReadableStream` from the `/api/sync/catch-up` response.
       - Iteratively updates a React state with new tokens as they are read.
       - Renders Markdown (using a library or basic formatting) and highlights citations.
       - Provides a loading state and an "Interrupt" button to stop the stream.
  </action>
  <verify>Visit `/catch-up` in the browser, type a query, and verify that the summary streams into the UI token-by-token.</verify>
  <done>Users can perform catch-up queries and see streaming results.</done>
</task>

<task type="auto">
  <name>Task 2: Disambiguation & Controls</name>
  <files>src/components/sync/ClarificationPicker.tsx, src/components/sync/SummaryControls.tsx</files>
  <action>
    1. Create `src/components/sync/ClarificationPicker.tsx` to handle `type: 'low_confidence'` responses.
    2. Show the top 3 matches (e.g. "Did you mean the Auth API issue or the Auth Slack thread?") and allow the user to click one to re-run the query with an explicit ID.
    3. Create `src/components/sync/SummaryControls.tsx` for the completed summary:
       - **"Pin to Project"** button: Persists the summary to a new `project_insights` table in the database (or similar).
       - **"Enhance with Cloud"** toggle: A UI-only toggle that provides a placeholder alert: "Cloud enhancement requires opt-in via Privacy Center".
  </action>
  <verify>Enter an ambiguous query; verify the Clarification Picker appears. Complete a query and click "Pin to Project"; verify it persists to the DB.</verify>
  <done>Ambiguous queries are resolvable, and useful summaries can be saved for later.</done>
</task>

</tasks>

<verification>
Perform a full "Catch Me Up" flow:
1. Enter query.
2. Observe streaming results.
3. Verify citations are clickable.
4. Pin the result and verify it stays in the project context (e.g. on refresh or in a list).
</verification>

<success_criteria>
- The UI feels responsive (streaming begins within 2 seconds).
- The user can choose between similar context matches when the engine is unsure.
- Summaries can be persisted to the project database.
</success_criteria>

<output>
After completion, create `.planning/phases/05-catch-me-up-experience-runtime-performance/05-03-SUMMARY.md`
</output>
