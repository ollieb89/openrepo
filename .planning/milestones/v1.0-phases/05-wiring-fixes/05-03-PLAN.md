---
phase: 05-wiring-fixes
plan: 03
type: verify
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - scripts/verify_phase5_integration.py
autonomous: true
requirements:
  - COM-01
  - COM-04

must_haves:
  truths:
    - "A single script validates ALL Phase 5 success criteria in one run"
    - "L1 config.json skill_registry resolves to a real, invokable router_skill"
    - "Snapshots directory is initialized AND snapshot capture produces a valid .diff artifact"
    - "Both COM-01 and COM-04 wiring works together as a unified system"
  artifacts:
    - path: "scripts/verify_phase5_integration.py"
      provides: "End-to-end integration verification tying COM-01 and COM-04 together"
      contains: "def verify_phase5_integration"
  key_links:
    - from: "scripts/verify_phase5_integration.py"
      to: "agents/clawdia_prime/agent/config.json"
      via: "loads and validates L1 config"
      pattern: "config.json"
    - from: "scripts/verify_phase5_integration.py"
      to: "orchestration/init.py"
      via: "calls initialize_workspace to ensure dirs exist"
      pattern: "initialize_workspace"
    - from: "scripts/verify_phase5_integration.py"
      to: "orchestration/snapshot.py"
      via: "imports capture_semantic_snapshot for end-to-end test"
      pattern: "capture_semantic_snapshot"
    - from: "scripts/verify_phase5_integration.py"
      to: "scripts/verify_l1_delegation.py"
      via: "cross-references individual verification results"
      pattern: "verify_l1_delegation"
---

<objective>
Create a single integration verification script that validates ALL Phase 5 success criteria together, proving COM-01 and COM-04 work as a unified system after both Wave 1 plans complete.

Purpose: Plans 05-01 and 05-02 each create component-level verification scripts (verify_l1_delegation.py and verify_snapshots.py). This plan adds a cross-cutting integration test that validates the full wiring chain: L1 config loads -> skill_registry resolves -> router_skill is invokable AND initialization module runs -> snapshots directory exists -> snapshot capture works. It also verifies there are no conflicts between the two subsystems (e.g., config paths consistent, project root detection works for both).

Output: A single integration script that a human or CI can run to confirm Phase 5 is complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Artifacts from 05-01 (must exist before this plan runs)
@agents/clawdia_prime/agent/config.json
@scripts/verify_l1_delegation.py

# Artifacts from 05-02 (must exist before this plan runs)
@orchestration/init.py
@scripts/verify_snapshots.py

# Existing orchestration modules used by both plans
@orchestration/config.py
@orchestration/snapshot.py
@orchestration/__init__.py

# Router skill for delegation test
@skills/router_skill/skill.json
@skills/router_skill/index.js

# Root config for gateway endpoint
@openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 5 integration verification script</name>
  <files>scripts/verify_phase5_integration.py</files>
  <action>
Create `scripts/verify_phase5_integration.py` — a comprehensive integration test that validates all three Phase 5 success criteria in a single run.

**This script is NOT a duplicate of the component scripts.** It tests cross-cutting concerns that neither component script covers:

1. **Cross-System Config Consistency** — Verify L1 config.json and orchestration/config.py agree on paths:
   - Load L1 config.json gateway endpoint
   - Load openclaw.json gateway port
   - Verify they reference the same gateway (e.g., both point to port 18789)
   - Load SNAPSHOT_DIR from orchestration/config.py
   - Verify it resolves to the same path that orchestration/init.py creates
   - Report any mismatches as FAIL with details

2. **Initialization-Then-Delegation Sequence** — Test the actual startup sequence:
   - Run `initialize_workspace()` from orchestration/init.py (ensures snapshots dir exists)
   - Then load L1 config.json and resolve skill_registry.router.skill_path
   - Verify the resolved path contains both index.js and skill.json
   - This proves the system can boot (init) and then delegate (config + skill)
   - Report: init result, config loaded, skill resolved

3. **Full Success Criteria Check** — Validate each Phase 5 success criterion explicitly:
   - SC1: "ClawdiaPrime (L1) has a config.json with skill_registry referencing router_skill"
     → Load config, check skill_registry.router exists and skill_path resolves
   - SC2: "L1 -> L2 delegation flow completes end-to-end"
     → Check router_skill files exist, attempt node invocation (or report WIRING OK if gateway down)
   - SC3: "workspace/.openclaw/snapshots/ directory exists and snapshot capture flow works"
     → Check directory exists, check it is writable, verify config.py SNAPSHOT_DIR matches
   - Report each SC as PASS/FAIL with the criterion text

4. **Phase Completion Summary** — Print a clear pass/fail summary:
   - List all success criteria with status
   - List requirement IDs (COM-01, COM-04) with coverage status
   - Print "PHASE 5 COMPLETE" if all pass, "PHASE 5 INCOMPLETE" if any fail
   - Exit 0 if complete, exit 1 if incomplete

**Output format:** Structured sections with ANSI colors. Each section header, each check on its own line with [PASS]/[FAIL]/[WARN] prefix.

**Error handling:**
- If a 05-01 artifact is missing (config.json, verify_l1_delegation.py): report as FAIL with "Run plan 05-01 first"
- If a 05-02 artifact is missing (init.py, verify_snapshots.py): report as FAIL with "Run plan 05-02 first"
- Gateway not running: report delegation wiring as PASS (config resolves) but runtime as WARN (not a failure — wiring is correct)

**Do NOT use external dependencies** — only Python stdlib (json, subprocess, socket, pathlib, sys, os, importlib).
  </action>
  <verify>
```bash
cd ~/.openclaw && python3 scripts/verify_phase5_integration.py
```
Script runs without import errors. If 05-01 and 05-02 artifacts exist: reports success criteria status. If artifacts are missing: reports which plan needs to run first. Exit code reflects overall status.

Verify it is distinct from component scripts:
```bash
# Should test cross-system consistency (not in component scripts)
grep -c "config_consistency\|cross.*system\|success.*criter" scripts/verify_phase5_integration.py
```
Should return non-zero count (proves it tests integration concerns, not just repeating component checks).
  </verify>
  <done>
scripts/verify_phase5_integration.py exists and runs without errors. It validates cross-system config consistency between L1 config.json and orchestration/config.py, tests the initialization-then-delegation startup sequence, explicitly checks all three Phase 5 success criteria, and prints a phase completion summary with requirement coverage. Distinct from component verification scripts — tests integration concerns only.
  </done>
</task>

</tasks>

<verification>
1. `python3 scripts/verify_phase5_integration.py` runs without import errors
2. Script checks all 3 success criteria explicitly (not just re-running component tests)
3. Script validates cross-system config consistency (gateway endpoint, snapshot path)
4. Script reports COM-01 and COM-04 requirement coverage
5. Script prints clear PHASE 5 COMPLETE / INCOMPLETE summary
6. Exit code 0 = all pass, exit code 1 = any fail
</verification>

<success_criteria>
- Integration script validates all Phase 5 success criteria in one run
- Cross-system consistency checked (L1 config vs orchestration config vs openclaw.json)
- Phase completion status clearly reported with requirement coverage
- Handles missing artifacts gracefully (directs user to run prerequisite plans)
- No external dependencies (Python stdlib only)
</success_criteria>

<output>
After completion, create `.planning/phases/05-wiring-fixes/05-03-SUMMARY.md`
</output>
