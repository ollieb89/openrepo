---
phase: 46-schema-validation-fail-fast-startup
plan: 03
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - packages/orchestration/src/openclaw/cli/config.py
  - packages/orchestration/pyproject.toml
  - config/openclaw.json.example
autonomous: true
requirements: [CONF-02, CONF-06]

must_haves:
  truths:
    - "Running `openclaw-config show` prints the effective openclaw.json as pretty-printed JSON to stdout"
    - "Running `openclaw-config show` with a broken config (missing gateway.port) exits non-zero with an actionable ERROR on stderr"
    - "`config/openclaw.json.example` exists and documents all valid fields with type, default, and required/optional status"
    - "The `openclaw-config` script entry point is registered in pyproject.toml"
  artifacts:
    - path: "packages/orchestration/src/openclaw/cli/config.py"
      provides: "openclaw-config CLI with show subcommand"
      exports: ["main"]
      min_lines: 50
    - path: "config/openclaw.json.example"
      provides: "Human-readable schema documentation for openclaw.json"
      min_lines: 40
  key_links:
    - from: "packages/orchestration/pyproject.toml"
      to: "packages/orchestration/src/openclaw/cli/config.py"
      via: "openclaw-config script entry point"
      pattern: "openclaw-config.*openclaw.cli.config:main"
    - from: "packages/orchestration/src/openclaw/cli/config.py"
      to: "packages/orchestration/src/openclaw/project_config.py"
      via: "load_and_validate_openclaw_config() call in cmd_show"
      pattern: "load_and_validate_openclaw_config"
---

<objective>
Add the `openclaw config show` CLI command and the `config/openclaw.json.example` schema documentation file.

Purpose: CONF-02 — schema documented in human-readable form; operators can audit the effective config. CONF-06 — the show command exercises the wired validation path for manual verification.
Output: New `cli/config.py` entry point, `pyproject.toml` script registration, and `config/openclaw.json.example` documenting every valid field.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-schema-validation-fail-fast-startup/46-CONTEXT.md
@.planning/phases/46-schema-validation-fail-fast-startup/46-RESEARCH.md
@.planning/phases/46-schema-validation-fail-fast-startup/46-02-SUMMARY.md
@packages/orchestration/src/openclaw/cli/project.py
@packages/orchestration/src/openclaw/project_config.py
@packages/orchestration/pyproject.toml
@config/openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cli/config.py with openclaw-config show subcommand</name>
  <files>
    packages/orchestration/src/openclaw/cli/config.py
    packages/orchestration/pyproject.toml
  </files>
  <action>
    Create `packages/orchestration/src/openclaw/cli/config.py` (new file — does not exist).

    Pattern: consistent with `cli/project.py` (argparse subparsers, Colors class, exit codes 0/1).
    The `show` subcommand calls `load_and_validate_openclaw_config()` which already has
    schema validation wired (from Plan 02). Output: pretty-printed JSON to stdout.

    ```python
    """
    OpenClaw Config CLI

    Provides subcommands for inspecting the OpenClaw configuration.

    Usage:
        openclaw-config show          # Print effective openclaw.json config
    """

    import argparse
    import json
    import sys

    from openclaw.project_config import load_and_validate_openclaw_config
    from openclaw.config import get_project_root


    # ANSI color codes — same as project.py and monitor.py
    class Colors:
        GREEN  = '\033[92m'
        RED    = '\033[91m'
        YELLOW = '\033[93m'
        BLUE   = '\033[94m'
        RESET  = '\033[0m'
        BOLD   = '\033[1m'


    def cmd_show(args: argparse.Namespace) -> int:
        """
        Print the effective openclaw.json configuration.

        Loads the config through the normal validation path — schema validation
        and agent hierarchy validation run as part of the load. If the config
        is invalid, validation errors are printed to stderr and this command
        exits non-zero before printing.

        Output format: pretty-printed JSON (indent=2) to stdout.
        """
        root = get_project_root()
        config_path = root / "openclaw.json"

        try:
            config = load_and_validate_openclaw_config()
        except FileNotFoundError:
            print(
                f"{Colors.RED}ERROR{Colors.RESET}: openclaw.json not found at {config_path}",
                file=sys.stderr,
            )
            return 1
        except Exception as exc:
            print(
                f"{Colors.RED}ERROR{Colors.RESET}: Failed to load config: {exc}",
                file=sys.stderr,
            )
            return 1

        print(f"# Effective config loaded from: {config_path}")
        print(json.dumps(config, indent=2, default=str))
        return 0


    def main() -> None:
        """CLI entrypoint for OpenClaw Config tools."""
        parser = argparse.ArgumentParser(
            description="OpenClaw Config Tools",
            formatter_class=argparse.RawDescriptionHelpFormatter,
        )

        subparsers = parser.add_subparsers(dest="command", help="Subcommand to run")

        # --- show ---
        subparsers.add_parser(
            "show",
            help="Print the effective openclaw.json configuration (file values + defaults merged)",
        )

        args = parser.parse_args()

        if not args.command:
            parser.print_help()
            sys.exit(1)

        if args.command == "show":
            sys.exit(cmd_show(args))
        else:
            parser.print_help()
            sys.exit(1)


    if __name__ == "__main__":
        main()
    ```

    Then add the script entry point to `packages/orchestration/pyproject.toml`.
    In `[project.scripts]`, add:
    ```toml
    openclaw-config = "openclaw.cli.config:main"
    ```

    After adding, reinstall the package so the script is available:
    ```bash
    cd ~/.openclaw && uv pip install -e packages/orchestration/
    ```

    Then verify the entry point works:
    ```bash
    cd ~/.openclaw && openclaw-config show 2>&1 | head -5
    ```
    Should print `# Effective config loaded from: ...` followed by JSON.

    Commit: `feat(46-03): add openclaw-config show CLI subcommand`
  </action>
  <verify>
    <automated>cd ~/.openclaw && openclaw-config show 2>&1 | head -3</automated>
    <manual>Output should start with "# Effective config loaded from:" followed by JSON containing "gateway" and "agents" keys.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    `cli/config.py` exists and `openclaw-config show` prints the effective config.
    `pyproject.toml` has `openclaw-config = "openclaw.cli.config:main"` in [project.scripts].
  </done>
</task>

<task type="auto">
  <name>Task 2: Create config/openclaw.json.example as schema documentation</name>
  <files>
    config/openclaw.json.example
  </files>
  <action>
    Create `config/openclaw.json.example` (new file — does not exist).

    This is a valid JSON file serving as human-readable schema documentation.
    It explains each field's type, default, and required/optional status via
    descriptive `"_comment_*"` keys adjacent to each field (JSON does not support
    comments; this is the standard workaround that keeps the file valid JSON).

    The file uses example values that match the real openclaw.json structure
    (verified against the actual config/openclaw.json). Every field present
    in OPENCLAW_JSON_SCHEMA must appear here with documentation.

    ```json
    {
      "_comment": "openclaw.json schema reference — copy and remove _comment_* keys for your config",
      "_schema_version": "1.5",

      "_comment_meta": "OPTIONAL object — metadata about when the config was last modified",
      "meta": {
        "lastTouchedVersion": "2026.2.15",
        "lastTouchedAt": "2026-02-17T00:00:00.000Z"
      },

      "_comment_active_project": "OPTIONAL string — active project ID; override with OPENCLAW_PROJECT env var",
      "active_project": "myproject",

      "_comment_source_directories": "OPTIONAL array of strings — root directories where project workspaces live",
      "source_directories": [
        "/home/user/Development/Projects",
        "/home/user/Development/Tools"
      ],

      "_comment_gateway": "REQUIRED object — OpenClaw gateway process config",
      "gateway": {
        "_comment_port": "REQUIRED integer (1-65535) — port the gateway listens on. Example: 18789",
        "port": 18789,
        "_comment_mode": "OPTIONAL string — 'local' (default) or 'remote'",
        "mode": "local",
        "_comment_bind": "OPTIONAL string — 'loopback' (default) or 'any'",
        "bind": "loopback",
        "_comment_auth": "OPTIONAL object — auth mode and token. Use env var ${OPENCLAW_GATEWAY_TOKEN} for the token value",
        "auth": {
          "mode": "token",
          "token": "${OPENCLAW_GATEWAY_TOKEN}"
        }
      },

      "_comment_agents": "REQUIRED object — agent registry; must contain 'list' array",
      "agents": {
        "_comment_defaults": "OPTIONAL object — default model, sandbox, and concurrency settings for all agents",
        "defaults": {
          "model": {
            "primary": "google-gemini-cli/gemini-2.5-flash"
          },
          "sandbox": {
            "mode": "non-main",
            "workspaceAccess": "none",
            "scope": "session"
          },
          "maxConcurrent": 4
        },
        "_comment_list": "REQUIRED array — list of agent definitions. Each agent needs: id (string), name (string), level (int 1-3), reports_to (string or null)",
        "list": [
          {
            "id": "l1_orchestrator",
            "name": "Strategic Orchestrator",
            "level": 1,
            "reports_to": null
          },
          {
            "id": "myproject_pm",
            "name": "MyProject Project Manager",
            "level": 2,
            "reports_to": "l1_orchestrator",
            "project": "myproject"
          },
          {
            "id": "l3_specialist",
            "name": "L3 Specialist Executor",
            "level": 3,
            "reports_to": "myproject_pm"
          }
        ]
      },

      "_comment_commands": "OPTIONAL object — native command and skill discovery settings",
      "commands": {
        "native": "auto",
        "nativeSkills": "auto"
      },

      "_comment_channels": "OPTIONAL object — external channel integrations (e.g. telegram)",
      "channels": {
        "telegram": {
          "enabled": true,
          "botToken": "${OPENCLAW_TELEGRAM_BOT_TOKEN}"
        }
      },

      "_comment_memory": "OPTIONAL object — memU service config",
      "memory": {
        "_comment_memu_api_url": "OPTIONAL string — URL of the memU API. Default: empty (memory disabled)",
        "memu_api_url": "http://localhost:18791"
      },

      "_comment_plugins": "OPTIONAL object — plugin enable/disable registry",
      "plugins": {
        "entries": {
          "telegram": {
            "enabled": true
          }
        }
      }
    }
    ```

    After creating the file, verify it is valid JSON:
    ```bash
    python3 -c "import json; json.load(open('~/.openclaw/config/openclaw.json.example')); print('valid JSON')"
    ```

    Commit: `docs(46-03): add openclaw.json.example as human-readable schema documentation`
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('~/.openclaw/config/openclaw.json.example')); print('valid JSON')"</automated>
    <manual>Open the file and confirm every field from OPENCLAW_JSON_SCHEMA has a corresponding entry with a _comment_ sibling key explaining type, default, and required/optional status.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    `config/openclaw.json.example` exists.
    It is valid JSON (no syntax errors).
    Every OPENCLAW_JSON_SCHEMA property is documented with a `_comment_*` key.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final regression check and phase success criteria verification</name>
  <files></files>
  <action>
    Run the complete test suite to confirm no regressions from Plan 03 changes:
    ```bash
    cd ~/.openclaw && uv run pytest packages/orchestration/tests/ -v 2>&1 | tail -15
    ```

    Then manually verify all 4 phase success criteria from the roadmap:

    **Criterion 1** — Unknown field → warning:
    ```bash
    cd ~/.openclaw && python3 -c "
    from openclaw.config_validator import validate_openclaw_config
    fatal, warnings = validate_openclaw_config(
        {'gateway': {'port': 18789}, 'agents': {'list': []}, 'gatewayy': {}},
        'test'
    )
    print('fatal:', fatal)
    print('warnings:', warnings)
    assert not fatal, 'Unknown field must not be fatal'
    assert any('gatewayy' in w for w in warnings), 'Warning must name the unknown field'
    print('CRITERION 1: PASS')
    "
    ```

    **Criterion 2** — Missing required field → exit before work:
    ```bash
    cd ~/.openclaw && python3 -c "
    from openclaw.config_validator import validate_openclaw_config
    fatal, _ = validate_openclaw_config({'gateway': {},'agents': {'list': []}}, 'test')
    assert fatal, 'Missing port must be fatal'
    assert any('port' in e.lower() or 'gateway' in e.lower() for e in fatal), 'Must name the field'
    print('CRITERION 2: PASS — fatal:', fatal[:1])
    "
    ```

    **Criterion 3** — Schema documented (already satisfied by openclaw.json.example):
    ```bash
    python3 -c "import json; d=json.load(open('~/.openclaw/config/openclaw.json.example')); print('CRITERION 3: PASS — fields:', [k for k in d if not k.startswith('_')])"
    ```

    **Criterion 4** — Valid config loads without error:
    ```bash
    cd ~/.openclaw && python3 -c "
    from openclaw.project_config import load_and_validate_openclaw_config
    c = load_and_validate_openclaw_config()
    print('CRITERION 4: PASS — gateway port:', c['gateway']['port'])
    "
    ```

    If all 4 criteria pass, commit:
    `chore(46-03): verify phase 46 success criteria — all pass`

    If any criterion fails, fix it before committing.
  </action>
  <verify>
    <automated>cd ~/.openclaw && uv run pytest packages/orchestration/tests/ 2>&1 | tail -3</automated>
    <manual>All 4 roadmap success criteria scripts must print PASS. Any failure must be investigated and fixed before marking plan complete.</manual>
    <sampling_rate>run once at end of plan</sampling_rate>
  </verify>
  <done>
    All 158 tests pass.
    All 4 phase success criteria verified passing.
    Phase 46 implementation complete.
  </done>
</task>

</tasks>

<verification>
After Plan 03:
1. `openclaw-config show` prints effective config JSON to stdout
2. `openclaw-config show` in a directory without `openclaw.json` prints ERROR to stderr and exits 1
3. `config/openclaw.json.example` is valid JSON: `python3 -c "import json; json.load(open('~/.openclaw/config/openclaw.json.example'))"`
4. `openclaw-config` appears in `pyproject.toml` scripts section
5. Full test suite still passes: `uv run pytest packages/orchestration/tests/ 2>&1 | tail -3`
</verification>

<success_criteria>
- `openclaw-config show` works end-to-end against the real `config/openclaw.json`
- `config/openclaw.json.example` documents every field from OPENCLAW_JSON_SCHEMA
- All 4 phase success criteria from ROADMAP.md are demonstrably true
- Test count is 158 (151 prior + 7 new schema tests), all passing
</success_criteria>

<output>
After completion, create `.planning/phases/46-schema-validation-fail-fast-startup/46-03-SUMMARY.md`
</output>
