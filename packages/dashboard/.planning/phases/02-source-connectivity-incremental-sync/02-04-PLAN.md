---
phase: 02-source-connectivity-incremental-sync
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
requirements: [INTG-04, PERF-02, INTG-03]
files_modified:
  - src/app/settings/connectors/page.tsx
  - src/components/connectors/SyncDashboard.tsx
  - src/components/layout/HeaderSyncStatus.tsx
  - src/components/common/SyncToast.tsx
  - src/lib/hooks/useSyncStatus.ts
  - src/app/api/connectors/health/route.ts
  - tests/connectors/sync-status.test.tsx
autonomous: true
must_haves:
  truths:
    - "Sync status is visible in both dedicated dashboard and always-visible compact indicator."
    - "Progress visibility includes per-connector counters and current step, not percent-only reporting."
    - "Interrupted sync resumes with preserved progress/checkpoint context and exposes actionable recovery controls."
    - "Connector health states and priority ordering remain consistent across backend and UI surfaces."
  artifacts:
    - path: "src/components/connectors/SyncDashboard.tsx"
      provides: "Detailed sync and health dashboard with counters, throughput, and recovery controls."
    - path: "src/components/layout/HeaderSyncStatus.tsx"
      provides: "Compact always-visible status indicator with traffic-light semantics."
    - path: "src/app/api/connectors/health/route.ts"
      provides: "Aggregated health/progress endpoint consumed by dashboard and compact indicator."
    - path: "src/components/common/SyncToast.tsx"
      provides: "Transient completion/failure notifications paired with persistent status cards."
  key_links:
    - from: "src/lib/hooks/useSyncStatus.ts"
      to: "src/app/api/connectors/health/route.ts"
      via: "polling/subscription hook feeds both dashboard and compact indicator"
      pattern: "shared status data source"
    - from: "src/components/connectors/SyncDashboard.tsx"
      to: "src/app/api/connectors/[id]/sync/route.ts"
      via: "recovery controls call sync-now and reconnect flows"
      pattern: "actionable recovery controls"
---

# Phase 02-04 Plan: Dual-Surface Sync Visibility, Health UX, and Recovery Controls

## Objective
Ship the required sync visibility surfaces (dashboard + compact indicator), health status semantics, and interruption recovery UX for Phase 2 completion.

## Context
- `.planning/phases/02-source-connectivity-incremental-sync/02-CONTEXT.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-RESEARCH.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-02-PLAN.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-03-PLAN.md`

<tasks>
  <task id="P02-04-T01" wave="3" type="auto">
    <name>Build sync dashboard and compact status indicator powered by a shared health/progress API</name>
    <title>Build sync dashboard and compact status indicator powered by a shared health/progress API</title>
    <requirements>INTG-04,PERF-02</requirements>
    <depends_on>P02-02-T02,P02-03-T02</depends_on>
    <files>
      <file>src/app/settings/connectors/page.tsx</file>
      <file>src/components/connectors/SyncDashboard.tsx</file>
      <file>src/components/layout/HeaderSyncStatus.tsx</file>
      <file>src/lib/hooks/useSyncStatus.ts</file>
      <file>src/app/api/connectors/health/route.ts</file>
      <file>tests/connectors/sync-status.test.tsx</file>
    </files>
    <action>Implement a dedicated sync dashboard and compact always-visible header indicator that both consume one normalized status endpoint. Surface per-connector and per-source counters, current stage, throughput, retry/backoff info, and last-successful-sync timestamps. Preserve locked health semantics and status priority ordering across both surfaces (`auth_expired` &gt; `error` &gt; `rate_limited` &gt; `syncing` &gt; `connected`) with traffic-light glanceability in compact mode.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/sync-status.test.tsx</automated>
      <manual>Start sync and observe dashboard + header indicator update together; confirm counters and stage change over time and status priority rendering is consistent when multiple error states are present.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Both required visibility surfaces exist, share one truth source, and expose meaningful progress/health details beyond percent bars.</done>
  </task>

  <task id="P02-04-T02" wave="3" type="auto">
    <name>Add persistent recovery cards and transient toasts for completion/failure and resume actions</name>
    <title>Add persistent recovery cards and transient toasts for completion/failure and resume actions</title>
    <requirements>PERF-02,INTG-03,INTG-04</requirements>
    <depends_on>P02-04-T01</depends_on>
    <files>
      <file>src/components/connectors/SyncDashboard.tsx</file>
      <file>src/components/common/SyncToast.tsx</file>
      <file>src/lib/hooks/useSyncStatus.ts</file>
      <file>tests/connectors/sync-status.test.tsx</file>
    </files>
    <action>Implement transient notifications for sync completion/failure plus persistent status cards containing actionable controls (`Retry`, `Reconnect`, `Resume now`). When sync is interrupted, cards must preserve last checkpoint metadata and explain resume behavior to user confidence. Ensure auth-expired states hard-stop sync and present explicit re-auth CTA while retaining last successful sync context.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/sync-status.test.tsx</automated>
      <manual>Interrupt an in-progress sync and verify persistent card offers resume/retry with last checkpoint info; simulate auth expiry and confirm re-auth CTA appears with sync blocked until reconnect.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Users receive immediate plus persistent sync outcome feedback and can recover from interruption/auth/rate-limit states without hidden system behavior.</done>
  </task>
</tasks>

## Verification Criteria
- Dashboard and compact indicator are both present and always reflect the same status source.
- Progress includes counters + stage + throughput, not percent-only abstraction.
- Interruption and auth-expired scenarios provide clear, actionable recovery paths with preserved sync context.

## Must Haves
<must_haves>
  <must_have id="MH-08" requirement="INTG-04">Connector health statuses are visible and action-oriented across product surfaces.</must_have>
  <must_have id="MH-09" requirement="PERF-02">Initial and subsequent sync runs show progress and resume correctly after interruption.</must_have>
  <must_have id="MH-10" requirement="INTG-03">Recovery flows keep cursor/checkpoint semantics intact to avoid duplicate or skipped records.</must_have>
</must_haves>
