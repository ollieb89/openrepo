---
phase: 45-path-resolver-constants-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "45-01"
files_modified:
  - packages/orchestration/src/openclaw/project_config.py
  - packages/orchestration/src/openclaw/snapshot.py
  - packages/orchestration/src/openclaw/cli/monitor.py
  - skills/spawn/spawn.py
  - skills/spawn/pool.py
  - packages/orchestration/tests/test_spawn_memory.py
autonomous: true
requirements:
  - CONF-01
  - CONF-05

must_haves:
  truths:
    - "All components resolve workspace state paths through config.get_state_path() — no module has its own path derivation logic"
    - "grep-ing the codebase for pool defaults, lock timeouts, cache TTL, log levels, and memory budget cap returns only config.py as the source — no duplicated literals"
    - "All call sites (state_engine, spawn, pool, monitor, snapshot) import constants from config.py rather than defining their own"
    - "Existing test suite passes after migration"
  artifacts:
    - path: "packages/orchestration/src/openclaw/project_config.py"
      provides: "Cleaned module — get_state_path, get_snapshot_dir, _find_project_root, _POOL_CONFIG_DEFAULTS removed"
    - path: "skills/spawn/spawn.py"
      provides: "Updated imports from openclaw.config for path resolution and MEMORY_CONTEXT_BUDGET"
    - path: "skills/spawn/pool.py"
      provides: "Updated imports from openclaw.config for pool defaults, get_state_path, get_snapshot_dir"
    - path: "packages/orchestration/src/openclaw/cli/monitor.py"
      provides: "Updated to use config.get_project_root() and config.get_state_path()"
    - path: "packages/orchestration/src/openclaw/snapshot.py"
      provides: "get_snapshot_dir import switched from project_config to config"
  key_links:
    - from: "skills/spawn/spawn.py"
      to: "packages/orchestration/src/openclaw/config.py"
      via: "from openclaw.config import get_project_root, get_state_path, MEMORY_CONTEXT_BUDGET"
      pattern: "from openclaw\\.config import"
    - from: "skills/spawn/pool.py"
      to: "packages/orchestration/src/openclaw/config.py"
      via: "from openclaw.config import DEFAULT_POOL_*, get_state_path, get_snapshot_dir"
      pattern: "from openclaw\\.config import"
    - from: "packages/orchestration/src/openclaw/project_config.py"
      to: "packages/orchestration/src/openclaw/config.py"
      via: "from .config import DEFAULT_POOL_* for get_pool_config() defaults"
      pattern: "from \\.config import"
    - from: "packages/orchestration/src/openclaw/cli/monitor.py"
      to: "packages/orchestration/src/openclaw/config.py"
      via: "from openclaw.config import get_project_root, get_state_path"
      pattern: "from openclaw\\.config import"
---

<objective>
Migrate all call sites to import path resolution and constants from config.py, and remove all duplicated definitions.

Purpose: Complete the CONF-01 and CONF-05 requirements by eliminating every divergent path resolver and duplicated constant definition. After this plan, `grep` for any constant or path function hits only config.py.
Output: Updated project_config.py, pool.py, spawn.py, monitor.py, snapshot.py, and test files — all importing from config.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-path-resolver-constants-foundation/45-RESEARCH.md
@.planning/phases/45-path-resolver-constants-foundation/45-01-SUMMARY.md
@packages/orchestration/src/openclaw/config.py
@packages/orchestration/src/openclaw/project_config.py
@skills/spawn/spawn.py
@skills/spawn/pool.py
@packages/orchestration/src/openclaw/cli/monitor.py
@packages/orchestration/src/openclaw/snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate project_config.py and snapshot.py</name>
  <files>
    packages/orchestration/src/openclaw/project_config.py
    packages/orchestration/src/openclaw/snapshot.py
  </files>
  <action>
**project_config.py changes:**

1. REMOVE `_find_project_root()` function entirely — replaced by `config._find_project_root()` / `config.get_project_root()`

2. REMOVE `get_state_path()` function entirely — replaced by `config.get_state_path()`

3. REMOVE `get_snapshot_dir()` function entirely — replaced by `config.get_snapshot_dir()`

4. REMOVE `_POOL_CONFIG_DEFAULTS` dict entirely

5. ADD import at top: `from .config import DEFAULT_POOL_MAX_CONCURRENT, DEFAULT_POOL_MODE, DEFAULT_POOL_OVERFLOW_POLICY, DEFAULT_POOL_QUEUE_TIMEOUT_S, DEFAULT_POOL_RECOVERY_POLICY`

6. UPDATE `get_pool_config()` to build its defaults dict from imported config constants:
   ```python
   defaults = {
       "max_concurrent": DEFAULT_POOL_MAX_CONCURRENT,
       "pool_mode": DEFAULT_POOL_MODE,
       "overflow_policy": DEFAULT_POOL_OVERFLOW_POLICY,
       "queue_timeout_s": DEFAULT_POOL_QUEUE_TIMEOUT_S,
       "recovery_policy": DEFAULT_POOL_RECOVERY_POLICY,
   }
   ```
   Rest of get_pool_config() logic unchanged.

7. UPDATE any internal references to `_find_project_root()` in remaining functions (like `load_and_validate_openclaw_config`) to import from `.config`:
   `from .config import get_project_root` and use `get_project_root()` instead of `_find_project_root()`

8. KEEP all other public functions: `load_project_config`, `get_workspace_path`, `get_active_project_id`, `get_agent_mapping`, `get_memu_config`, `get_source_directories`, `get_pool_config`, `load_and_validate_openclaw_config`, `ProjectNotFoundError`

**snapshot.py changes:**

Update import: change `from .project_config import ..., get_snapshot_dir` to import `get_snapshot_dir` from `.config` instead. Keep other project_config imports.
  </action>
  <verify>
    <automated>cd ~/.openclaw && uv run python -c "from openclaw.project_config import get_pool_config, load_project_config, get_active_project_id; from openclaw.snapshot import get_snapshot_dir; print('imports OK')"</automated>
  </verify>
  <done>project_config.py has no path resolver functions or pool defaults dict; snapshot.py imports get_snapshot_dir from config</done>
</task>

<task type="auto">
  <name>Task 2: Migrate spawn.py, pool.py, monitor.py, and tests</name>
  <files>
    skills/spawn/spawn.py
    skills/spawn/pool.py
    packages/orchestration/src/openclaw/cli/monitor.py
    packages/orchestration/tests/test_spawn_memory.py
  </files>
  <action>
**spawn.py changes:**

1. REMOVE `project_root = Path(__file__).parent.parent.parent` (around line 433) and all other `Path(__file__)` root derivations (around line 131 for config_path)

2. REMOVE local `MEMORY_CONTEXT_BUDGET = 2000` constant (around line 42)

3. ADD imports: `from openclaw.config import get_project_root, get_state_path, MEMORY_CONTEXT_BUDGET`

4. Replace `project_root` usages with `get_project_root()`:
   - Config path: `get_project_root() / "agents" / ...`
   - Volume mounts host-side: `str(get_project_root() / "workspace" / ".openclaw")`

5. REMOVE `get_state_path` from the `from openclaw.project_config import ...` line (it no longer lives there)

6. For OPENCLAW_STATE_FILE injection into containers: KEEP the hardcoded container-side path `f"/workspace/.openclaw/{project_id}/workspace-state.json"` — this is the container-internal mount path, NOT the host path. Add a comment: `# Container-internal path — host equivalent is config.get_state_path(project_id)`

7. For host-side JarvisState creation, use `get_state_path(project_id)` from config

**pool.py changes:**

1. REMOVE `_POOL_DEFAULTS` dict (around lines 43-49)

2. UPDATE imports: move `get_state_path` and `get_snapshot_dir` from `openclaw.project_config` to `openclaw.config`

3. ADD imports: `from openclaw.config import DEFAULT_POOL_MAX_CONCURRENT, DEFAULT_POOL_MODE, DEFAULT_POOL_OVERFLOW_POLICY, DEFAULT_POOL_QUEUE_TIMEOUT_S, DEFAULT_POOL_RECOVERY_POLICY`

4. Replace all `_POOL_DEFAULTS["key"]` references with corresponding `DEFAULT_POOL_*` constants (e.g., `_POOL_DEFAULTS["overflow_policy"]` → `DEFAULT_POOL_OVERFLOW_POLICY`)

**monitor.py changes:**

1. REMOVE `root = Path(__file__).parent.parent` (around line 65) from `_discover_projects()`

2. ADD import: `from openclaw.config import get_project_root, get_state_path`

3. In `_discover_projects()`:
   - Use `get_project_root()` for `projects_dir` enumeration: `projects_dir = get_project_root() / "projects"`
   - Use `get_state_path(entry.name)` for state file path instead of inline construction

**test_spawn_memory.py changes:**

1. UPDATE import: change `from spawn import ..., MEMORY_CONTEXT_BUDGET` to `from openclaw.config import MEMORY_CONTEXT_BUDGET` (keep other spawn imports)

2. Verify no other test files import path functions or constants from project_config or pool that have been moved. Check with grep before committing.
  </action>
  <verify>
    <automated>cd ~/.openclaw && uv run pytest packages/orchestration/tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All call sites import from config.py; no module defines its own path resolver or pool defaults; test suite passes</done>
</task>

<task type="auto">
  <name>Task 3: Verify no duplicated constants remain</name>
  <files></files>
  <action>
Run verification grep commands to confirm CONF-01 and CONF-05 success criteria:

1. Pool defaults: `grep -rn "max_concurrent.*=.*3" packages/orchestration/src/ skills/spawn/ --include="*.py"` — should only hit config.py definition and get_pool_config() defaults dict construction
2. Memory budget: `grep -rn "MEMORY_CONTEXT_BUDGET\|2000.*budget\|budget.*2000" packages/orchestration/src/ skills/spawn/ --include="*.py"` — should only hit config.py definition and imports
3. Path resolver: `grep -rn "_find_project_root\|Path(__file__)" packages/orchestration/src/ skills/spawn/ --include="*.py"` — `_find_project_root` should only hit config.py; `Path(__file__)` should NOT appear for root discovery (may still exist for other purposes like skill path detection — that's OK)
4. Pool defaults dict: `grep -rn "_POOL_DEFAULTS\|_POOL_CONFIG_DEFAULTS" packages/orchestration/src/ skills/spawn/ --include="*.py"` — should return zero results
5. Import consistency: `grep -rn "from.*project_config.*import.*get_state_path\|from.*project_config.*import.*get_snapshot_dir" packages/orchestration/src/ skills/spawn/ --include="*.py"` — should return zero results (all moved to config)

If any grep reveals duplicated definitions (not imports), fix the offending file before completing.
  </action>
  <verify>
    <automated>cd ~/.openclaw && grep -rn "_POOL_DEFAULTS\|_POOL_CONFIG_DEFAULTS" packages/orchestration/src/ skills/spawn/ --include="*.py" | grep -v "test_" | wc -l</automated>
    <manual>Grep output shows zero duplicated pool defaults dicts outside tests</manual>
  </verify>
  <done>No duplicated constants or path resolvers remain outside config.py — CONF-01 and CONF-05 success criteria met</done>
</task>

</tasks>

<verification>
1. Full test suite: `uv run pytest packages/orchestration/tests/ -x -q` — all tests pass
2. Import check: `uv run python -c "from openclaw.config import get_state_path, get_snapshot_dir, get_project_root, DEFAULT_POOL_MAX_CONCURRENT, MEMORY_CONTEXT_BUDGET; print('All imports OK')"` — succeeds
3. No duplicated pool defaults: `grep -rn "_POOL_DEFAULTS\|_POOL_CONFIG_DEFAULTS" packages/orchestration/src/ skills/spawn/ --include="*.py"` — empty
4. No `Path(__file__)` for root discovery: `grep -rn "Path(__file__).*parent.*parent.*parent" packages/orchestration/src/ skills/spawn/ --include="*.py"` — empty
5. No project_config path function imports: `grep -rn "from.*project_config.*import.*get_state_path\|from.*project_config.*import.*get_snapshot_dir" packages/orchestration/src/ skills/spawn/ --include="*.py"` — empty
6. Path correctness: `uv run python -c "import os; os.environ.pop('OPENCLAW_STATE_FILE', None); from openclaw.config import get_state_path; p = get_state_path('pumplai'); assert '.openclaw/workspace/.openclaw/pumplai/workspace-state.json' in str(p), f'Bad: {p}'; print('Path OK:', p)"` — correct derivation
</verification>

<success_criteria>
- All 6 files updated with imports from config.py
- Zero duplicated constant definitions outside config.py
- Zero divergent path resolvers outside config.py
- Full test suite passes
- CONF-01 and CONF-05 requirements satisfied per success criteria in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/45-path-resolver-constants-foundation/45-02-SUMMARY.md`
</output>
