---
phase: 05-catch-me-up-experience-runtime-performance
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/lib/ollama.ts, src/lib/sync/synthesis.ts, src/app/api/sync/catch-up/route.ts]
autonomous: true
requirements: [CMEU-02, CMEU-03, PERF-03]
must_haves:
  truths:
    - "Ollama responses stream token-by-token to the client"
    - "Synthesis output follows a structured timeline with source citations"
    - "Low-confidence results trigger a structured 'low_confidence' response instead of a summary"
  artifacts:
    - path: "src/lib/sync/synthesis.ts"
      provides: "RAG synthesis engine with confidence scoring"
    - path: "src/app/api/sync/catch-up/route.ts"
      provides: "Streaming API endpoint for catch-up queries"
  key_links:
    - from: "src/lib/sync/synthesis.ts"
      to: "src/lib/ollama.ts"
      via: "streamCompletion call"
    - from: "src/app/api/sync/catch-up/route.ts"
      to: "src/lib/sync/synthesis.ts"
      via: "synthesizeTimeline call"
---

<objective>
Implement the Streaming RAG & Synthesis engine to generate verifiable project timelines from retrieved context.

Purpose: Provide interactive, streaming summaries with clear provenance and handle ambiguous/low-confidence queries gracefully.
Output: Streaming Ollama integration and a RAG synthesis module.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-catch-me-up-experience-runtime-performance/05-CONTEXT.md
@src/lib/ollama.ts
@.planning/phases/05-catch-me-up-experience-runtime-performance/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ollama Streaming Support</name>
  <files>src/lib/ollama.ts</files>
  <action>
    1. Update `src/lib/ollama.ts` to include a `streamCompletion(prompt: string, model?: string)` function.
    2. Use `fetch` (or the `ollama` library's stream option) to call the local Ollama API with `stream: true`.
    3. Return a `ReadableStream` that yields tokens or parsed NDJSON chunks.
    4. Ensure it uses the 'phi3' model by default.
  </action>
  <verify>Run a test script that calls `streamCompletion` and logs tokens to the console as they arrive.</verify>
  <done>LLM completions can be streamed token-by-token.</done>
</task>

<task type="auto">
  <name>Task 2: Synthesis Engine & Confidence Logic</name>
  <files>src/lib/sync/synthesis.ts, src/app/api/sync/catch-up/route.ts</files>
  <action>
    1. Create `src/lib/sync/synthesis.ts` implementing `synthesizeTimeline(context: VectorRecord[], intent: IntentMetadata)`.
    2. Add **Low-Confidence** logic: If the highest relevance score in `context` is < 0.4, return a special `type: 'low_confidence'` object containing the top 3 fuzzy matches for the Clarification Picker.
    3. Construct a prompt for Phi-3 that:
       - Inputs the retrieved Slack threads and Tracker issues.
       - Enforces a timeline format.
       - Mandates citations using source IDs or links.
    4. Create `src/app/api/sync/catch-up/route.ts` as a POST handler that:
       - Parses query and activeProjectId from the body.
       - Calls `parseIntent` and `searchContext`.
       - Calls `synthesizeTimeline` and returns the stream to the client.
  </action>
  <verify>Test the API endpoint with `curl -N` and verify it streams NDJSON containing tokens or a low-confidence signal.</verify>
  <done>The backend can synthesize streaming timelines with citation support and ambiguity handling.</done>
</task>

</tasks>

<verification>
Verify that queries with good context produce a streaming timeline, and queries with no good matches return a low-confidence signal.
</verification>

<success_criteria>
- Completions stream immediately (within 2s start-time).
- Output includes clear references to Slack/Tracker sources.
- Ambiguous queries result in a "low_confidence" response instead of a hallucinated summary.
</success_criteria>

<output>
After completion, create `.planning/phases/05-catch-me-up-experience-runtime-performance/05-02-SUMMARY.md`
</output>
