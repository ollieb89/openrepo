---
phase: 25-monitor-cache-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestration/monitor.py
autonomous: true
requirements:
  - PERF-04

must_haves:
  truths:
    - "tail_state() multi-project path creates JarvisState once per project before the poll loop and reuses it across iterations"
    - "Cache hit rate in structured logs shows hits (not only misses) during multi-project monitor tail polling"
    - "show_status() and show_task_detail() multi-project paths reuse JarvisState instances instead of recreating per project"
    - "If a project state file disappears mid-tail, the monitor logs a warning and skips that project without crashing"
  artifacts:
    - path: "orchestration/monitor.py"
      provides: "JarvisState instance reuse across poll cycles"
      contains: "js_instances"
  key_links:
    - from: "orchestration/monitor.py:tail_state"
      to: "orchestration/state_engine.py:JarvisState"
      via: "session-scoped dict of JarvisState instances keyed by project_id"
      pattern: "js_instances\\[proj_id\\]"
---

<objective>
Fix JarvisState cache reuse in monitor.py multi-project paths so that the mtime-based in-memory cache (Phase 21) actually provides hits between poll cycles instead of cold-starting every iteration.

Purpose: Close PERF-04 integration gap — the Phase 21 cache delivers zero benefit in monitor polling because tail_state() creates a fresh JarvisState instance inside the loop on every iteration, discarding the cached state.

Output: Updated orchestration/monitor.py with session-scoped JarvisState reuse in tail_state(), show_status(), and show_task_detail().
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-monitor-cache-fix/25-CONTEXT.md
@.planning/phases/21-state-engine-performance/21-02-SUMMARY.md
@orchestration/monitor.py
@orchestration/state_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist JarvisState creation out of tail_state poll loop</name>
  <files>orchestration/monitor.py</files>
  <action>
In tail_state() multi-project path (the `while True` loop starting around line 141):

1. Before the `while True` loop, create a dict `js_instances: Dict[str, JarvisState] = {}` keyed by project_id.

2. Before the loop, iterate `_discover_projects()` results and pre-create JarvisState instances for each project whose state file exists:
   ```python
   for proj_id, state_file in projects:
       if state_file.exists():
           js_instances[proj_id] = JarvisState(state_file)
   ```

3. Inside the loop body, replace `js = JarvisState(state_file)` (line ~150) with a lookup from the dict:
   ```python
   if proj_id not in js_instances:
       if not state_file.exists():
           continue
       js_instances[proj_id] = JarvisState(state_file)
   js = js_instances[proj_id]
   ```
   This handles the case where a new project appears after tail starts (lazy creation), and handles the case where a state file did not exist initially but was created later.

4. If `js.read_state()` raises an exception (state file disappears mid-tail per CONTEXT.md decision), catch it, log a warning via `print()` to stderr (consistent with existing error handling pattern in this function), remove the project from `js_instances` (so it can be re-created if the file comes back), and continue to the next project.

5. Add a structured log entry (at DEBUG level) after each poll cycle iteration summarizing cache stats. Import `get_logger` from orchestration.logging at the top of the file (alongside existing imports). Create a module-level `logger = get_logger('monitor')`. After the inner `for proj_id, state_file in projects` loop completes (but before `time.sleep`), log a single entry:
   ```python
   logger.debug("poll cycle complete", extra={"projects_polled": len(projects), "instances_cached": len(js_instances)})
   ```
   Per CONTEXT.md: cache stats go through structured logging only, not visible in tail output.

Instance dict teardown is implicit when tail_state() exits (per CONTEXT.md decision).
  </action>
  <verify>
    <automated>python3 -c "import ast; tree = ast.parse(open('orchestration/monitor.py').read()); funcs = [n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name == 'tail_state']; src = open('orchestration/monitor.py').read(); assert 'js_instances' in src, 'js_instances dict not found'; assert src.count('JarvisState(state_file)') == 1 or 'js_instances[proj_id] = JarvisState(state_file)' in src, 'JarvisState still created in loop body without caching'; print('PASS: JarvisState instance reuse verified')"</automated>
    <manual>Run `python3 orchestration/monitor.py tail --interval 2` with at least one project that has a state file. Check stderr for structured log entries showing poll_cycle_complete with instances_cached > 0.</manual>
  </verify>
  <done>tail_state() creates JarvisState instances once per project outside the poll loop and reuses them across iterations. New/recovered projects are lazily added. Disappeared state files produce a warning and skip without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Add JarvisState reuse to show_status and show_task_detail</name>
  <files>orchestration/monitor.py</files>
  <action>
Per CONTEXT.md: show_status/show_task reuse is explicitly lower priority than tail_state(). These are one-shot calls, so the benefit is smaller. Still worth fixing for consistency and for cases where an operator runs `status` repeatedly.

1. In `show_status()` multi-project path (around line 297-308): Create a local dict and reuse instances within the single call. Currently each project creates `js = JarvisState(state_file)` inside the for loop — this is one-shot so there is no cross-call reuse, but the pattern should be consistent. The main fix here is minor: no change needed since it is one-shot. However, for `show_pool_utilization()` (line ~576-591) the same pattern exists. Apply the same consistency: create `js = JarvisState(state_file)` once per project (already the case for one-shot, so this is already correct).

2. In `show_task_detail()` multi-project path (around line 458-466): Same analysis — one-shot call, JarvisState created once per project iteration. Already correct for one-shot. No code change needed.

3. Since show_status and show_task_detail are one-shot (not polling loops), the CONTEXT.md explicitly says "skip if awkward." These are already correct — each creates JarvisState once per project during a single call, which is fine. The real fix was Task 1 (the polling loop).

**Instead of changing show_status/show_task_detail, use this task to add the structured logger import and module-level logger** (if not already done in Task 1's execution), and add a brief docstring note to the multi-project JarvisState creation sites in show_status, show_task_detail, and show_pool_utilization explaining that these are one-shot calls so instance reuse across calls is not needed (documentation for future maintainers).

Add inline comments at lines where `js = JarvisState(state_file)` appears in show_status(), show_task_detail(), and show_pool_utilization():
```python
# One-shot call — JarvisState created per project per invocation (no cross-call cache needed)
js = JarvisState(state_file)
```
  </action>
  <verify>
    <automated>python3 -c "src = open('orchestration/monitor.py').read(); assert 'get_logger' in src, 'get_logger not imported'; assert \"logger = get_logger('monitor')\" in src or \"logger = get_logger(\\\"monitor\\\")\" in src, 'module-level logger not created'; assert 'One-shot call' in src, 'Documentation comments not added'; print('PASS: Logger and documentation verified')"</automated>
  </verify>
  <done>Module-level structured logger is wired. One-shot multi-project paths are documented as not needing cross-call instance reuse. All JarvisState creation sites in monitor.py are intentional and documented.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import orchestration.monitor; print('import OK')"` — module imports without error
2. Grep for `JarvisState(` in monitor.py — should appear in: (a) _tail_single_file (legacy, once before loop — already correct), (b) tail_state (now in js_instances dict creation, NOT inside while loop body directly), (c) show_status (one-shot, documented), (d) show_task_detail (one-shot, documented), (e) show_pool_utilization (one-shot, documented), (f) _show_status_single_file (legacy), (g) _show_task_detail_single_file (legacy)
3. No new external dependencies introduced
</verification>

<success_criteria>
- JarvisState instances in tail_state() are session-scoped (created once per project, reused across poll cycles)
- Structured logger emits poll cycle summaries at DEBUG level
- One-shot paths documented as intentionally not needing cross-call reuse
- Module imports cleanly, no regressions in CLI argument parsing
</success_criteria>

<output>
After completion, create `.planning/phases/25-monitor-cache-fix/25-01-SUMMARY.md`
</output>
