---
phase: 15-dashboard-project-switcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/occc/src/lib/projects.ts
  - workspace/occc/src/app/api/projects/route.ts
  - workspace/occc/src/app/api/swarm/route.ts
  - workspace/occc/src/app/api/swarm/stream/route.ts
autonomous: true
requirements:
  - DSH-05
  - DSH-06
  - DSH-07

must_haves:
  truths:
    - "GET /api/projects returns a JSON array of all projects discovered from projects/*/project.json, excluding _templates"
    - "GET /api/swarm?project=pumplai returns only PumplAI state data"
    - "GET /api/swarm?project=nonexistent returns 404"
    - "GET /api/swarm (no param) defaults to first available project instead of crashing"
    - "GET /api/swarm/stream?project=pumplai streams only PumplAI state events"
    - "Module-level cache in route.ts is per-project (Map keyed by projectId), not a singleton"
  artifacts:
    - path: "workspace/occc/src/lib/projects.ts"
      provides: "Shared Project type, resolveStateFilePath, resolveProjectsDir, getDefaultProject helpers"
      min_lines: 20
    - path: "workspace/occc/src/app/api/projects/route.ts"
      provides: "GET /api/projects endpoint scanning projects directory"
      min_lines: 30
    - path: "workspace/occc/src/app/api/swarm/route.ts"
      provides: "Project-scoped swarm state with per-project cache Map"
      contains: "searchParams.get"
    - path: "workspace/occc/src/app/api/swarm/stream/route.ts"
      provides: "Project-scoped SSE stream"
      contains: "searchParams.get"
  key_links:
    - from: "workspace/occc/src/app/api/projects/route.ts"
      to: "workspace/occc/src/lib/projects.ts"
      via: "import resolveProjectsDir, resolveStateFilePath"
      pattern: "import.*from.*lib/projects"
    - from: "workspace/occc/src/app/api/swarm/route.ts"
      to: "workspace/occc/src/lib/projects.ts"
      via: "import resolveStateFilePath"
      pattern: "resolveStateFilePath"
    - from: "workspace/occc/src/app/api/swarm/stream/route.ts"
      to: "workspace/occc/src/app/api/swarm/route.ts"
      via: "import getSwarmState (now accepts projectId)"
      pattern: "getSwarmState"
---

<objective>
Thread project identity through all API routes so the dashboard backend returns project-scoped data.

Purpose: The dashboard currently reads a single hardcoded state file. This plan makes all API routes accept a `?project=<id>` query parameter, returning only that project's state. A new `/api/projects` discovery endpoint enables the frontend to know which projects exist.

Output: Three API routes (one new, two modified) and a shared path-resolution library.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dashboard-project-switcher/15-RESEARCH.md
@workspace/occc/src/app/api/swarm/route.ts
@workspace/occc/src/app/api/swarm/stream/route.ts
@workspace/occc/src/lib/jarvis.ts
@workspace/occc/src/lib/metrics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared project helpers and /api/projects discovery endpoint</name>
  <files>
    workspace/occc/src/lib/projects.ts
    workspace/occc/src/app/api/projects/route.ts
  </files>
  <action>
Create `src/lib/projects.ts` with:
- `OPENCLAW_ROOT` constant: `process.env.OPENCLAW_ROOT || '/home/ollie/.openclaw'`
- `ProjectInfo` interface: `{ id: string; name: string; status: 'active' | 'idle' | 'error' }`
- `resolveStateFilePath(projectId: string): string` — returns `path.join(OPENCLAW_ROOT, 'workspace', '.openclaw', projectId, 'workspace-state.json')`. This mirrors Python's `get_state_path()` convention from Phase 11.
- `resolveProjectsDir(): string` — returns `path.join(OPENCLAW_ROOT, 'projects')`
- `getDefaultProject(): Promise<string | null>` — scans `projects/` dir, skips entries starting with `_`, returns first alphabetically-sorted project ID that has a `project.json`, or null if none found.

Create `src/app/api/projects/route.ts` with a GET handler that:
1. Reads `resolveProjectsDir()` with `fs.readdir({ withFileTypes: true })`
2. Skips directories starting with `_` (filters out `_templates`)
3. For each remaining directory, reads `project.json`, parses `id` and `name`
4. Derives status by checking the project's state file: if state file exists and has `in_progress` tasks -> `'active'`; has `failed` tasks and none in_progress -> `'error'`; otherwise -> `'idle'` (including when state file is missing)
5. Sorts projects alphabetically by name
6. Returns `{ projects: ProjectInfo[] }`
7. On filesystem errors, returns `{ error: '...', projects: [] }` with status 500

Use `fs/promises` for all file operations. Import `resolveProjectsDir`, `resolveStateFilePath`, `ProjectInfo` from `@/lib/projects`.
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit src/lib/projects.ts src/app/api/projects/route.ts 2>&1 | head -20</automated>
    <manual>Start dev server, visit http://localhost:6987/api/projects — should return JSON with pumplai and geriai entries</manual>
  </verify>
  <done>
    /api/projects endpoint returns discovered projects with id, name, and status fields; _templates directory is excluded; missing state files result in 'idle' status not errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add project-scoping to /api/swarm and /api/swarm/stream routes</name>
  <files>
    workspace/occc/src/app/api/swarm/route.ts
    workspace/occc/src/app/api/swarm/stream/route.ts
  </files>
  <action>
**Modify `src/app/api/swarm/route.ts`:**

1. Import `resolveStateFilePath` and `getDefaultProject` from `@/lib/projects`.
2. Replace the module-level `let cachedState: CachedState | null = null` singleton with `const stateCache = new Map&lt;string, CachedState&gt;()` — this prevents cross-project cache bleed (research pitfall #1).
3. Change `getSwarmState()` signature to `getSwarmState(projectId: string)`. Update all internal references:
   - Replace `readStateFile()` to accept `projectId` and use `resolveStateFilePath(projectId)` instead of `DEFAULT_STATE_FILE`.
   - Cache lookups use `stateCache.get(projectId)` / `stateCache.set(projectId, ...)`.
4. Change the `GET` handler to accept `request: NextRequest` (import from `next/server`):
   - Parse `projectId` from `new URL(request.url).searchParams.get('project')`.
   - If no `project` param, call `getDefaultProject()` for fallback.
   - If still null (no projects at all), return `{ error: 'No projects found' }` with status 404.
   - Validate the project exists by checking if `resolveStateFilePath(projectId)` parent directory exists OR if `projects/<id>/project.json` exists. If neither, return 404 with `{ error: 'Project not found', project: projectId }`.
   - If state file does not exist but project.json does, return an empty-but-valid response: `{ agents: [], metrics: {zeroed}, state: {empty JarvisState}, lastUpdated: new Date().toISOString() }`.
   - Call `getSwarmState(projectId)` for projects with state files.
5. Remove `DEFAULT_STATE_FILE` constant. Keep `DEFAULT_OPENCLAW_CONFIG` since `openclaw.json` is global not per-project.
6. The `getSwarmState` function must remain exported — `stream/route.ts` imports it.

**Modify `src/app/api/swarm/stream/route.ts`:**

1. Import `resolveStateFilePath` and `getDefaultProject` from `@/lib/projects`.
2. Parse `projectId` from `new URL(request.url).searchParams.get('project')` at the top of the GET handler. Fallback to `await getDefaultProject()`.
3. If no project resolved, send an error SSE event and close.
4. Replace `DEFAULT_STATE_FILE` usage with `resolveStateFilePath(projectId)` for the `stateFilePath` variable.
5. Pass `projectId` to `getSwarmState(projectId)` calls (initial state + poll updates).
6. Remove the `DEFAULT_STATE_FILE` constant from this file.
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>curl http://localhost:6987/api/swarm?project=pumplai — should return state; curl with project=nonexistent — should return 404</manual>
  </verify>
  <done>
    /api/swarm?project=X returns project-scoped state; /api/swarm/stream?project=X streams project-scoped events; unknown project returns 404; missing param defaults to first available project; module-level cache is a per-project Map.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors: `cd workspace/occc && npx tsc --noEmit`
2. `/api/projects` returns JSON array with at least pumplai entry (geriai may also appear)
3. `/api/swarm?project=pumplai` returns swarm state (or empty state if no state file exists yet)
4. `/api/swarm?project=doesnotexist` returns HTTP 404
5. `/api/swarm` (no param) does not crash — defaults to first project
6. `getSwarmState` is still exported from route.ts and callable with a projectId argument
</verification>

<success_criteria>
All three API routes compile and serve project-scoped data. The per-project cache Map prevents cross-project bleed. The shared `lib/projects.ts` provides consistent path resolution matching Python's `get_state_path()` convention. No new npm dependencies added.
</success_criteria>

<output>
After completion, create `.planning/phases/15-dashboard-project-switcher/15-01-SUMMARY.md`
</output>
