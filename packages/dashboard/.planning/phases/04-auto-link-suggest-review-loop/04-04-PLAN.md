---
phase: 04-auto-link-suggest-review-loop
plan: 04
type: execute
wave: 3
depends_on: ["04-03-PLAN.md"]
files_modified: [src/app/api/links/suggestions/route.ts, src/components/privacy/LinkReviewPanel.tsx, src/components/layout/SuggestionToast.tsx]
autonomous: false
requirements: [LINK-02, LINK-03]
---

<objective>
Implement the Link UI to allow users to review, accept, or reject suggested links between conversations and issues.

Purpose: Provide a human-in-the-loop interface for persisting AI-suggested relationships.
Output: Real-time suggestion toasts and a centralized review dashboard.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/sync/vector-store.ts
@src/lib/sync/suggestions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Endpoints for Suggestion Management</name>
  <files>src/app/api/links/suggestions/route.ts, src/app/api/links/suggestions/[id]/action/route.ts</files>
  <action>
    1. Create `GET /api/links/suggestions`: Returns all 'pending' link suggestions, joined with decision and issue content for display.
    2. Create `POST /api/links/suggestions/[id]/action`: Accepts an `action` ('accept' | 'reject'). 
       - If 'accept': Update status to 'accepted'.
       - If 'reject': Update status to 'rejected'.
  </action>
  <verify>
    Verify API responses with mock data using `curl` or a test script.
  </verify>
  <done>
    Backend support for reviewing suggestions is complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI Components for Review</name>
  <files>src/components/sync/SuggestionToast.tsx, src/components/sync/LinkReviewPanel.tsx</files>
  <action>
    1. Build `SuggestionToast`: A transient alert that appears when a high-confidence suggestion is generated (or simply periodically polls for pending counts).
    2. Build `LinkReviewPanel`: A list or grid view showing the Decision Outcome vs. the Issue Title with "Accept" and "Ignore" buttons.
  </action>
  <verify>
    Navigate to the review surface and verify that pending suggestions are displayed and the action buttons trigger API calls.
  </verify>
  <done>
    Users can visually review and act on link suggestions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Functional Verification</name>
  <files>none</files>
  <action>Manually verify the Link Suggestion and Review flow.</action>
  <verify>
    1. Trigger a decision summarization that should result in a suggestion.
    2. Verify the suggestion appears in the UI.
    3. Click "Accept" and verify the status updates in the database.
  </verify>
  <done>
    User confirms the end-to-end linking workflow works as intended.
  </done>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Link suggestions are visible in the UI.
- Accepting/Rejecting suggestions persists the state in `nexus-sync.db`.
- UI updates correctly after actions.
</verification>

<success_criteria>
- Users have control over which chat-to-issue links are established.
- The system correctly surfaces relevant matches for human review.
- Bidirectional links are established and persisted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auto-link-suggest-review-loop/04-04-SUMMARY.md`
</output>
