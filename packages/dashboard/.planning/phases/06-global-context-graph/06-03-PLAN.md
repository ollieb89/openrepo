---
phase: 06-global-context-graph
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/sync/vector-store.ts]
autonomous: true
requirements: [REAS-05]
must_haves:
  truths:
    - "Catch Me Up results include items linked via the context graph to top semantic matches"
    - "Linked records receive a +0.3 score boost during retrieval"
    - "Performance of searchContext remains within sub-2s threshold"
  artifacts:
    - path: "src/lib/sync/vector-store.ts"
      contains: "boostByGraph" # or similar logic integrated into searchContext
  key_links:
    - from: "src/lib/sync/vector-store.ts"
      to: "src/lib/sync/graph.ts"
      via: "neighbor expansion during searchContext"
---

<objective>
Enhance the "Catch Me Up" retrieval engine to consider graph relationships, boosting the relevance of records that are explicitly linked to top semantic matches.

Purpose: Surface relevant context that might be semantically distant but structurally close via explicit links.
Output: Graph-aware ranking logic in searchContext.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-context-graph/06-RESEARCH.md
@.planning/phases/06-global-context-graph/06-01-SUMMARY.md
@src/lib/sync/vector-store.ts
@src/lib/sync/graph.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Graph Expansion in searchContext</name>
  <files>src/lib/sync/vector-store.ts</files>
  <action>
    Update `searchContext` in `src/lib/sync/vector-store.ts` to perform graph expansion.
    Workflow:
    1. Perform initial semantic/temporal search (existing logic).
    2. Identify top K (e.g. 5) seed records from the initial results.
    3. Query the `edges` table for 1-hop neighbors of these seed records.
    4. Fetch any neighbor records that aren't already in the initial result set from `vector_cache`.
    5. Merge these new records into the scoring pool.
  </action>
  <verify>
    Verify that neighbors are added to the list of records before scoring.
  </verify>
  <done>
    Search retrieval pool includes graph neighbors of top matches.
  </done>
</task>

<task type="auto">
  <name>Apply Graph-Aware Relevance Boost</name>
  <files>src/lib/sync/vector-store.ts</files>
  <action>
    Modify the scoring loop in `searchContext`:
    - Create a map of boosts derived from graph connectivity.
    - If a record is a neighbor of a seed record, apply a +0.3 boost to its final score.
    - Ensure the final score is capped at 1.0.
    - Ensure records from different projects still receive the boost if they are linked (fulfilling REAS-05).
  </action>
  <verify>
    Seed a test case: Query Q matches Record A (semantic). Record A is linked to Record B (graph). Record B does not match Q semantically.
    Run `searchContext` for Q and verify Record B appears in results with boosted score.
  </verify>
  <done>
    Linked records receive a relevance boost in Catch Me Up results.
  </done>
</task>

</tasks>

<verification>
Perform a sample Catch Me Up query and verify that linked records from disparate projects are surfaced and boosted.
</verification>

<success_criteria>
- Multi-Project Boost (+0.3) is applied to graph-linked items.
- Retrieval includes neighbors of top semantic hits.
- Search latency remains acceptable (sub-2s).
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-context-graph/06-03-SUMMARY.md`
</output>
