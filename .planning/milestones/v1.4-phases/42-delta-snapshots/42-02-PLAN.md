---
phase: 42-delta-snapshots
plan: "02"
type: execute
wave: 2
depends_on:
  - "42-01"
files_modified:
  - orchestration/state_engine.py
  - skills/spawn_specialist/spawn.py
  - docker/memory/memory_service/models.py
  - docker/memory/memory_service/routers/retrieve.py
autonomous: true
requirements:
  - PERF-05
  - PERF-06
  - PERF-07

must_haves:
  truths:
    - "workspace-state.json contains metadata.memory_cursors[project_id] as an ISO timestamp after a spawn completes"
    - "_retrieve_memories_sync returns a (list, bool) tuple — bool=True on success, bool=False on network error"
    - "When created_after is passed to spawn retrieval, the payload sent to /retrieve contains the created_after key"
    - "The memU /retrieve endpoint accepts created_after in the request body and filters results to items newer than that timestamp"
    - "A corrupt or malformed cursor in state.json causes get_memory_cursor to return None, triggering a full fetch"
  artifacts:
    - path: "orchestration/state_engine.py"
      provides: "get_memory_cursor() and update_memory_cursor() methods on JarvisState"
      contains: "get_memory_cursor"
    - path: "skills/spawn_specialist/spawn.py"
      provides: "Cursor-aware _retrieve_memories_sync returning (list, bool) tuple"
      contains: "created_after"
    - path: "docker/memory/memory_service/models.py"
      provides: "Optional created_after field on RetrieveRequest"
      contains: "created_after"
    - path: "docker/memory/memory_service/routers/retrieve.py"
      provides: "_filter_after helper and cursor filtering in /retrieve endpoint"
      contains: "_filter_after"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "orchestration/state_engine.py"
      via: "JarvisState.get_memory_cursor() before fetch, update_memory_cursor() after ok=True"
      pattern: "get_memory_cursor|update_memory_cursor"
    - from: "skills/spawn_specialist/spawn.py"
      to: "docker/memory/memory_service/routers/retrieve.py"
      via: "created_after in POST /retrieve payload when cursor is not None"
      pattern: "created_after.*payload|payload.*created_after"
    - from: "docker/memory/memory_service/routers/retrieve.py"
      to: "_filter_after"
      via: "called after memu.retrieve() when payload.created_after is set"
      pattern: "_filter_after\\(result"
---

<objective>
Implement cursor-based memory retrieval: JarvisState cursor helpers (PERF-05), cursor-aware spawn retrieval with (list, bool) return type (PERF-06), and the memU /retrieve created_after filter (PERF-07).

Purpose: Subsequent L3 spawns fetch only memories created since the last retrieval, keeping SOUL context focused on recent learnings and reducing memU query volume at scale.
Output: state_engine.py with cursor methods, spawn.py with cursor-aware retrieval, models.py + retrieve.py with created_after filter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-delta-snapshots/42-RESEARCH.md
@.planning/phases/42-delta-snapshots/42-CONTEXT.md
@.planning/phases/42-delta-snapshots/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: JarvisState cursor helpers (PERF-05) and cursor-aware spawn retrieval (PERF-06)</name>
  <files>orchestration/state_engine.py, skills/spawn_specialist/spawn.py</files>
  <action>
**Part A: state_engine.py — add two methods to JarvisState**

Read the full `orchestration/state_engine.py` to understand the existing `_write_state_locked`, `_read_state_locked`, `_acquire_lock`, `_release_lock`, and `read_state` patterns before adding methods.

Add `get_memory_cursor(self, project_id: str) -> Optional[str]` and `update_memory_cursor(self, project_id: str, iso_timestamp: str) -> None` to the JarvisState class using the exact patterns from RESEARCH.md Pattern 1:

- `get_memory_cursor`: uses `self.read_state()` (shared read), navigates to `state["metadata"]["memory_cursors"][project_id]`, validates with `datetime.fromisoformat(value.rstrip("Z"))`, returns None on any exception (including corrupt/missing value). Logs warning via the module logger on exception.
- `update_memory_cursor`: uses the LOCK_EX read-modify-write pattern (same as existing write methods — study the class before copying). Creates `state["metadata"]["memory_cursors"]` dict if absent. Swallows all exceptions with a warning log — cursor write failure must never abort a spawn. Does NOT raise.

Import `Optional` from typing if not already imported. Import `datetime` inside the method body (not at module level) to avoid circular import risk — the project's `sys.path` guard for `logging` shadowing applies here too.

**Part B: spawn.py — change _retrieve_memories_sync return type and add cursor wiring**

Read `skills/spawn_specialist/spawn.py` fully before modifying.

1. Change `_retrieve_memories_sync` signature to:
   ```python
   def _retrieve_memories_sync(
       base_url: str,
       project_id: str,
       query: str,
       created_after: Optional[str] = None,
   ) -> tuple[list, bool]:
   ```
   - In the success path: include `created_after` in the payload when not None (`payload["created_after"] = created_after`).
   - Return `(items, True)` on success (where items is the existing sliced list).
   - Return `([], False)` in the except block (not `[]` alone). The bool=False signals the caller not to advance the cursor.

2. In `spawn_l3_specialist()`, find the existing memory retrieval block. Replace it with the cursor-aware pattern from RESEARCH.md Pattern 2:
   - Instantiate `JarvisState` using the existing `get_state_path(project_id)` call (already present in the function for other state reads — do not create a duplicate).
   - Call `jarvis.get_memory_cursor(project_id)` to get the cursor (None if first spawn or corrupt).
   - Call `memories, fetch_ok = _retrieve_memories_sync(memu_url, project_id, query, created_after=cursor)`.
   - Only if `fetch_ok is True`: call `jarvis.update_memory_cursor(project_id, datetime.now(timezone.utc).isoformat())`.
   - The cursor update must happen BEFORE `_format_memory_context(memories)` is called (to reduce window for race).

**Pitfall 1 (from RESEARCH.md):** Do not update the cursor if `fetch_ok` is False — a network failure must not advance the cursor.
**Pitfall 3 (from RESEARCH.md):** Use the same `jarvis` instance for both get and update in the same spawn call. Do not create two JarvisState objects for the same state file.

After modifying spawn.py, verify all other callers of `_retrieve_memories_sync` are updated if they unpack the return value (there should be only one call site — confirm with a search before saving).
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 -m pytest tests/test_delta_snapshots.py::test_get_memory_cursor_absent tests/test_delta_snapshots.py::test_update_memory_cursor_writes tests/test_delta_snapshots.py::test_get_memory_cursor_corrupt tests/test_delta_snapshots.py::test_retrieve_sends_created_after tests/test_delta_snapshots.py::test_cursor_not_updated_on_fetch_failure tests/test_delta_snapshots.py::test_cursor_updated_after_success -x -q</automated>
    <manual>Confirm 6 tests pass (PERF-05 and PERF-06 green). Remaining 7 tests (PERF-07, PERF-08) still fail — that is expected.</manual>
    <sampling_rate>run immediately after task commits</sampling_rate>
  </verify>
  <done>6 tests pass: 3 cursor helper tests (PERF-05) + 3 spawn retrieval tests (PERF-06). JarvisState has get_memory_cursor and update_memory_cursor. spawn.py _retrieve_memories_sync returns (list, bool) and passes created_after in payload.</done>
</task>

<task type="auto">
  <name>Task 2: memU retrieve router — created_after filter (PERF-07)</name>
  <files>docker/memory/memory_service/models.py, docker/memory/memory_service/routers/retrieve.py</files>
  <action>
Read both files fully before modifying.

**models.py:**
Add `created_after: Optional[str] = None` to `RetrieveRequest`. Follow the existing field ordering and type annotation style in the file. `Optional` is likely already imported via pydantic or typing — check first.

**routers/retrieve.py:**
1. Add the `_filter_after(items: list, created_after: str) -> list` module-level helper function using the exact implementation from RESEARCH.md Pattern 3. The function must:
   - Parse `created_after` with `datetime.fromisoformat(created_after.rstrip("Z"))` inside a try/except — on ValueError/AttributeError, log a warning and return `items` unchanged.
   - For each item: handle `created_at` as None (pass through), as `int`/`float` (use `datetime.utcfromtimestamp(ts)`), as string (use `datetime.fromisoformat(str(ts).rstrip("Z"))`). On any parse error per item, pass through conservatively.
   - Import `datetime` from the stdlib at the top of the function (inside the function body or at module level — check existing imports in the file).

2. In the `retrieve` endpoint handler, after the existing `result = await memu.retrieve(...)` call, add the cursor filter block:
   ```python
   if payload.created_after:
       if isinstance(result, list):
           result = _filter_after(result, payload.created_after)
       elif isinstance(result, dict) and "items" in result:
           result["items"] = _filter_after(result["items"], payload.created_after)
   ```
   This must come before the `return result` line and after the existing memu.retrieve() call. Do not modify the error handling paths.

**Pitfall 2 (from RESEARCH.md):** The `created_at` field shape in memU inmemory backend is unknown at development time. The conservative pass-through (return item on any parse error) ensures correctness in all cases. Add a debug log line inside `_filter_after` when `created_at` is None: `logger.debug("Memory item missing created_at — passing through in cursor filter")`. This surfaces the field-absent case during integration testing without failing silently.
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 -m pytest tests/test_delta_snapshots.py::test_filter_after_timestamp tests/test_delta_snapshots.py::test_filter_after_missing_created_at tests/test_delta_snapshots.py::test_filter_after_unix_float tests/test_delta_snapshots.py::test_filter_after_bad_cursor_passthrough -x -q</automated>
    <manual>Confirm 4 PERF-07 tests pass. Also verify PERF-05 and PERF-06 tests still pass (no regression): python3 -m pytest tests/test_delta_snapshots.py -x -q — expect 10 of 13 passing, 3 PERF-08 still failing.</manual>
    <sampling_rate>run immediately after task commits</sampling_rate>
  </verify>
  <done>4 _filter_after tests pass. models.py has created_after Optional[str] field. retrieve.py has _filter_after helper and filter block in the retrieve endpoint. Total: 10/13 tests passing (PERF-05 + PERF-06 + PERF-07 green).</done>
</task>

</tasks>

<verification>
cd ~/.openclaw && python3 -m pytest tests/test_delta_snapshots.py -x -q 2>&1 | tail -10

Expected: 10 passed, 3 failed (the 3 PERF-08 snapshot pruning tests — covered in Plan 03).

Also verify spawn.py is syntactically correct:
python3 -c "import ast; ast.parse(open('skills/spawn_specialist/spawn.py').read()); print('OK')"
</verification>

<success_criteria>
- 10 of 13 tests in test_delta_snapshots.py pass (PERF-05, PERF-06, PERF-07 fully green)
- JarvisState.get_memory_cursor() returns None when cursor absent or corrupt
- JarvisState.update_memory_cursor() writes ISO timestamp under metadata.memory_cursors[project_id]
- _retrieve_memories_sync returns (list, bool) tuple; bool=False on network failure
- /retrieve endpoint applies created_after filter when payload field is set
- No regressions in existing test suite: python3 -m pytest tests/ -x -q passes
</success_criteria>

<output>
After completion, create `.planning/phases/42-delta-snapshots/42-02-SUMMARY.md`
</output>
