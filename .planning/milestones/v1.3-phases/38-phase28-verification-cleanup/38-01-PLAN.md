---
phase: 38-phase28-verification-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orchestration/memory_client.py
  - docker/l3-specialist/entrypoint.sh
  - .planning/phases/28-l3-auto-memorization/28-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MEM-01, MEM-03]

must_haves:
  truths:
    - "Phase 28 VERIFICATION.md exists with 5/5 observable truths verified against test_pool_memorization.py evidence"
    - "MEMU_SERVICE_URL constant is removed from memory_client.py — no production code references it"
    - "Stale placeholder comment is removed from entrypoint.sh — the if block stands alone"
    - "MEM-01 and MEM-03 are marked Complete in REQUIREMENTS.md traceability table"
  artifacts:
    - path: ".planning/phases/28-l3-auto-memorization/28-VERIFICATION.md"
      provides: "Formal verification report for Phase 28 L3 Auto-Memorization"
      contains: "score: 5/5"
    - path: "orchestration/memory_client.py"
      provides: "Memory client without dead MEMU_SERVICE_URL constant"
    - path: "docker/l3-specialist/entrypoint.sh"
      provides: "Entrypoint without stale placeholder comment"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability with MEM-01 and MEM-03 Complete"
  key_links:
    - from: ".planning/phases/28-l3-auto-memorization/28-VERIFICATION.md"
      to: "tests/test_pool_memorization.py"
      via: "Test evidence cross-reference"
      pattern: "test_pool_memorization"
    - from: ".planning/phases/28-l3-auto-memorization/28-VERIFICATION.md"
      to: ".planning/phases/33-integration-gap-closure/33-VERIFICATION.md"
      via: "Cross-reference for MEM-01/MEM-03 prior declaration"
      pattern: "Phase 33"
---

<objective>
Remove two confirmed dead code items (MEMU_SERVICE_URL constant in memory_client.py, stale placeholder comment in entrypoint.sh) and write the missing Phase 28 VERIFICATION.md using existing test suite evidence.

Purpose: Phase 28 is the only v1.3 milestone phase without formal verification. This closes that gap and removes dead code flagged during the v1.3 audit. Completing this unblocks v1.3 milestone sign-off.

Output: 28-VERIFICATION.md (5/5 truths), cleaned memory_client.py, cleaned entrypoint.sh, updated REQUIREMENTS.md traceability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-phase28-verification-cleanup/38-RESEARCH.md
@.planning/phases/28-l3-auto-memorization/28-01-PLAN.md
@.planning/phases/28-l3-auto-memorization/28-02-PLAN.md
@.planning/phases/28-l3-auto-memorization/28-01-SUMMARY.md
@.planning/phases/28-l3-auto-memorization/28-02-SUMMARY.md
@.planning/phases/33-integration-gap-closure/33-VERIFICATION.md
@orchestration/memory_client.py
@docker/l3-specialist/entrypoint.sh
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code from memory_client.py and entrypoint.sh</name>
  <files>orchestration/memory_client.py, docker/l3-specialist/entrypoint.sh</files>
  <action>
In `orchestration/memory_client.py`:

1. Remove the dead constant definition at line 28-29:
```python
#: Default memU service URL — override via environment variable.
MEMU_SERVICE_URL = os.environ.get("MEMU_SERVICE_URL", "http://memu-server:18791")
```

2. Update the module docstring example at line 10 — replace `MEMU_SERVICE_URL` with a literal URL string:
```python
# Before:
    async with MemoryClient(MEMU_SERVICE_URL, "pumplai", AgentType.L3_CODE) as client:
# After:
    async with MemoryClient("http://localhost:18791", "pumplai", AgentType.L3_CODE) as client:
```

3. Remove `import os` at line 15 if `os` has no other usages in the file after the constant removal. Search for `os.` in the file — if zero hits remain, remove the import.

In `docker/l3-specialist/entrypoint.sh`:

4. Remove the two stale comment lines (75-76):
```bash
# Placeholder: actual CLI invocation will depend on runtime
# This is the hook point where Claude Code / Codex / Gemini CLI runs
```
Leave the surrounding lines intact: line 74 (`update_state "in_progress" ...`) connects directly to the `if command -v` block.

5. Run `bash -n ~/.openclaw/docker/l3-specialist/entrypoint.sh` to confirm syntax remains valid after the edit.
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 -c "src = open('orchestration/memory_client.py').read(); assert 'MEMU_SERVICE_URL' not in src, 'MEMU_SERVICE_URL still present'; print('PASS: memory_client.py clean')" && bash -n docker/l3-specialist/entrypoint.sh && echo "PASS: entrypoint.sh syntax OK" && python3 -m pytest tests/test_pool_memorization.py -v --tb=short 2>&1 | tail -10</automated>
    <manual>Confirm no grep hits for MEMU_SERVICE_URL across the codebase</manual>
  </verify>
  <done>MEMU_SERVICE_URL constant and its docstring reference are gone from memory_client.py; unused os import removed if applicable; stale placeholder comment removed from entrypoint.sh; bash syntax check passes; existing test suite still passes</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 28 VERIFICATION.md and update REQUIREMENTS.md traceability</name>
  <files>.planning/phases/28-l3-auto-memorization/28-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
1. Run the existing test suite to capture fresh evidence:
```bash
cd ~/.openclaw && python3 -m pytest tests/test_pool_memorization.py -v
```
Record the output (5 tests, pass/fail status, duration).

2. Create `.planning/phases/28-l3-auto-memorization/28-VERIFICATION.md` matching the exact format used by Phase 33's VERIFICATION.md. Use this YAML frontmatter:
```yaml
---
phase: 28-l3-auto-memorization
verified: 2026-02-24T00:00:00Z
status: passed
score: 5/5 must-haves verified
gaps: []
human_verification: []
---
```

Body sections (match Phase 33 format exactly):

**Goal Achievement** section with three subsections:

Observable Truths table (5 rows, one per must_have from 28-02-PLAN.md):
| # | Truth | Status | Evidence |
1. After a successful L3 task completes, a memorize call is fired via asyncio.create_task (non-blocking) | VERIFIED | `test_memorize_called_on_success` passes; `asyncio.create_task(_memorize_snapshot_fire_and_forget(...))` in pool.py success branch
2. The pool slot is released before the memorize pipeline finishes (fire-and-forget) | VERIFIED | Architectural: `asyncio.create_task` schedules coroutine; `_attempt_task` returns before coroutine completes; semaphore released in `spawn_and_monitor` finally block
3. When memU service is unreachable, the L3 task still completes successfully | VERIFIED | `test_memorize_exception_is_non_blocking` passes; belt-and-suspenders `except Exception` in `_memorize_snapshot_fire_and_forget()`
4. Only successful task completions trigger memorization | VERIFIED | Code review: `asyncio.create_task` call is inside `if result["status"] == "completed":` branch only; no calls in failed/timeout/error branches
5. Snapshot content is read from the .diff file on disk after container exit | VERIFIED | `test_snapshot_content_includes_header` passes; `get_snapshot_dir(project_id) / f"{task_id}.diff"` with `exists()` guard

Required Artifacts table (2 rows):
- `skills/spawn_specialist/pool.py` — contains `_memorize_snapshot_fire_and_forget`
- `tests/test_pool_memorization.py` — 5 tests, all passing

Key Link Verification table (2 rows):
- pool.py → memory_client.py via MemoryClient import and memorize() call
- pool.py → project_config.py via get_memu_config() and get_snapshot_dir() imports

**Requirements Coverage** table:
| Requirement | Source Plan | Description | Status | Evidence |
- MEM-01: 28-02-PLAN.md | L3 task outcomes auto-memorized via fire-and-forget | SATISFIED | `_memorize_snapshot_fire_and_forget()` in pool.py; `test_memorize_called_on_success` passes; Phase 33 VERIFICATION.md confirmed pre-existing implementation
- MEM-03: 28-02-PLAN.md | Memorization failure is non-blocking | SATISFIED | `test_memorize_exception_is_non_blocking` passes; MemoryClient.memorize() catches all exceptions; outer except in fire-and-forget method; Phase 33 VERIFICATION.md confirmed pre-existing implementation

Note that MEM-04 was a Plan 01 deliverable — mention it was formally verified in Phase 33's VERIFICATION.md. Do NOT re-verify it here.

**Anti-Patterns Found:** None
**Human Verification Required:** None
**Gaps Summary:** 0 gaps. All 5 tests pass (include test run output line).

3. Update `.planning/REQUIREMENTS.md` traceability table:
- Change MEM-01 row: `Phase 37, 38` → `Phase 28, 37, 38` and status to `Complete`
- Change MEM-03 row: `Phase 38` → `Phase 28, 38` and status from `Pending` to `Complete`
- Change the MEM-03 checkbox from `[ ]` to `[x]`
  </action>
  <verify>
    <automated>cd ~/.openclaw && test -f .planning/phases/28-l3-auto-memorization/28-VERIFICATION.md && echo "PASS: VERIFICATION.md exists" && python3 -c "r = open('.planning/REQUIREMENTS.md').read(); assert '- [x] **MEM-03**' in r, 'MEM-03 not checked'; assert 'Complete' in r.split('MEM-03')[1].split('\n')[0] or 'Complete' in r.split('MEM-03')[2].split('\n')[0], 'MEM-03 not Complete'; print('PASS: REQUIREMENTS.md updated')"</automated>
  </verify>
  <done>28-VERIFICATION.md exists with 5/5 truths verified, YAML frontmatter matching project schema, cross-references to Phase 33; REQUIREMENTS.md has MEM-01 and MEM-03 as Complete with correct phase references</done>
</task>

</tasks>

<verification>
1. `grep -rn "MEMU_SERVICE_URL" ~/.openclaw/orchestration/` returns zero hits
2. `bash -n ~/.openclaw/docker/l3-specialist/entrypoint.sh` exits 0
3. `python3 -m pytest tests/test_pool_memorization.py -v` shows 5 passed
4. `.planning/phases/28-l3-auto-memorization/28-VERIFICATION.md` exists with `score: 5/5`
5. `.planning/REQUIREMENTS.md` has `[x] **MEM-03**` and both MEM-01/MEM-03 show `Complete` in traceability
6. No `import os` in memory_client.py (if os was only used for MEMU_SERVICE_URL)
</verification>

<success_criteria>
- MEMU_SERVICE_URL constant fully removed from memory_client.py (definition + docstring reference)
- Stale placeholder comment removed from entrypoint.sh
- Phase 28 VERIFICATION.md created with 5/5 observable truths, matching project VERIFICATION.md schema
- REQUIREMENTS.md traceability updated: MEM-01 Complete, MEM-03 Complete (checkbox + table)
- All existing tests still pass after dead code removal
</success_criteria>

<output>
After completion, create `.planning/phases/38-phase28-verification-cleanup/38-01-SUMMARY.md`
</output>
