---
phase: 02-source-connectivity-incremental-sync
plan: 02
type: execute
wave: 2
depends_on: [02-01]
requirements: [INTG-01, INTG-03]
files_modified:
  - src/lib/connectors/slack.ts
  - src/app/api/connectors/slack/oauth/route.ts
  - src/app/api/connectors/slack/channels/route.ts
  - src/app/api/connectors/slack/sync/route.ts
  - src/components/connectors/SlackConnectorCard.tsx
  - src/lib/hooks/useConnectorStatus.ts
  - tests/connectors/slack-adapter.test.ts
autonomous: true
must_haves:
  truths:
    - "User can connect exactly one Slack workspace and choose channels included in sync scope."
    - "Slack incremental sync only ingests net-new/changed records after first import window."
    - "Slack connector surfaces rate limit/auth-expired/error state transitions through shared health model."
  artifacts:
    - path: "src/lib/connectors/slack.ts"
      provides: "Slack adapter implementing OAuth token usage, channel discovery, and incremental history sync."
    - path: "src/app/api/connectors/slack/channels/route.ts"
      provides: "Channel selection read/write endpoint used to scope initial and incremental sync."
    - path: "src/components/connectors/SlackConnectorCard.tsx"
      provides: "UI flow for connect, channel selection, and manual sync-now trigger."
  key_links:
    - from: "src/app/api/connectors/slack/sync/route.ts"
      to: "src/lib/sync/engine.ts"
      via: "Slack sync route delegates execution to shared incremental engine"
      pattern: "engine delegation"
    - from: "src/components/connectors/SlackConnectorCard.tsx"
      to: "src/app/api/connectors/slack/channels/route.ts"
      via: "selected channels are saved and used as source scope"
      pattern: "channel scope persistence"
---

# Phase 02-02 Plan: Slack Connectivity and Channel-Scoped Incremental Ingestion

## Objective
Deliver one-workspace Slack connectivity with channel selection and incremental, resumable ingestion through the shared sync core.

## Context
- `.planning/phases/02-source-connectivity-incremental-sync/02-CONTEXT.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-RESEARCH.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-01-PLAN.md`

<tasks>
  <task id="P02-02-T01" wave="2" type="auto">
    <name>Implement Slack adapter, OAuth/connect routes, and channel-scope persistence</name>
    <title>Implement Slack adapter, OAuth/connect routes, and channel-scope persistence</title>
    <requirements>INTG-01,INTG-03</requirements>
    <depends_on>P02-01-T01,P02-01-T02</depends_on>
    <files>
      <file>src/lib/connectors/slack.ts</file>
      <file>src/app/api/connectors/slack/oauth/route.ts</file>
      <file>src/app/api/connectors/slack/channels/route.ts</file>
      <file>src/app/api/connectors/slack/sync/route.ts</file>
      <file>tests/connectors/slack-adapter.test.ts</file>
    </files>
    <action>Implement Slack connector integration for one workspace: OAuth token exchange/storage, channel discovery, and selected-channel scope persistence. Enforce first-sync window selection (default to a controlled window such as 30 days) before first import. Use Slack cursor/timestamp semantics for incremental runs and route all ingestion through the shared sync engine so checkpoint-after-persist and idempotent upsert rules are inherited.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/slack-adapter.test.ts tests/connectors/sync-engine.test.ts</automated>
      <manual>Connect one Slack workspace, select two channels, run first sync with 30-day window, then rerun sync and confirm only changed/new messages are ingested.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Slack connector supports workspace connect + channel selection and executes source-aware incremental sync behavior without full replay.</done>
  </task>

  <task id="P02-02-T02" wave="2" type="auto">
    <name>Add Slack connector card UX for connect state, channel scope, and sync-now control</name>
    <title>Add Slack connector card UX for connect state, channel scope, and sync-now control</title>
    <requirements>INTG-01</requirements>
    <depends_on>P02-02-T01</depends_on>
    <files>
      <file>src/components/connectors/SlackConnectorCard.tsx</file>
      <file>src/lib/hooks/useConnectorStatus.ts</file>
      <file>src/app/settings/connectors/page.tsx</file>
    </files>
    <action>Create Slack connector card UI that shows workspace connection status, channel selection controls, last sync timestamp, and manual `Sync now` trigger. The UI must surface state transitions from shared health semantics and show actionable error text for `rate_limited` and `auth_expired` without inventing new status names.</action>
    <verify>
      <automated>npm run lint</automated>
      <manual>From connectors settings page: connect Slack, select channels, click Sync now, and verify card state changes to syncing then connected (or rate_limited/auth_expired with clear action hints).</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Slack connector UX supports end-to-end connect/scope/sync flow and reflects required health semantics.</done>
  </task>
</tasks>

## Verification Criteria
- Slack connection supports one workspace and persisted channel scope.
- First sync respects user-selected window and subsequent runs ingest only deltas.
- UI and API share same health/status semantics.

## Must Haves
<must_haves>
  <must_have id="MH-04" requirement="INTG-01">User can connect one Slack workspace and explicitly select channels for sync scope.</must_have>
  <must_have id="MH-05" requirement="INTG-03">Slack incremental sync uses source-aware cursor semantics and avoids duplicate ingestion after resume.</must_have>
</must_haves>
