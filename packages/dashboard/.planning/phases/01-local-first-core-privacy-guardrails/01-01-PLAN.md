---
phase: 01-local-first-core-privacy-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PRIV-01, PRIV-02]
files_modified:
  - src/lib/types/privacy.ts
  - src/lib/privacy/policy.ts
  - src/lib/privacy/consent-store.ts
  - src/lib/privacy/guard.ts
  - src/lib/privacy/transport.ts
  - src/app/api/privacy/consent/route.ts
  - tests/privacy/privacy-guard.test.ts
  - tests/privacy/transport.test.ts
autonomous: true
must_haves:
  truths:
    - "Inference defaults to local-only unless low confidence and explicit project consent allow remote."
    - "Project-scoped consent can be set and revoked without affecting other projects."
    - "Remote transport rejects insecure endpoint configurations."
  artifacts:
    - path: "src/lib/privacy/guard.ts"
      provides: "Single non-bypassable decision gateway for local vs remote execution."
    - path: "src/lib/privacy/consent-store.ts"
      provides: "Project-scoped consent read/write/revoke primitives."
    - path: "src/app/api/privacy/consent/route.ts"
      provides: "Consent API for project-level opt-in and revoke."
    - path: "src/lib/privacy/transport.ts"
      provides: "HTTPS-only remote client configuration enforcement."
  key_links:
    - from: "src/lib/privacy/guard.ts"
      to: "src/lib/privacy/consent-store.ts"
      via: "consent lookup during decision evaluation"
      pattern: "project consent check"
    - from: "src/lib/privacy/guard.ts"
      to: "src/lib/privacy/transport.ts"
      via: "remote path allowed only through guarded transport"
      pattern: "guarded remote client invocation"
---

# Phase 01-01 Plan: Privacy Guard, Consent, and Secure Transport

## Objective
Implement the non-bypassable local-first decision boundary and project-scoped consent primitives that gate all remote inference.

## Context
- `.planning/phases/01-local-first-core-privacy-guardrails/01-CONTEXT.md`
- `.planning/phases/01-local-first-core-privacy-guardrails/01-RESEARCH.md`

<tasks>
  <task id="P01-01-T01" wave="1" type="auto">
    <name>Implement privacy guard, project-scoped consent APIs, and secure remote client constraints</name>
    <title>Implement privacy guard, project-scoped consent APIs, and secure remote client constraints</title>
    <requirements>PRIV-01,PRIV-02</requirements>
    <depends_on></depends_on>
    <files>
      <file>src/lib/types/privacy.ts</file>
      <file>src/lib/privacy/policy.ts</file>
      <file>src/lib/privacy/consent-store.ts</file>
      <file>src/lib/privacy/guard.ts</file>
      <file>src/lib/privacy/transport.ts</file>
      <file>src/app/api/privacy/consent/route.ts</file>
      <file>tests/privacy/privacy-guard.test.ts</file>
      <file>tests/privacy/transport.test.ts</file>
    </files>
    <action>Create one non-bypassable privacy gateway used for inference/embedding entry points. Default mode must be LOCAL_ONLY; remote path may resolve only when local confidence is below configured threshold and project/workspace consent is explicitly true. Implement consent read/write/revoke APIs scoped by projectId (not global and not per-request). Build remote transport through a single client factory that enforces HTTPS-only and certificate validation, and rejects insecure endpoint configs. Do not allow direct remote calls outside guard.ts.</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/privacy/privacy-guard.test.ts tests/privacy/transport.test.ts</automated>
      <manual>Call consent API for one project to opt in and revoke; verify guard decisions change only for that project and insecure http:// endpoint is rejected.</manual>
      <sampling_rate>run after this task commits, before wave 2 begins</sampling_rate>
    </verify>
    <done>Local-first decision engine exists and is wired as the sole entry path; remote inference cannot execute without project-scoped opt-in and secure transport constraints.</done>
  </task>
</tasks>

## Verification Criteria
- Wave 1 checks pass before any Wave 2 execution.

## Must Haves
<must_haves>
  <must_have id="MH-01" requirement="PRIV-01">Default execution remains local for processing and embeddings unless guard explicitly allows remote after opt-in + low-confidence condition.</must_have>
  <must_have id="MH-02" requirement="PRIV-02">Remote path is unreachable without explicit project-scoped consent, and uses encrypted transit only.</must_have>
</must_haves>
