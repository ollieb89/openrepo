---
phase: 21-state-engine-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/spawn_specialist/spawn.py
  - skills/spawn_specialist/pool.py
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Spawning three L3 containers in sequence reuses a single Docker client connection — no repeated docker.from_env() calls"
    - "Docker client is lazily created on first use and reused across subsequent spawns"
    - "Structured logs confirm client reuse (absence of repeated connection setup entries)"
  artifacts:
    - path: "skills/spawn_specialist/spawn.py"
      provides: "Module-level _docker_client with lazy initialization via get_docker_client()"
      contains: "get_docker_client"
    - path: "skills/spawn_specialist/pool.py"
      provides: "Pool uses shared Docker client from spawn module"
      contains: "get_docker_client"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "docker.DockerClient"
      via: "get_docker_client() lazy singleton"
      pattern: "get_docker_client"
    - from: "skills/spawn_specialist/pool.py"
      to: "skills/spawn_specialist/spawn.py"
      via: "imports get_docker_client"
      pattern: "from spawn import.*get_docker_client"
---

<objective>
Introduce Docker client pooling so that multiple L3 container spawns reuse a single Docker client connection instead of creating a new one per spawn.

Purpose: Eliminate repeated `docker.from_env()` overhead during concurrent spawning — each call opens a new connection to the Docker daemon. A shared client reduces latency and resource usage under load.
Output: Modified `spawn.py` with a lazy Docker client singleton and `pool.py` consuming it.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/spawn_specialist/spawn.py
@skills/spawn_specialist/pool.py
@orchestration/logging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Docker client singleton with lazy initialization to spawn.py</name>
  <files>skills/spawn_specialist/spawn.py</files>
  <action>
Add a module-level Docker client singleton with a `get_docker_client()` function:

1. Add a module-level variable `_docker_client: Optional[docker.DockerClient] = None`

2. Create `get_docker_client() -> docker.DockerClient`:
   - If `_docker_client` is None, create via `docker.from_env()`, log at INFO: "Docker client created"
   - If `_docker_client` exists, ping it to verify connection is alive. On failure (daemon restart), recreate and log at WARNING: "Docker client reconnected (daemon restart detected)"
   - Store in `_docker_client` and return it
   - Log cache hit at DEBUG: "Docker client reused"

3. Modify `spawn_l3_specialist()`:
   - Replace `client = docker.from_env()` with `client = get_docker_client()`
   - Remove the separate `client.ping()` block (ping is now inside get_docker_client)
   - Keep the image check (`client.images.get(...)`) as-is

4. Export `get_docker_client` so pool.py can import it.

Do NOT use threading locks — Docker SDK's DockerClient is already thread-safe for concurrent API calls. The singleton is only for connection reuse.
  </action>
  <verify>
    <automated>python3 -c "import ast; tree = ast.parse(open('skills/spawn_specialist/spawn.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'get_docker_client' in funcs, 'get_docker_client not found'; content = open('skills/spawn_specialist/spawn.py').read(); assert 'docker.from_env()' not in content.split('def spawn_l3_specialist')[1].split('def ')[0] if 'def spawn_l3_specialist' in content else True, 'docker.from_env still in spawn_l3_specialist'; print('PASS')"</automated>
    <manual>Verify get_docker_client exists and spawn_l3_specialist no longer calls docker.from_env() directly</manual>
  </verify>
  <done>get_docker_client() lazily creates a Docker client, verifies liveness via ping, reconnects on failure. spawn_l3_specialist() calls get_docker_client() instead of docker.from_env(). Structured log entries confirm creation vs reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pool.py to use shared Docker client from spawn module</name>
  <files>skills/spawn_specialist/pool.py</files>
  <action>
Update pool.py to use the shared Docker client:

1. Add `get_docker_client` to the existing spawn import line:
   ```python
   from spawn import (
       cleanup_container,
       get_container_logs,
       get_docker_client,
       get_skill_timeout,
       load_l3_config,
       spawn_l3_specialist,
   )
   ```

2. No other changes needed in pool.py — `spawn_l3_specialist` already uses `get_docker_client()` internally. The import makes it available if pool.py ever needs direct Docker access (e.g., for container cleanup verification).

3. Verify that `cleanup_container()` in spawn.py still works — it operates on a container object already obtained via the shared client, so no change needed.
  </action>
  <verify>
    <automated>python3 -c "content = open('skills/spawn_specialist/pool.py').read(); assert 'get_docker_client' in content, 'get_docker_client not imported in pool.py'; print('PASS')"</automated>
    <manual>Verify pool.py imports get_docker_client from spawn module</manual>
  </verify>
  <done>pool.py imports get_docker_client from spawn. All container spawns flow through a single Docker client instance. No docker.from_env() calls remain outside get_docker_client().</done>
</task>

</tasks>

<verification>
1. `grep -rn "docker.from_env" skills/spawn_specialist/` — should appear only inside `get_docker_client()`
2. `grep -n "get_docker_client" skills/spawn_specialist/spawn.py skills/spawn_specialist/pool.py` — should appear in both files
3. `python3 -c "import ast; tree = ast.parse(open('skills/spawn_specialist/spawn.py').read()); print('Valid Python')"` — syntax check
4. `python3 -c "import ast; tree = ast.parse(open('skills/spawn_specialist/pool.py').read()); print('Valid Python')"` — syntax check
</verification>

<success_criteria>
- docker.from_env() only appears inside get_docker_client()
- spawn_l3_specialist() calls get_docker_client() instead of creating its own client
- pool.py imports get_docker_client for availability
- Structured log entries at INFO (creation), WARNING (reconnect), DEBUG (reuse)
- No threading locks added (Docker SDK is thread-safe)
</success_criteria>

<output>
After completion, create `.planning/phases/21-state-engine-performance/21-01-SUMMARY.md`
</output>
