---
phase: 02-core-orchestration
plan: 02
type: execute
wave: 2
depends_on: [02-01-PLAN.md]
files_modified:
  - openclaw.json
  - skills/router_skill/index.js
  - skills/router_skill/skill.json
autonomous: true
requirements: [COM-01, COM-02]
must_haves:
  truths:
    - "ClawdiaPrime (L1) can dispatch directives to PumplAI_PM via the Gateway"
    - "Lane Queues are correctly configured for task prioritization"
    - "router_skill is registered to ClawdiaPrime in openclaw.json"
  artifacts:
    - path: "skills/router_skill/index.js"
      provides: "Hub-and-spoke dispatch logic"
    - path: "skills/router_skill/skill.json"
      provides: "Skill metadata and command definition"
  key_links:
    - from: "router_skill"
      to: "Gateway API"
      via: "/api/v1/lane/enqueue"
    - from: "openclaw.json"
      to: "router_skill"
      via: "capabilities: ['router_skill']"
---

<objective>
Implement the hub-and-spoke communication and lane queue prioritization for the swarm.

Purpose: Enable the L1 orchestrator to delegate tasks to L2 PMs via a central message bus, ensuring concurrency control and reliable routing.
Output: Functional router skill and verified L1 -> L2 delegation handshake.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Router Skill for ClawdiaPrime (COM-01)</name>
  <files>
    - skills/router_skill/index.js
    - skills/router_skill/skill.json
  </files>
  <action>
    Create a specialized routing skill for the L1 orchestrator:
    1. Create directory `mkdir -p skills/router_skill`.
    2. Implement `skills/router_skill/index.js` using `axios` to POST tasks to the Gateway's `/api/v1/lane/enqueue`.
    3. Configure `skills/router_skill/skill.json` to expose the `dispatch_directive` command.
  </action>
  <verify>
    ls skills/router_skill/ && cat skills/router_skill/index.js | grep "axios.post"
  </verify>
  <done>The Hub-and-Spoke router skill is implemented and ready for L1 usage.</done>
</task>

<task type="auto">
  <name>Task 2: Register Skill and Configure Lanes in openclaw.json (COM-01, COM-02)</name>
  <files>openclaw.json</files>
  <action>
    Integrate the routing skill and define the lane queue structure:
    1. Add `router_skill` to `clawdia_prime.capabilities` in `openclaw.json`.
    2. Add `lanes` configuration if required by the Gateway version.
    3. Ensure `pumplai_pm` is mapped to its own lane for tactical tasks.
  </action>
  <verify>
    cat openclaw.json | jq '.agents.list[] | select(.id=="clawdia_prime").capabilities'
  </verify>
  <done>Router skill is registered and Lane Queues are configured.</done>
</task>

<task type="auto">
  <name>Task 3: Verify L1 -> L2 Delegation (COM-01)</name>
  <files></files>
  <action>
    Perform an end-to-end handshake test:
    1. Start the OpenClaw Gateway (if not running).
    2. Issue a directive from L1: `openclaw exec clawdia_prime "Ask PumplAI_PM to synchronize workspace status"`.
    3. Verify the Gateway logs show the task enqueued and picked up by `pumplai_pm`.
  </action>
  <verify>
    Check Gateway logs or verify jobId response from the exec command.
  </verify>
  <done>L1 to L2 delegation is verified and functional.</done>
</task>

</tasks>

<verification>
Check Gateway connectivity.
Confirm `router_skill` is available to `clawdia_prime`.
</verification>

<success_criteria>
- L1 can successfully route a task to L2.
- Gateway handles the request via `/api/v1/lane/enqueue`.
- Task priority and lane separation are enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-orchestration/02-02-SUMMARY.md`
Then create `.planning/phases/02-core-orchestration/02-VERIFICATION.md`
</output>
