---
phase: 14-project-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - scripts/verify_phase14.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06

must_haves:
  truths:
    - "Verification script confirms all 6 CLI requirements pass with structural and functional checks"
    - "Verification script is non-destructive — creates temp projects, tests them, cleans up, restores original active project"
  artifacts:
    - path: "scripts/verify_phase14.py"
      provides: "Phase 14 verification script with per-requirement checks"
      min_lines: 100
  key_links:
    - from: "scripts/verify_phase14.py"
      to: "orchestration/project_cli.py"
      via: "subprocess calls to project_cli.py subcommands"
      pattern: "project_cli.py"
---

<objective>
Create a verification script that confirms all 6 CLI requirements (CLI-01 through CLI-06) are satisfied with concrete evidence.

Purpose: Provide repeatable proof that Phase 14 implementation is correct, following the established verification script convention (one function per requirement, PASS/FAIL output, exits 0 on all pass).
Output: `scripts/verify_phase14.py`
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-project-cli/14-01-SUMMARY.md
@orchestration/project_cli.py
@scripts/verify_phase13.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 14 verification script</name>
  <files>scripts/verify_phase14.py</files>
  <action>
Create `scripts/verify_phase14.py` following the established convention from `scripts/verify_phase13.py` (one function per requirement, prints [PASS]/[FAIL] per check, main() exits 0 on all pass, 1 on any failure).

The script must be non-destructive: save the current `active_project`, create temporary test projects, verify behavior, clean up test projects, and restore the original active project.

**Structure:**

```python
#!/usr/bin/env python3
"""Phase 14 Verification: Project CLI requirements CLI-01 through CLI-06."""
```

**verify_cli_01() — init creates project.json from flags:**
- Use `subprocess.run` to call `python3 orchestration/project_cli.py init --id _verify14test --name "Verify Test" --force`
- Check `projects/_verify14test/project.json` exists and is valid JSON
- Check required fields: id, name, agent_display_name, workspace, tech_stack, agents, l3_overrides
- Check SOUL.md was created (either in agents dir or projects dir — check write_soul output)
- Cleanup: `shutil.rmtree("projects/_verify14test")` — using `_` prefix so it won't interfere with list

Actually, do NOT use `_` prefix for test project IDs — that would make them invisible to `list`. Use `verify14test` as the ID. Clean up at the end.

**verify_cli_02() — list shows projects:**
- Run `python3 orchestration/project_cli.py list` and capture stdout
- Verify output contains "pumplai" and "geriai"
- Verify output contains an active marker (`*`) on exactly one line

**verify_cli_03() — switch updates active_project:**
- Save current active_project from openclaw.json
- Run `python3 orchestration/project_cli.py init --id verify14switch --name "Switch Test" --force`
- Run `python3 orchestration/project_cli.py switch verify14switch`
- Read openclaw.json — verify `active_project == "verify14switch"`
- Restore original active project
- Cleanup test project

**verify_cli_04() — remove deletes project dir, blocks active removal:**
- Create test project: `init --id verify14rm --name "Remove Test" --force`
- Switch to a different project (ensure verify14rm is not active)
- Run `python3 orchestration/project_cli.py remove verify14rm --force`
- Verify `projects/verify14rm/` no longer exists
- Test active-project guard: try to remove the current active project, verify exit code is non-zero

**verify_cli_05() — init --template applies preset values:**
- Run `python3 orchestration/project_cli.py init --id verify14tmpl --name "Template Test" --template fullstack --force`
- Load `projects/verify14tmpl/project.json`
- Verify `tech_stack.frontend` is non-empty (should contain "Next.js" from fullstack template)
- Verify `tech_stack.backend` is non-empty
- Cleanup test project

**verify_cli_06() — template presets exist in projects/_templates/:**
- Verify `projects/_templates/fullstack.json` exists and is valid JSON with `_template` field
- Verify `projects/_templates/backend.json` exists and is valid JSON with `_template` field
- Verify `projects/_templates/ml-pipeline.json` exists and is valid JSON with `_template` field
- Each must have `tech_stack` and `l3_overrides` keys

**main():**
- Save original active_project at start
- Run all 6 verify functions, collect pass/fail
- Print summary table
- Restore original active_project in a finally block
- Clean up any leftover test projects (verify14test, verify14switch, verify14tmpl, verify14rm) in a finally block
- Exit 0 if all pass, 1 if any fail
  </action>
  <verify>
    <automated>python3 scripts/verify_phase14.py</automated>
  </verify>
  <done>
- `python3 scripts/verify_phase14.py` exits 0 with all 6 checks showing [PASS]
- No test artifacts remain after script completes (verify14* projects cleaned up)
- Original active_project restored after verification
  </done>
</task>

</tasks>

<verification>
`python3 scripts/verify_phase14.py` exits 0 with PASS for all 6 CLI requirements.
</verification>

<success_criteria>
Verification script confirms all 6 CLI requirements with concrete evidence. Script is self-cleaning and non-destructive. Follows established verify_phase13.py convention.
</success_criteria>

<output>
After completion, create `.planning/phases/14-project-cli/14-02-SUMMARY.md`
</output>
