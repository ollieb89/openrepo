---
phase: 24-dashboard-metrics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/occc/src/components/agents/AgentTree.tsx
  - workspace/occc/src/components/agents/AgentCard.tsx
autonomous: true
requirements:
  - DSH-09

must_haves:
  truths:
    - "Switching projects updates the agent tree to show only agents for that project plus global agents"
    - "Global agents (no project field) appear in a separate 'Global' section at the top with a badge"
    - "Project-specific agents appear in a separate section below the global section"
    - "Each agent has a status indicator dot: green=idle, yellow=busy, gray=offline"
    - "Expand/collapse state resets when switching projects"
    - "Empty project section shows 'No agents assigned to this project' message"
  artifacts:
    - path: "workspace/occc/src/components/agents/AgentTree.tsx"
      provides: "Split global vs project agent rendering with collapsible sections"
      min_lines: 50
    - path: "workspace/occc/src/components/agents/AgentCard.tsx"
      provides: "Status indicator dot on each agent card"
      min_lines: 40
  key_links:
    - from: "workspace/occc/src/components/agents/AgentTree.tsx"
      to: "useAgents + useTasks hooks"
      via: "Agent filtering and task-based status derivation"
      pattern: "useAgents|useTasks"
    - from: "workspace/occc/src/components/agents/AgentCard.tsx"
      to: "AgentTree.tsx"
      via: "status prop passed from tree to card"
      pattern: "status.*idle|busy|offline"
---

<objective>
Enhance the agent hierarchy view to separate global and project agents into distinct sections, add status indicator dots derived from task state, and reset expand/collapse state on project switch.

Purpose: DSH-09 requires the agent tree to filter by selected project, showing relevant L2/L3 agents with visual status indicators. Global agents must remain visible across all projects.

Output: Updated AgentTree with global/project sections, AgentCard with status dots, and instant swap on project change.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-dashboard-metrics/24-RESEARCH.md
@workspace/occc/src/components/agents/AgentTree.tsx
@workspace/occc/src/components/agents/AgentCard.tsx
@workspace/occc/src/lib/hooks/useAgents.ts
@workspace/occc/src/lib/hooks/useTasks.ts
@workspace/occc/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status dot to AgentCard and derive agent status in AgentTree</name>
  <files>
    workspace/occc/src/components/agents/AgentCard.tsx
    workspace/occc/src/components/agents/AgentTree.tsx
  </files>
  <action>
1. Update `AgentCard.tsx` to accept and display a status prop:
- Add `status?: 'idle' | 'busy' | 'offline'` prop alongside `agent`
- Add a status dot next to the agent name in the header row: `<span className="w-2.5 h-2.5 rounded-full {colorClass}" />`
  - `idle` → `bg-green-400` (green)
  - `busy` → `bg-yellow-400` (yellow)
  - `offline` → `bg-gray-400` (gray)
- The dot should appear between the level badge and the agent name
- If `status` prop is not provided, default to `'offline'` (backward compatible)

2. Rewrite `AgentTree.tsx` to split global vs project agents and derive status:
- Import `useTasks` from `@/lib/hooks/useTasks`
- After getting `agents` from `useAgents(projectId)`, split into:
  - `globalAgents = agents.filter(a => !a.project)` — agents with no project field
  - `projectAgents = agents.filter(a => a.project === projectId)` — agents explicitly assigned to this project
- Derive agent status from task data:
  - Get `tasks` via `useTasks(projectId)` (already available, polls every 3s)
  - `activeTasks = tasks.filter(t => ['in_progress', 'starting', 'testing'].includes(t.status))`
  - `hasNonTerminal = tasks.some(t => !['completed', 'failed', 'rejected'].includes(t.status))`
  - Status logic per agent level:
    - L1 agents: always `'idle'` (strategic, no direct task ownership)
    - L2 agents: `hasNonTerminal ? 'busy' : 'idle'` (busy when any task is non-terminal)
    - L3 agents: `activeTasks.length > 0 ? 'busy' : 'idle'` (busy when tasks are actively running)
  - Create a helper: `function getAgentStatus(agent: Agent, activeTasks: Task[], hasNonTerminal: boolean): 'idle' | 'busy' | 'offline'`
- Pass `status` to each `<AgentCard agent={agent} status={status} />`

3. Render two sections in AgentTree:
- **Global section** (always shown):
  - Header: `<div className="flex items-center gap-2 mb-3"><span className="px-2 py-0.5 bg-gray-100 dark:bg-gray-700 rounded-full text-xs font-semibold text-gray-600 dark:text-gray-300">Global</span></div>`
  - Render `globalAgents` using the existing `AgentNode` recursive component
  - If no global agents, show nothing (global agents always exist in openclaw)

- **Project section** (below global):
  - Header: `<div className="flex items-center gap-2 mb-3 mt-6"><span className="px-2 py-0.5 bg-blue-100 dark:bg-blue-900/30 rounded-full text-xs font-semibold text-blue-700 dark:text-blue-300">Project</span></div>`
  - Render `projectAgents` using `AgentNode`
  - If no project agents: show `<p className="text-sm text-gray-500 dark:text-gray-400 py-4">No agents assigned to this project</p>`

4. Key the outer wrapper on `projectId` to force state reset on project switch:
- The top-level export should be a wrapper that keys the tree: wrap the internal tree component in a parent that applies `key={projectId}`
- Pattern: `export default function AgentTree() { ... return <AgentTreeInner key={projectId} ... /> }`
- This ensures expand/collapse useState resets when project changes (per research pitfall #5)

5. Update `AgentNode` to accept and pass down the `getAgentStatus` function (or precompute a status map):
- Simplest: create a `statusMap: Record<string, 'idle' | 'busy' | 'offline'>` before rendering, mapping `agent.id → status`
- Pass `statusMap` through `AgentNode` props, look up `statusMap[agent.id]` when rendering `AgentCard`
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw/workspace/occc && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit http://localhost:6987/agents — verify global agents appear in "Global" section at top with badge, project agents in "Project" section below; each agent shows a colored status dot; switch projects and confirm tree resets and updates</manual>
  </verify>
  <done>AgentTree splits agents into Global (badge, top) and Project (badge, below) sections; AgentCard shows green/yellow/gray status dot; status derived from task data (L1=idle, L2=busy when non-terminal tasks, L3=busy when active tasks); expand/collapse resets on project switch; empty project section shows message</done>
</task>

</tasks>

<verification>
1. Navigate to `/agents` — agent tree shows two sections: Global (with gray badge) and Project (with blue badge)
2. Global agents (L1 ClawdiaPrime) appear at top regardless of which project is selected
3. Project agents (L2 PM + L3 Specialist for selected project) appear in Project section
4. Each agent card has a colored dot: green (idle), yellow (busy), gray (offline)
5. Switch projects in header selector — agent tree updates instantly with correct agents, expand state resets
6. Select a project with no assigned agents — "No agents assigned to this project" message appears in Project section
7. Agent status dots reflect current task state (L3 yellow when tasks running, green when idle)
</verification>

<success_criteria>
- Agent tree has two visual sections (Global + Project) with labeled badges
- Status indicator dots appear on every agent card with correct color logic
- Project switch causes instant tree swap with no stale expand/collapse state
- Empty project section shows helpful message
- Existing `/agents` page continues to work with enhanced tree
</success_criteria>

<output>
After completion, create `.planning/phases/24-dashboard-metrics/24-02-SUMMARY.md`
</output>
