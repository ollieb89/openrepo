---
phase: 01-environment-substrate
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [SET-03, SEC-01]
must_haves:
  truths:
    - "OpenClaw Gateway is running and accessible on port 18789"
    - "PumplAI_PM agent cannot access files outside its defined workspace"
  artifacts:
    - path: "openclaw.json"
      provides: "Gateway token"
  key_links:
    - from: "Gateway"
      to: "Port 18789"
      via: "TCP Listen"
---

<objective>
Activate the OpenClaw communication layer and verify the security boundaries of the swarm.

Purpose: Enable agent-to-agent communication via the Gateway and ensure that the L2 PM agent is strictly confined to the project workspace.
Output: Active Gateway and verified isolation.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE_1_RESEARCH.md
@openclaw.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize and Verify Gateway (SET-03)</name>
  <files></files>
  <action>
    Start the OpenClaw Gateway using the configuration from `openclaw.json`:
    1. Run `openclaw gateway run` in the background (or as a service if applicable).
    2. Verify the gateway is listening on port 18789.
    3. Perform a health check/ping against the gateway using the token from `openclaw.json`.
  </action>
  <verify>
    curl -I http://localhost:18789/health (or similar command verified via openclaw --help)
  </verify>
  <done>The Gateway is active and responding to authenticated requests.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify Agent Workspace Isolation (SEC-01)</name>
  <what-built>Gateway and PumplAI_PM agent configuration.</what-built>
  <how-to-verify>
    Verify that the `pumplai_pm` agent is restricted to its workspace:
    1. Attempt to run a command via the agent that lists a directory outside its workspace (e.g., `openclaw run --agent pumplai_pm "ls ~/"`).
    2. Confirm that the command fails or is restricted to the container root, and that it cannot see the host's `~/` directory.
    3. Verify the agent can see `~/Development/Projects/pumplai` as its root or primary workspace.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <done>PumplAI_PM agent isolation is confirmed.</done>
</task>

</tasks>

<verification>
Check gateway connectivity.
Check agent container logs for workspace mounting errors.
</verification>

<success_criteria>
- Gateway responds with 200 OK to health checks.
- Agent isolation test prevents access to sensitive host directories.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-substrate/01-02-SUMMARY.md`
</output>
