---
phase: 18-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/l3-specialist/entrypoint.sh
  - skills/spawn_specialist/spawn.py
  - orchestration/__init__.py
  - projects/geriai/project.json
autonomous: true
requirements:
  - CFG-03
  - CFG-06
  - MPR-03

must_haves:
  truths:
    - "entrypoint.sh uses DEFAULT_BRANCH env var instead of hardcoded 'main' for staging branch base"
    - "spawn_l3_specialist() passes DEFAULT_BRANCH env var derived from _detect_default_branch() to L3 containers"
    - "orchestration/__init__.py exports get_state_path, get_snapshot_dir, ProjectNotFoundError, render_soul, write_soul in __all__"
    - "projects/geriai/project.json has id 'geriai' and geriai-specific values instead of copy-pasted pumplai values"
  artifacts:
    - path: "docker/l3-specialist/entrypoint.sh"
      provides: "Dynamic branch detection via DEFAULT_BRANCH env var"
      contains: "DEFAULT_BRANCH"
    - path: "skills/spawn_specialist/spawn.py"
      provides: "DEFAULT_BRANCH env var injection into container environment"
      contains: "DEFAULT_BRANCH"
    - path: "orchestration/__init__.py"
      provides: "Complete public API surface with docstring"
      contains: "get_state_path"
    - path: "projects/geriai/project.json"
      provides: "Correct geriai project identity"
      contains: "geriai"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "docker/l3-specialist/entrypoint.sh"
      via: "DEFAULT_BRANCH env var in container environment dict"
      pattern: "DEFAULT_BRANCH.*_detect_default_branch"
    - from: "orchestration/__init__.py"
      to: "orchestration/project_config.py"
      via: "import and re-export in __all__"
      pattern: "get_state_path.*get_snapshot_dir.*ProjectNotFoundError"
---

<objective>
Fix 3 independent integration wiring issues: thread DEFAULT_BRANCH env var from spawn.py through to entrypoint.sh, add missing symbols to orchestration package exports with complete __all__ and docstring, and correct geriai project.json identity.

Purpose: Close CFG-06 (dynamic branch detection at container boundary), CFG-03 (package export surface), and MPR-03 (geriai state path resolution) gaps identified by the v1.1 milestone audit.
Output: 3 files modified with wiring fixes, 1 JSON data correction.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-integration-hardening/18-RESEARCH.md
@orchestration/__init__.py
@orchestration/snapshot.py
@skills/spawn_specialist/spawn.py
@docker/l3-specialist/entrypoint.sh
@projects/geriai/project.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread DEFAULT_BRANCH env var through container boundary</name>
  <files>
    skills/spawn_specialist/spawn.py
    docker/l3-specialist/entrypoint.sh
  </files>
  <action>
Two coordinated changes to close the entrypoint branch detection gap:

**spawn.py** (inside `spawn_l3_specialist()`, before the `container_config` dict):
1. Import `_detect_default_branch` from `orchestration.snapshot` (use direct submodule import per project convention: `from orchestration.snapshot import _detect_default_branch`)
2. Call `_detect_default_branch(Path(workspace_path), project_id)` to get the default branch name
3. Add `"DEFAULT_BRANCH": default_branch` to the `"environment"` dict inside `container_config`

Place the import at the top of the file with other orchestration imports (not a local import — this module already does top-level imports from orchestration). Add the `_detect_default_branch` call after the `staging_branch` assignment (around line 138) and before `container_config` (line 157).

**entrypoint.sh**:
1. Add `DEFAULT_BRANCH` to the env var declarations near the top (after line 9): `: "${DEFAULT_BRANCH:=main}"` — uses `:=` to assign default if unset
2. Change line 39 from:
   `git checkout -b "${STAGING_BRANCH}" main 2>/dev/null || git checkout -b "${STAGING_BRANCH}"`
   to:
   `git checkout -b "${STAGING_BRANCH}" "${DEFAULT_BRANCH}" 2>/dev/null || git checkout -b "${STAGING_BRANCH}"`
  </action>
  <verify>
    <automated>grep -q 'DEFAULT_BRANCH' /home/ollie/.openclaw/skills/spawn_specialist/spawn.py && grep -q 'DEFAULT_BRANCH' /home/ollie/.openclaw/docker/l3-specialist/entrypoint.sh && echo "PASS" || echo "FAIL"</automated>
    <manual>Verify spawn.py calls _detect_default_branch and passes result as DEFAULT_BRANCH env var; verify entrypoint.sh uses ${DEFAULT_BRANCH} instead of hardcoded main</manual>
  </verify>
  <done>spawn_l3_specialist() injects DEFAULT_BRANCH env var derived from _detect_default_branch(); entrypoint.sh consumes ${DEFAULT_BRANCH:-main} for staging branch base instead of hardcoded "main"</done>
</task>

<task type="auto">
  <name>Task 2: Define complete orchestration package public API with docstring</name>
  <files>orchestration/__init__.py</files>
  <action>
Replace the entire contents of `orchestration/__init__.py` with the complete public API surface per the locked CONTEXT.md decision ("full __all__ — not just the 3 missing symbols, but the complete public API"):

1. Add a module docstring at the top:
```python
"""
OpenClaw Orchestration Package

Public API for the Jarvis Protocol state engine, project configuration,
git snapshot workflow, SOUL renderer, and workspace initialization.

External consumers (L3 containers, CLI tools, dashboard) should import
from this package root. Internal cross-module code should use direct
submodule imports (e.g., `from orchestration.config import X`).
"""
```

2. Add the 3 missing imports from project_config: `get_state_path`, `get_snapshot_dir`, `ProjectNotFoundError`
3. Add soul_renderer imports: `from .soul_renderer import render_soul, write_soul`
4. Rebuild `__all__` with categorized comments matching the research template:
   - State engine: JarvisState
   - Config constants: LOCK_TIMEOUT, POLL_INTERVAL
   - Workspace lifecycle: initialize_workspace, verify_workspace
   - Project configuration: load_project_config, get_workspace_path, get_tech_stack, get_agent_mapping, get_active_project_id, get_state_path, get_snapshot_dir, ProjectNotFoundError
   - Git snapshot workflow: create_staging_branch, capture_semantic_snapshot, l2_review_diff, l2_merge_staging, l2_reject_staging, cleanup_old_snapshots, GitOperationError
   - SOUL renderer: render_soul, write_soul

**Strictly public symbols only** — no private/internal functions.
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "from orchestration import get_state_path, get_snapshot_dir, ProjectNotFoundError, render_soul, write_soul; print('PASS')"</automated>
    <manual>Verify __all__ contains all listed symbols and module docstring is present</manual>
  </verify>
  <done>orchestration/__init__.py exports get_state_path, get_snapshot_dir, ProjectNotFoundError, render_soul, write_soul alongside all existing public symbols; module docstring documents package purpose and import conventions</done>
</task>

<task type="auto">
  <name>Task 3: Correct geriai project.json identity</name>
  <files>projects/geriai/project.json</files>
  <action>
Replace the copy-pasted pumplai values with geriai-specific values. The critical correctness requirement is `"id": "geriai"` — this is what `get_state_path("geriai")` validates against.

Set:
- `"id": "geriai"`
- `"name": "GerIAI"`
- `"agent_display_name": "GerIAI_PM"`
- `"workspace": "/home/ollie/Development/Projects/geriai"`
- `"tech_stack"`: empty strings for all 3 fields (frontend, backend, infra) — actual stack unknown, placeholder is fine
- `"agents"`: `{"l2_pm": "geriai_pm", "l3_executor": "l3_specialist"}`
- `"l3_overrides"`: keep same defaults as pumplai (4g mem, 100000 cpu_quota, same 3 runtimes)
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "import json; d=json.load(open('projects/geriai/project.json')); assert d['id']=='geriai', f'id is {d[\"id\"]}'; print('PASS')"</automated>
    <manual>Verify all fields have geriai-specific values, not pumplai copy-paste</manual>
  </verify>
  <done>projects/geriai/project.json has "id": "geriai" and all geriai-specific values; get_state_path("geriai") will resolve to the correct project identity</done>
</task>

</tasks>

<verification>
All 3 fixes verified independently:
1. `grep -c 'DEFAULT_BRANCH' spawn.py entrypoint.sh` — both files contain the env var
2. `python3 -c "from orchestration import get_state_path, get_snapshot_dir, ProjectNotFoundError, render_soul, write_soul"` — all 5 missing symbols importable
3. `python3 -c "import json; assert json.load(open('projects/geriai/project.json'))['id'] == 'geriai'"` — geriai identity correct
</verification>

<success_criteria>
- spawn.py calls _detect_default_branch() and injects DEFAULT_BRANCH into L3 container environment
- entrypoint.sh uses ${DEFAULT_BRANCH:-main} for staging branch creation base
- orchestration/__init__.py has module docstring and complete __all__ with all public symbols including get_state_path, get_snapshot_dir, ProjectNotFoundError, render_soul, write_soul
- projects/geriai/project.json has "id": "geriai" with geriai-specific values
</success_criteria>

<output>
After completion, create `.planning/phases/18-integration-hardening/18-01-SUMMARY.md`
</output>
