---
phase: 09-integration-wiring-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/spawn_specialist/spawn.py
  - workspace/occc/src/lib/docker.ts
  - skills/review_skill/review.py
  - skills/review_skill/skill.json
autonomous: true
requirements:
  - DSH-03
  - COM-04

must_haves:
  truths:
    - "spawn.py sets openclaw.managed=true label on all new L3 containers"
    - "listSwarmContainers filters by label only — no name-pattern fallback"
    - "skills/review_skill/ directory exists with a working Python entry point"
  artifacts:
    - path: "skills/spawn_specialist/spawn.py"
      provides: "Container labels including openclaw.managed=true"
      contains: "openclaw.managed"
    - path: "workspace/occc/src/lib/docker.ts"
      provides: "Label-only container filtering"
      contains: "openclaw.managed=true"
    - path: "skills/review_skill/review.py"
      provides: "Stub review skill entry point"
      contains: "review_l3_work"
    - path: "skills/review_skill/skill.json"
      provides: "Skill registration metadata"
      contains: "review_skill"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "workspace/occc/src/lib/docker.ts"
      via: "openclaw.managed=true label set in spawn, filtered in docker.ts"
      pattern: "openclaw\\.managed"
    - from: "skills/review_skill/skill.json"
      to: "agents/pumplai_pm/agent/config.json"
      via: "skill_path reference in pumplai_pm config points to skills/review_skill"
      pattern: "review_skill"
---

<objective>
Fix container label gap (INT-03) and create review_skill stub (INT-02).

Purpose: INT-03 ensures listSwarmContainers uses label-only filtering (single source of truth) instead of the name-pattern fallback. INT-02 resolves the phantom skill reference in pumplai_pm config by creating a real stub directory.
Output: spawn.py with correct labels, simplified docker.ts, and skills/review_skill/ with entry point + registration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-wiring-cleanup/09-RESEARCH.md
@skills/spawn_specialist/spawn.py
@workspace/occc/src/lib/docker.ts
@agents/pumplai_pm/agent/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openclaw.managed label to spawn.py and simplify docker.ts</name>
  <files>
    skills/spawn_specialist/spawn.py
    workspace/occc/src/lib/docker.ts
  </files>
  <action>
**spawn.py (INT-03 label fix):**
Edit the labels dict in `spawn_l3_specialist()` to add `openclaw.managed` and `openclaw.level`. The current labels dict has `openclaw.tier`, `openclaw.task_id`, `openclaw.spawned_by`, `openclaw.skill`. Add these two new entries at the TOP of the dict:

```python
"labels": {
    "openclaw.managed": "true",
    "openclaw.level": str(l3_config.get("level", 3)),
    "openclaw.task_id": task_id,
    "openclaw.spawned_by": spawned_by,
    "openclaw.skill": skill_hint,
    "openclaw.tier": f"l{l3_config.get('level', 3)}",  # preserved for backward compat
},
```

IMPORTANT: Docker label values must be strings. Use `"true"` (string), NOT `True` (Python bool). Keep `openclaw.tier` for backward compatibility — do NOT remove it.

**docker.ts (INT-03 fallback removal):**
Replace the entire `listSwarmContainers()` function body with the simplified label-only version. Remove the name-pattern fallback and the merge/deduplicate logic. Target implementation:

```typescript
export async function listSwarmContainers(): Promise<Docker.ContainerInfo[]> {
  if (!(await checkDockerAvailability())) return [];

  try {
    // Label-only filter: openclaw.managed=true is the single source of truth
    // spawn.py now sets this label on all managed containers (Phase 9: INT-03)
    return await docker.listContainers({
      filters: {
        label: ['openclaw.managed=true'],
      },
    });
  } catch (error) {
    console.error('[Docker] Error listing swarm containers:', error);
    return [];
  }
}
```

Keep all other functions in docker.ts unchanged.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('skills/spawn_specialist/spawn.py').read()); print('spawn.py: valid Python')" && grep -q 'openclaw.managed' skills/spawn_specialist/spawn.py && grep -q 'openclaw.level' skills/spawn_specialist/spawn.py && echo "Labels present"</automated>
    <manual>Run `docker inspect` on a test L3 container after spawning to confirm labels include openclaw.managed=true</manual>
  </verify>
  <done>spawn.py labels dict includes openclaw.managed="true" and openclaw.level. docker.ts listSwarmContainers uses label-only filter with no name-pattern fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Create review_skill stub directory with Python entry point</name>
  <files>
    skills/review_skill/review.py
    skills/review_skill/skill.json
  </files>
  <action>
**Create skills/review_skill/ directory** with two files mirroring the spawn_specialist pattern.

**skills/review_skill/skill.json:**
```json
{
  "id": "review_skill",
  "name": "L3 Work Reviewer",
  "description": "Reviews L3 git diffs on staging branches and merges or rejects to main. Stub: logs request and returns success.",
  "owner": "pumplai_pm",
  "commands": [
    {
      "name": "review",
      "description": "Review L3 staging branch and merge or reject.",
      "parameters": {
        "task_id": { "type": "string", "required": true },
        "staging_branch": { "type": "string", "required": true },
        "action": { "type": "string", "enum": ["merge", "reject"], "required": true }
      },
      "handler": "python3 review.py"
    }
  ]
}
```

**skills/review_skill/review.py:**
Create a Python entry point that:
1. Accepts `task_id`, `staging_branch`, and `action` (merge|reject) as CLI args via argparse
2. Logs the review request with `[review_skill]` prefix to stdout
3. Returns a JSON result dict with status "acknowledged", the input params, and a note about stub implementation
4. Exits with code 0

Use the exact code from 09-RESEARCH.md Pattern 4 (the `review_l3_work` function with argparse main block). This is a stub — keep it minimal. Do NOT add any git operations, docker imports, or state engine calls.
  </action>
  <verify>
    <automated>python3 skills/review_skill/review.py test-123 l3/task-test-123 merge 2>&1 | grep -q "acknowledged" && python3 -c "import json; json.load(open('skills/review_skill/skill.json')); print('skill.json: valid JSON')" && echo "Review skill stub works"</automated>
  </verify>
  <done>skills/review_skill/ directory exists with review.py (callable, returns acknowledged JSON) and skill.json (valid registration matching spawn_specialist pattern). The pumplai_pm config.json skill_path "skills/review_skill" now resolves to a real directory.</done>
</task>

</tasks>

<verification>
1. `grep -q 'openclaw.managed' skills/spawn_specialist/spawn.py` — label present in spawn
2. `grep -q 'openclaw.managed=true' workspace/occc/src/lib/docker.ts` — filter present in docker.ts
3. `grep -v 'name.*openclaw' workspace/occc/src/lib/docker.ts | grep -c 'listSwarmContainers'` — no name-pattern fallback
4. `python3 skills/review_skill/review.py test-1 l3/task-test-1 merge` — returns JSON with "acknowledged"
5. `test -f skills/review_skill/skill.json` — registration file exists
</verification>

<success_criteria>
- spawn.py labels dict has `openclaw.managed: "true"` and `openclaw.level` entries
- docker.ts listSwarmContainers is label-only (no name-pattern fallback code)
- skills/review_skill/review.py runs and returns acknowledged JSON
- skills/review_skill/skill.json is valid JSON with correct skill registration
- Separate commits for INT-03 and INT-02 per locked decision
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-wiring-cleanup/09-01-SUMMARY.md`
</output>
