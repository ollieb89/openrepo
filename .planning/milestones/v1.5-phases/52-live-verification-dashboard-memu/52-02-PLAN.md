---
phase: 52-live-verification-dashboard-memu
plan: 02
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - .planning/phases/52-live-verification-dashboard-memu/52-verification.json
  - .planning/phases/52-live-verification-dashboard-memu/52-screenshots/
autonomous: false
requirements:
  - "v1.4 gap closure"
must_haves:
  truths:
    - "Health scan populates flag badges on memories with issues"
    - "Archive stale flag PUT succeeds, flag removed, toast appears"
    - "Conflict badge click opens ConflictPanel with side-by-side diff"
    - "Editing a flagged memory triggers re-scan automatically"
    - "Threshold adjustment in settings page persists across refresh"
    - "/suggestions page renders without errors"
    - "Accepting a suggestion appends to soul-override.md and re-renders SOUL"
    - "Sidebar badge shows correct pending suggestion count"
    - "POST /api/suggestions invokes suggest.py and returns results"
  artifacts:
    - path: ".planning/phases/52-live-verification-dashboard-memu/52-verification.json"
      provides: "Structured test results with timestamps and evidence"
      contains: "status"
  key_links:
    - from: "dashboard /api/memory/health-scan"
      to: "memU POST /memories/health-scan"
      via: "proxy fetch"
      pattern: "health-scan"
    - from: "dashboard /api/suggestions POST"
      to: "suggest.py --project"
      via: "execFileAsync"
      pattern: "suggest"
    - from: "dashboard /api/suggestions/[id]/action POST"
      to: "soul-override.md append + soul_renderer.py"
      via: "fs.appendFile + execFile"
      pattern: "soul-override"
---

<objective>
Execute all 9 live verification tests for v1.4 dashboard and memU features. Log structured results with timestamps and evidence. Fix any additional bugs discovered during testing and re-verify.

Purpose: Close the v1.4 human verification gap for dashboard and memory features (tests 2-11 from the v1.4 audit).
Output: 52-verification.json with pass/fail for all 9 tests, screenshots for UI tests, any bug fixes applied.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-live-verification-dashboard-memu/52-RESEARCH.md
@.planning/phases/52-live-verification-dashboard-memu/52-01-SUMMARY.md
@packages/dashboard/src/components/memory/MemoryPanel.tsx
@packages/dashboard/src/components/memory/SettingsPanel.tsx
@packages/dashboard/src/components/memory/ConflictPanel.tsx
@packages/dashboard/src/components/layout/Sidebar.tsx
@packages/dashboard/src/app/api/suggestions/route.ts
@packages/dashboard/src/app/api/suggestions/[id]/action/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute all 9 verification tests with structured logging</name>
  <files>
    .planning/phases/52-live-verification-dashboard-memu/52-verification.json
    .planning/phases/52-live-verification-dashboard-memu/52-screenshots/
  </files>
  <action>
Run all 9 tests sequentially. For each test, log a JSON entry with: test_id, description, timestamp, status (pass/fail/skip), evidence (response data, screenshots), and notes.

Per locked decision: "Run all 9 tests first (log and continue on failure), then address failures in priority order."

Create screenshots directory: `.planning/phases/52-live-verification-dashboard-memu/52-screenshots/`

Ensure `localStorage('occc-project')` is set to `pumplai` in the dashboard browser context before running UI tests (navigate to dashboard, switch project selector to pumplai).

**Test 1 — Health scan populates flag badges:**
- Open dashboard at `http://localhost:6987/` and navigate to Memory page
- Run health scan (via API: `POST http://localhost:6987/api/memory/health-scan` with `user_id: "pumplai"`, relaxed thresholds: `age_threshold_days: 0, retrieval_window_days: 0, similarity_min: 0.75, similarity_max: 0.97`)
- Evidence: API response body showing flags array with at least 1 flag entry. Screenshot of memory page showing flag badges.
- Pass criteria: Response has `flags` array with >= 1 entry, each with `flag_type` and `memory_id`

**Test 2 — Archive stale flag PUT succeeds, flag removed, toast appears:**
- Pick a memory_id from Test 1's stale flags
- Call `PUT http://localhost:6987/api/memory/{id}` with `content: "[ARCHIVED 2026-02-25T...] <original content>"`
- Evidence: PUT response status 200, verify flag disappears from health flags
- Screenshot: Toast notification visible after archive action
- Pass criteria: PUT returns 200, subsequent health scan shows no flag for that memory_id

**Test 3 — Conflict badge click opens ConflictPanel with side-by-side diff:**
- From Test 1's conflict flags, identify a conflict pair
- Evidence: Screenshot of ConflictPanel showing two memories side by side with word diff highlighting
- If no conflict flags in Test 1: POST additional similar memories and re-scan with lower similarity_min
- Pass criteria: ConflictPanel renders with flaggedItem and conflictItem, word diff visible

**Test 4 — Editing flagged memory triggers re-scan automatically:**
- Edit a flagged memory's content via the dashboard ConflictPanel or memory edit UI
- Evidence: After edit completes, health flags automatically update (no manual re-scan button click)
- Screenshot: Health tab after edit showing updated flag state
- Pass criteria: Flag list changes after edit without manual re-scan trigger

**Test 5 — Threshold adjustment in settings page persists across refresh:**
- Open Settings panel in memory page
- Change `age_threshold_days` from default (30) to a different value (e.g., 7)
- Click Apply
- Hard refresh the page (Ctrl+F5 or navigate away and back)
- Evidence: Screenshot showing the changed value persists after refresh
- Pass criteria: After refresh, `age_threshold_days` shows 7 (not 30)

**Test 6 — /suggestions page renders without errors:**
- Navigate to `http://localhost:6987/suggestions` (or however the suggestions page is routed)
- Evidence: Screenshot of suggestions page. API response from `GET /api/suggestions?project=pumplai`
- Pass criteria: Page renders without JS errors, shows suggestions list or "No patterns met threshold" empty state

**Test 7 — Accepting a suggestion appends to soul-override.md and re-renders SOUL:**
- First, generate suggestions: `POST http://localhost:6987/api/suggestions?project=pumplai`
- Wait for response, then GET suggestions to find a pending one
- Accept the suggestion via `POST http://localhost:6987/api/suggestions/{id}/action` with `{action: "accept", project: "pumplai", diff_text: "<the suggestion's diff_text>"}`
- Evidence: Response status 200. Verify soul-override.md was modified (check file mtime or content). Verify suggestion status changed to "accepted".
- Pass criteria: Accept returns 200, soul-override.md updated, suggestion marked accepted

**Test 8 — Sidebar badge shows correct pending suggestion count:**
- After Test 7 (some suggestions accepted, some pending), check sidebar
- Evidence: Screenshot of sidebar showing badge count matching actual pending suggestion count
- Verify: `GET /api/suggestions?project=pumplai` → count items where `status === "pending"` → compare with badge number
- Pass criteria: Badge number matches pending count (or badge hidden when 0)

**Test 9 — POST /api/suggestions invokes suggest.py and returns results:**
- `POST http://localhost:6987/api/suggestions?project=pumplai`
- Evidence: Full request + response (this is a critical path per locked decision). Response should include suggestions array.
- Pass criteria: Returns 200 with `suggestions` array (may be empty if suggest.py finds nothing, but the invocation itself must succeed without error)

Write all results to `52-verification.json` as a JSON array of test result objects.
  </action>
  <verify>
    <automated>cat .planning/phases/52-live-verification-dashboard-memu/52-verification.json | python3 -c "import json,sys; tests=json.load(sys.stdin); assert len(tests)==9, f'Expected 9 tests, got {len(tests)}'; passed=[t for t in tests if t['status']=='pass']; print(f'{len(passed)}/9 tests passed')"</automated>
    <manual>Review 52-verification.json and screenshots for completeness</manual>
  </verify>
  <done>All 9 tests executed with structured evidence. Results logged in 52-verification.json with timestamps, status, and evidence per test.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any failing tests and re-verify</name>
  <files>
    .planning/phases/52-live-verification-dashboard-memu/52-verification.json
  </files>
  <action>
Per locked decision: "Bugs discovered during verification are fixed in-phase and re-verified."

Review 52-verification.json from Task 1. For each test with `status: "fail"`:

1. Diagnose root cause from the logged evidence and error messages
2. Apply minimal targeted fix (code change, config adjustment, or data seeding)
3. Re-run ONLY the failing test
4. Update the test entry in 52-verification.json with the fix description and new status

If ALL 9 tests passed in Task 1, this task is a no-op — update 52-verification.json with a summary entry confirming clean pass.

If memU or dashboard becomes unresponsive during testing:
- Check `docker compose logs` for memU
- Check dashboard terminal output for Next.js errors
- Restart the failing service and retry
- If unrecoverable after basic troubleshooting, mark the test as "blocked" with the error details

Priority order for fixes (per research risk assessment):
1. HIGH risk tests first (T1, T2, T3, T5)
2. MEDIUM risk tests (T4, T7, T9)
3. LOW risk tests (T6, T8)

After all fixes applied and re-verified, add a summary object to the JSON log with: total_passed, total_failed, total_blocked, fixes_applied (list of descriptions).
  </action>
  <verify>
    <automated>cat .planning/phases/52-live-verification-dashboard-memu/52-verification.json | python3 -c "import json,sys; data=json.load(sys.stdin); tests=[t for t in data if 'test_id' in t]; passed=[t for t in tests if t['status']=='pass']; print(f'Final: {len(passed)}/{len(tests)} passed')"</automated>
    <manual>Review that all fixes are minimal and targeted, no unrelated changes introduced</manual>
  </verify>
  <done>All fixable test failures resolved and re-verified. 52-verification.json contains final pass/fail state for all 9 tests with fix descriptions where applicable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of verification results</name>
  <files>.planning/phases/52-live-verification-dashboard-memu/52-verification.json</files>
  <action>
CHECKPOINT — Human verifies the 9 test results from Tasks 1 and 2.

What was built: Executed all 9 live verification tests for v1.4 dashboard and memU features. Applied code fixes for settings persistence (localStorage) and auto-rescan after edit. Rebuilt memU Docker image and seeded test data.

How to verify:
1. Open `.planning/phases/52-live-verification-dashboard-memu/52-verification.json` — review all 9 test results
2. Check screenshots in `.planning/phases/52-live-verification-dashboard-memu/52-screenshots/` for UI evidence
3. Open dashboard at `http://localhost:6987/` — navigate to Memory page:
   - Run health scan — verify flag badges appear
   - Open Settings — change a threshold, refresh — verify persistence
4. Navigate to Suggestions page — verify it renders
5. Review any fixes applied (listed in verification.json summary)

Resume signal: Type "approved" if verification results are acceptable, or describe issues to address.
  </action>
  <verify>Human reviews 52-verification.json and confirms all 9 tests are acceptable</verify>
  <done>Human has approved the v1.4 dashboard and memU verification results</done>
</task>

</tasks>

<verification>
1. `52-verification.json` exists with 9 test entries + summary
2. Each test has: test_id, description, timestamp, status, evidence
3. Screenshots exist for UI-based tests (T1, T3, T5, T6, T8)
4. All fixable tests show final status "pass"
5. Any "blocked" tests have clear root cause documentation
</verification>

<success_criteria>
- All 9 v1.4 gap closure tests executed with structured evidence
- Health scan badges, conflict panel, archive flow work end-to-end
- Settings persistence survives page refresh
- Suggestions page renders, accept flow modifies soul-override.md
- Sidebar badge reflects correct pending count
- POST /api/suggestions successfully invokes suggest.py
- Human has reviewed and approved verification results
</success_criteria>

<output>
After completion, create `.planning/phases/52-live-verification-dashboard-memu/52-02-SUMMARY.md`
</output>
