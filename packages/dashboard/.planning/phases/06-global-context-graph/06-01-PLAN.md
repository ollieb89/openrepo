---
phase: 06-global-context-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/sync/vector-store.ts, src/lib/sync/graph.ts, src/app/api/links/suggestions/[id]/action/route.ts]
autonomous: true
requirements: [REAS-03]
must_haves:
  truths:
    - "Edges table exists in the database with correct schema"
    - "Accepting a link suggestion creates a corresponding record in the edges table"
    - "Graph service can retrieve direct neighbors for a given node"
  artifacts:
    - path: "src/lib/sync/graph.ts"
      provides: "Edge persistence and retrieval logic"
    - path: "src/lib/sync/vector-store.ts"
      contains: "CREATE TABLE IF NOT EXISTS edges"
  key_links:
    - from: "src/app/api/links/suggestions/[id]/action/route.ts"
      to: "src/lib/sync/graph.ts"
      via: "addEdge call on accept"
---

<objective>
Establish the data foundation for the Global Context Graph by creating the schema and providing a mechanism to persist edges when users accept link suggestions.

Purpose: Enables cross-project relationship tracking by persisting bidirectional links.
Output: Edges table schema, Graph persistence service, and integrated link acceptance flow.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-context-graph/06-RESEARCH.md
@src/lib/sync/vector-store.ts
@src/app/api/links/suggestions/[id]/action/route.ts
</context>

<tasks>

<task type="auto">
  <name>Initialize Edges Table</name>
  <files>src/lib/sync/vector-store.ts</files>
  <action>
    Update the `initVectorStore` function to include the creation of the `edges` table.
    Schema:
    - `source_id` TEXT NOT NULL
    - `target_id` TEXT NOT NULL
    - `relationship_type` TEXT NOT NULL
    - `weight` REAL DEFAULT 1.0
    - `metadata` JSON
    - `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
    - PRIMARY KEY (`source_id`, `target_id`, `relationship_type`)
    - FOREIGN KEY (`source_id`) REFERENCES `vector_cache`(`id`)
    - FOREIGN KEY (`target_id`) REFERENCES `vector_cache`(`id`)

    Add indexes:
    - `idx_edges_source` on `edges(source_id)`
    - `idx_edges_target` on `edges(target_id)`
  </action>
  <verify>
    Run a script to initialize the DB and check table existence:
    `sqlite3 workspace/.openclaw/nexus-sync.db ".schema edges"`
  </verify>
  <done>
    Edges table and indexes are created in the SQLite database.
  </done>
</task>

<task type="auto">
  <name>Implement Graph Persistence Service</name>
  <files>src/lib/sync/graph.ts</files>
  <action>
    Create a new service `src/lib/sync/graph.ts` for edge management.
    Implement:
    - `addEdge(sourceId: string, targetId: string, type: string, weight?: number)`: Inserts a new edge. Handle duplicates gracefully (ignore or update).
    - `getNeighbors(nodeId: string)`: Returns all direct neighbors (both source and target directions).
    - Use `better-sqlite3` and the same DB path logic as `vector-store.ts`.
  </action>
  <verify>
    Create a temporary test file to add and retrieve an edge.
  </verify>
  <done>
    Graph service can persist and retrieve direct edges.
  </done>
</task>

<task type="auto">
  <name>Integrate Edge Creation with Suggestions</name>
  <files>src/app/api/links/suggestions/[id]/action/route.ts, src/lib/sync/vector-store.ts</files>
  <action>
    Update `src/lib/sync/vector-store.ts`:
    - Modify `updateLinkSuggestionStatus` to return the `LinkSuggestion` object (with `decision_id` and `issue_id`) on success, instead of just a boolean.

    Update `src/app/api/links/suggestions/[id]/action/route.ts`:
    - When the action is 'accept', call `graph.addEdge(suggestion.decision_id, suggestion.issue_id, 'relates_to')`.
    - Import `addEdge` from `@/lib/sync/graph`.
  </action>
  <verify>
    1. Seed a suggestion.
    2. Accept it via `curl -X POST /api/links/suggestions/{id}/action -d '{"action":"accept"}'`.
    3. Check `edges` table for the new record: `SELECT * FROM edges WHERE source_id = ? AND target_id = ?`.
  </verify>
  <done>
    Accepting a link suggestion creates a persistent edge in the graph.
  </done>
</task>

</tasks>

<verification>
Check DB schema and verify end-to-end edge creation via API.
</verification>

<success_criteria>
- `edges` table exists with proper indexes.
- Link acceptance workflow persists edges to the graph.
- Graph service provides clean API for 1-hop traversal.
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-context-graph/06-01-SUMMARY.md`
</output>
