---
phase: 02-source-connectivity-incremental-sync
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INTG-03, INTG-04, PERF-02]
files_modified:
  - src/lib/types/connectors.ts
  - src/lib/connectors/store.ts
  - src/lib/sync/checkpoints.ts
  - src/lib/sync/health.ts
  - src/lib/sync/engine.ts
  - src/app/api/connectors/route.ts
  - src/app/api/connectors/[id]/sync/route.ts
  - tests/connectors/sync-engine.test.ts
autonomous: true
must_haves:
  truths:
    - "Incremental sync resumes from the last persisted checkpoint instead of restarting from scratch."
    - "Connector health states are normalized as connected/syncing/rate_limited/auth_expired/error/disconnected with required priority."
    - "Sync progress snapshots expose per-connector counters and current step for UI surfaces."
  artifacts:
    - path: "src/lib/sync/checkpoints.ts"
      provides: "Durable per-source cursor checkpoint load/save primitives used for resume."
    - path: "src/lib/sync/health.ts"
      provides: "Shared health classification and status-priority utilities for provider responses."
    - path: "src/lib/sync/engine.ts"
      provides: "Incremental sync orchestration that commits checkpoint-after-persist and emits progress snapshots."
    - path: "src/app/api/connectors/[id]/sync/route.ts"
      provides: "Manual sync-now trigger that runs through the shared incremental engine."
  key_links:
    - from: "src/lib/sync/engine.ts"
      to: "src/lib/sync/checkpoints.ts"
      via: "read checkpoint at start and persist new cursor after successful chunk upsert"
      pattern: "checkpoint-after-persist"
    - from: "src/lib/sync/engine.ts"
      to: "src/lib/sync/health.ts"
      via: "provider errors are normalized to connector health state transitions"
      pattern: "health classifier"
---

# Phase 02-01 Plan: Connector Runtime Foundation and Resumable Sync Core

## Objective
Establish the shared connector runtime contract, resumable incremental sync engine, and health/progress state model used by all Phase 2 provider integrations.

## Context
- `.planning/phases/02-source-connectivity-incremental-sync/02-CONTEXT.md`
- `.planning/phases/02-source-connectivity-incremental-sync/02-RESEARCH.md`
- `.planning/REQUIREMENTS.md`

<tasks>
  <task id="P02-01-T01" wave="1" type="auto">
    <name>Define connector, cursor, and health contracts with persistent store support</name>
    <title>Define connector, cursor, and health contracts with persistent store support</title>
    <requirements>INTG-03,INTG-04,PERF-02</requirements>
    <depends_on></depends_on>
    <files>
      <file>src/lib/types/connectors.ts</file>
      <file>src/lib/connectors/store.ts</file>
      <file>src/lib/sync/checkpoints.ts</file>
      <file>src/lib/sync/health.ts</file>
      <file>tests/connectors/sync-engine.test.ts</file>
    </files>
    <action>Create typed connector contracts covering provider identity, source scope, cursor payload, progress counters, and the locked health state model. Implement persistent connector state + checkpoint helpers with per-connector and per-source keys so resume never depends on in-memory state. Add health classifier utilities that map 429 to `rate_limited`, 401/403 to `auth_expired`, and apply required status priority ordering (`auth_expired` &gt; `error` &gt; `rate_limited` &gt; `syncing` &gt; `connected`).</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/sync-engine.test.ts</automated>
      <manual>Inspect persisted connector records to confirm separate checkpoints exist for each source (for example Slack channel A vs channel B) and that status priority sorting places auth_expired above rate_limited.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Connector runtime primitives exist and enforce source-aware checkpoints plus the required health state semantics.</done>
  </task>

  <task id="P02-01-T02" wave="1" type="auto">
    <name>Implement shared incremental engine and sync-now endpoint with checkpoint-after-persist semantics</name>
    <title>Implement shared incremental engine and sync-now endpoint with checkpoint-after-persist semantics</title>
    <requirements>INTG-03,PERF-02</requirements>
    <depends_on>P02-01-T01</depends_on>
    <files>
      <file>src/lib/sync/engine.ts</file>
      <file>src/app/api/connectors/route.ts</file>
      <file>src/app/api/connectors/[id]/sync/route.ts</file>
      <file>tests/connectors/sync-engine.test.ts</file>
    </files>
    <action>Build a reusable incremental sync runner that accepts adapter callbacks, loads last checkpoint, processes changed records, upserts idempotently, and writes next checkpoint only after successful persistence. Emit progress snapshots with per-source counters (`scanned`, `changed`, `upserted`), current stage, throughput, and retry metadata. Expose API routes for connector listing/config fetch and manual `Sync now` invocation that use this engine (not connector-specific one-offs).</action>
    <verify>
      <automated>npm run lint &amp;&amp; npm run test -- tests/connectors/sync-engine.test.ts</automated>
      <manual>Trigger sync-now, interrupt process mid-run, and restart; confirm resumed run continues from saved checkpoint and does not duplicate previously upserted records.</manual>
      <sampling_rate>run after this task commits</sampling_rate>
    </verify>
    <done>Incremental sync is resumable by design, exposes progress telemetry, and is reachable through shared connector APIs.</done>
  </task>
</tasks>

## Verification Criteria
- Checkpoint updates occur only after successful record persistence.
- Health state classification and priority logic match locked context semantics.
- Sync-now API executes shared engine and exposes progress counters for UI consumption.

## Must Haves
<must_haves>
  <must_have id="MH-01" requirement="INTG-03">Incremental runs process only changed/new records and safely resume from persisted per-source checkpoints.</must_have>
  <must_have id="MH-02" requirement="INTG-04">Connector health state transitions are normalized and include required statuses with correct priority routing.</must_have>
  <must_have id="MH-03" requirement="PERF-02">Sync progress snapshots provide persistent, restart-safe visibility into current step and per-source counters.</must_have>
</must_haves>
