---
phase: 11-config-decoupling-foundation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - orchestration/snapshot.py
  - orchestration/init.py
  - orchestration/config.py
  - orchestration/monitor.py
  - skills/spawn_specialist/spawn.py
  - skills/spawn_specialist/pool.py
  - docker/l3-specialist/entrypoint.sh
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-06
  - CFG-07

must_haves:
  truths:
    - "spawn.py resolves state file path via get_state_path() instead of hardcoded string"
    - "pool.py resolves state file path via get_state_path() instead of hardcoded string"
    - "snapshot.py uses _detect_default_branch() in all diff/merge/checkout operations instead of hardcoded 'main'"
    - "snapshot.py writes snapshots to get_snapshot_dir() instead of hardcoded 'workspace/.openclaw/snapshots'"
    - "init.py creates per-project directories using get_state_path/get_snapshot_dir"
    - "L3 entrypoint reads STATE_FILE from environment variable"
    - "monitor.py resolves default --state-file path via get_state_path() instead of deprecated STATE_FILE constant"
    - "spawn.py resolves L3 agent config path from project manifest agent mapping"
  artifacts:
    - path: "skills/spawn_specialist/spawn.py"
      provides: "Project-aware state path and agent config resolution"
      contains: "get_state_path"
    - path: "skills/spawn_specialist/pool.py"
      provides: "Project-aware state path resolution"
      contains: "get_state_path"
    - path: "orchestration/snapshot.py"
      provides: "Dynamic branch detection and per-project snapshot paths"
      contains: "_detect_default_branch"
    - path: "orchestration/init.py"
      provides: "Per-project directory initialization"
      contains: "get_state_path"
    - path: "orchestration/monitor.py"
      provides: "Project-aware default state file path"
      contains: "get_state_path"
    - path: "docker/l3-specialist/entrypoint.sh"
      provides: "Environment-driven state file path"
      contains: "OPENCLAW_STATE_FILE"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "orchestration/project_config.py"
      via: "import get_state_path, get_agent_mapping"
      pattern: "from orchestration.project_config import"
    - from: "skills/spawn_specialist/pool.py"
      to: "orchestration/project_config.py"
      via: "import get_state_path"
      pattern: "from orchestration.project_config import"
    - from: "orchestration/snapshot.py"
      to: "orchestration/project_config.py"
      via: "import get_snapshot_dir"
      pattern: "from.*project_config import.*get_snapshot_dir"
    - from: "orchestration/monitor.py"
      to: "orchestration/project_config.py"
      via: "import get_state_path for default --state-file arg"
      pattern: "from.*project_config import.*get_state_path"
    - from: "skills/spawn_specialist/spawn.py"
      to: "docker/l3-specialist/entrypoint.sh"
      via: "OPENCLAW_STATE_FILE env var injection into container"
      pattern: "OPENCLAW_STATE_FILE"
---

<objective>
Update all hardcoded path references and hardcoded "main" branch strings across the codebase to use the new project_config.py path APIs and _detect_default_branch helper from Plan 01.

Purpose: This is the bulk of Phase 11 — converting 14 hardcoded locations (per RESEARCH.md inventory) to use the project-aware APIs. After this plan, all orchestration code resolves paths and branches dynamically per project.

Output: All call sites updated; L3 entrypoint uses env var; agent config resolved from project manifest.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-config-decoupling-foundation/11-RESEARCH.md
@.planning/phases/11-config-decoupling-foundation/11-01-SUMMARY.md
@orchestration/project_config.py
@orchestration/config.py
@orchestration/snapshot.py
@orchestration/init.py
@orchestration/monitor.py
@skills/spawn_specialist/spawn.py
@skills/spawn_specialist/pool.py
@docker/l3-specialist/entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update snapshot.py, init.py, config.py, and monitor.py to use project-aware paths and branch detection</name>
  <files>
    orchestration/snapshot.py
    orchestration/init.py
    orchestration/config.py
    orchestration/monitor.py
  </files>
  <action>
**snapshot.py changes** — Replace ALL hardcoded "main" references with `_detect_default_branch()` calls and ALL hardcoded snapshot paths with `get_snapshot_dir()`:

Add import at top: `from .project_config import get_snapshot_dir` (add to existing project_config import if one exists from Plan 01).

Hardcoded locations to fix (line numbers from RESEARCH.md — verify against actual file):
1. `capture_semantic_snapshot` line ~170: `'diff', 'main...HEAD'` → `'diff', f'{default_branch}...HEAD'` where `default_branch = _detect_default_branch(workspace)`
2. `capture_semantic_snapshot` line ~180: `'diff', '--stat', 'main...HEAD'` → same pattern
3. `capture_semantic_snapshot` line ~209: `workspace / '.openclaw' / 'snapshots'` → `get_snapshot_dir()` (pass project_id if available in function params, else use default)
4. `l2_review_diff` line ~263: `'diff', '--stat', f'main...{branch_name}'` → use `_detect_default_branch(workspace)`
5. `l2_review_diff` line ~272: `'diff', f'main...{branch_name}'` → same
6. `l2_merge_staging` line ~309: `'checkout', 'main'` → `'checkout', default_branch`
7. `l2_merge_staging` line ~319: merge message `'into main'` → `f'into {default_branch}'`
8. `l2_reject_staging` line ~393: `'checkout', 'main'` → `'checkout', default_branch`
9. `cleanup_old_snapshots` line ~443: `workspace / '.openclaw' / 'snapshots'` → `get_snapshot_dir()`

For each function that uses `_detect_default_branch`, call it once at the start of the function and store in a local variable `default_branch`. Fresh detection per function call (no caching, per locked decision).

**init.py changes** — Update `initialize_workspace()` and `verify_workspace()`:

Add import: `from .project_config import get_state_path, get_snapshot_dir`

1. `initialize_workspace` line ~73: replace `workspace / '.openclaw' / 'snapshots'` with `get_snapshot_dir()`. The function should also ensure the per-project state directory exists: `get_state_path().parent.mkdir(parents=True, exist_ok=True)`.
2. `verify_workspace` line ~112: replace `workspace / '.openclaw'` check with `get_state_path().parent.exists()` check.

If these functions accept a project_id parameter, pass it through. If not, add an optional `project_id: Optional[str] = None` parameter.

**config.py changes** — Deprecate hardcoded constants:

Add deprecation comments to `STATE_FILE` and `SNAPSHOT_DIR` constants:
```python
# DEPRECATED: Use orchestration.project_config.get_state_path() instead.
# These constants will be removed in Phase 13. Retained for backward compatibility.
```

Do NOT remove the constants yet — they are retained for backward compatibility. Just add the deprecation comment.

**monitor.py changes** — Update the default `--state-file` argument to use the project-aware path:

Add import: `from .project_config import get_state_path`

In the argparse setup for the `--state-file` argument (which currently defaults to the `STATE_FILE` constant from `config.py`):
1. Change the default from `STATE_FILE` to `None`
2. At argument parse time (after `args = parser.parse_args()`), resolve the actual default: `if args.state_file is None: args.state_file = str(get_state_path())`
3. This must be computed at parse time, not import time, because `get_state_path()` may depend on runtime context (e.g., working directory).
4. Remove or update the import of `STATE_FILE` from `config.py` if it is no longer used elsewhere in monitor.py. If `STATE_FILE` is used in other places within monitor.py, keep the import but add a comment noting it should migrate to `get_state_path()`.
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 -c "
import ast, sys
# Verify no hardcoded 'main' remains in snapshot.py diff/merge operations
with open('orchestration/snapshot.py') as f:
    content = f.read()
# Check _detect_default_branch is called (from Plan 01)
assert '_detect_default_branch' in content, 'Missing _detect_default_branch usage'
# Check get_snapshot_dir is imported
assert 'get_snapshot_dir' in content, 'Missing get_snapshot_dir import'
# Count remaining hardcoded main in git commands (should be zero in diff/checkout/merge contexts)
import re
bad_patterns = re.findall(r\"'diff',.*'main\", content) + re.findall(r\"'checkout',.*'main'\", content) + re.findall(r\"into main'\", content)
assert len(bad_patterns) == 0, f'Still has hardcoded main: {bad_patterns}'
# Verify init.py uses project_config
with open('orchestration/init.py') as f:
    init_content = f.read()
assert 'get_state_path' in init_content or 'get_snapshot_dir' in init_content, 'init.py not updated'
# Verify config.py has deprecation
with open('orchestration/config.py') as f:
    config_content = f.read()
assert 'DEPRECATED' in config_content, 'config.py missing deprecation comment'
# Verify monitor.py uses get_state_path
with open('orchestration/monitor.py') as f:
    monitor_content = f.read()
assert 'get_state_path' in monitor_content, 'monitor.py not updated to use get_state_path'
print('All snapshot/init/config/monitor verification passed')
"</automated>
  </verify>
  <done>All hardcoded "main" references in snapshot.py replaced with _detect_default_branch(); snapshot paths use get_snapshot_dir(); init.py creates per-project dirs; config.py constants deprecated; monitor.py default --state-file uses get_state_path()</done>
</task>

<task type="auto">
  <name>Task 2: Update spawn.py, pool.py, and entrypoint.sh for per-project state and agent resolution</name>
  <files>
    skills/spawn_specialist/spawn.py
    skills/spawn_specialist/pool.py
    docker/l3-specialist/entrypoint.sh
  </files>
  <action>
**spawn.py changes:**

1. Add import: `from orchestration.project_config import get_state_path, get_agent_mapping`
   (Check existing imports — spawn.py may already import from project_config. If so, extend the import.)

2. **State path** (line ~177): Replace `project_root / "workspace" / ".openclaw" / "workspace-state.json"` with `get_state_path()`. The function may need to handle the case where `get_state_path()` returns an absolute path vs the current relative path — check whether `JarvisState` or other downstream code expects relative or absolute and adjust.

3. **Agent config resolution (CFG-07)**: In `load_l3_config()` (currently hardcodes `agents/l3_specialist/config.json`):
   - Get agent mapping: `agent_map = get_agent_mapping()` (try/except FileNotFoundError to fall back to `"l3_specialist"` if no project config)
   - Resolve L3 agent ID: `l3_agent_id = agent_map.get("l3_executor", "l3_specialist")`
   - Build config path: `config_path = Path(__file__).parent.parent.parent / "agents" / l3_agent_id / "config.json"`

4. **Inject STATE_FILE env var into L3 containers**: Where spawn.py builds the container environment dict (look for `environment=` in the Docker container creation), add `"OPENCLAW_STATE_FILE": str(get_state_path())`. The value should be the container-internal path. Since workspace is mounted at `/workspace`, compute the relative portion: if `get_state_path()` returns `<root>/workspace/.openclaw/pumplai/workspace-state.json`, the container path is `/workspace/.openclaw/pumplai/workspace-state.json`.

**pool.py changes:**

1. Add import: `from orchestration.project_config import get_state_path`

2. **State path** (line ~50 in `__init__`): Replace `self.project_root / "workspace" / ".openclaw" / "workspace-state.json"` with `get_state_path()`.

**entrypoint.sh changes:**

1. Replace the hardcoded `STATE_FILE="/workspace/.openclaw/workspace-state.json"` (line ~11) with:
   ```bash
   STATE_FILE="${OPENCLAW_STATE_FILE:-/workspace/.openclaw/workspace-state.json}"
   ```
   This reads the env var injected by spawn.py, with the old path as fallback for backward compat.
  </action>
  <verify>
    <automated>cd ~/.openclaw && python3 -c "
# Verify spawn.py uses get_state_path
with open('skills/spawn_specialist/spawn.py') as f:
    content = f.read()
assert 'get_state_path' in content, 'spawn.py missing get_state_path'
assert 'get_agent_mapping' in content, 'spawn.py missing get_agent_mapping for CFG-07'
# Verify pool.py uses get_state_path
with open('skills/spawn_specialist/pool.py') as f:
    content = f.read()
assert 'get_state_path' in content, 'pool.py missing get_state_path'
# Verify entrypoint uses env var
with open('docker/l3-specialist/entrypoint.sh') as f:
    content = f.read()
assert 'OPENCLAW_STATE_FILE' in content, 'entrypoint.sh missing OPENCLAW_STATE_FILE env var'
print('All spawn/pool/entrypoint verification passed')
"</automated>
  </verify>
  <done>spawn.py uses get_state_path() for state file and get_agent_mapping() for L3 config resolution; pool.py uses get_state_path(); entrypoint.sh reads STATE_FILE from OPENCLAW_STATE_FILE env var; spawn.py injects OPENCLAW_STATE_FILE into L3 containers</done>
</task>

</tasks>

<verification>
1. `grep -rn "hardcoded\|'main'" orchestration/snapshot.py` shows no hardcoded "main" in git command contexts
2. `grep -n "get_state_path\|get_snapshot_dir" orchestration/snapshot.py orchestration/init.py orchestration/monitor.py skills/spawn_specialist/spawn.py skills/spawn_specialist/pool.py` shows all call sites updated
3. `grep "OPENCLAW_STATE_FILE" docker/l3-specialist/entrypoint.sh` confirms env var usage
4. `grep "get_agent_mapping" skills/spawn_specialist/spawn.py` confirms CFG-07 resolution
5. `grep "DEPRECATED" orchestration/config.py` confirms deprecation markers
</verification>

<success_criteria>
- Zero hardcoded "main" in snapshot.py diff/checkout/merge operations
- Zero hardcoded state file paths in spawn.py, pool.py, or monitor.py
- Zero hardcoded snapshot directory paths in snapshot.py or init.py
- L3 entrypoint reads state file path from environment
- Agent config path resolved from project manifest
- config.py constants deprecated with comments
</success_criteria>

<output>
After completion, create `.planning/phases/11-config-decoupling-foundation/11-02-SUMMARY.md`
</output>
