---
phase: 03-decision-summaries-with-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/types/decisions.ts"
  - "src/lib/sync/storage.ts"
  - "src/lib/sync/engine.ts"
autonomous: true
requirements:
  - SUMM-01

must_haves:
  truths:
    - "System persists synced Slack thread records locally for summarization."
    - "Raw text is redacted before persistence to enforce privacy guardrails."
    - "Decision models clearly define fields for Outcome, Participants, Next Steps, and Citation."
  artifacts:
    - path: "src/lib/types/decisions.ts"
      provides: "Data structures for thread records and extracted decisions"
    - path: "src/lib/sync/storage.ts"
      provides: "File-based persistence for sync records"
  key_links:
    - from: "src/lib/sync/engine.ts"
      to: "src/lib/sync/storage.ts"
      via: "upsertRecords calling storage to persist thread data"
      pattern: "storage.saveSyncRecords"
---

<objective>
Create the storage foundation for synced thread records and define the Decision data models.

Purpose: Before we can extract "Convergent Evolution" summaries, we need a local, privacy-safe storage mechanism for the ingested Slack threads. This ensures we adhere to Phase 1 privacy rules by applying redaction before saving.
Output: Persistent storage logic for `SyncRecord`s and TypeScript models for `Decision` outcomes.
</objective>

<execution_context>
@/home/ollie/.gemini/get-shit-done/workflows/execute-plan.md
@/home/ollie/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/sync/engine.ts
@src/lib/redaction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Decision Data Models</name>
  <files>src/lib/types/decisions.ts</files>
  <action>
    Create TypeScript interfaces for Phase 03:
    - `ThreadRecord`: Extends basic `SyncRecord` payload to require `text`, `user`, `messageTs`, `channelId`.
    - `Decision`: Contains `id`, `threadId` (source reference), `outcome` (string), `participants` (string array), `nextStep` (string | null), `citation` (the exact "smoking gun" quote), `linearIds` (string array), and `isHidden` (boolean).
    - `DecisionStore`: Interface for managing decisions per project.
  </action>
  <verify>bun test -e 'import type { Decision } from "@/lib/types/decisions";' (should compile)</verify>
  <done>TypeScript models accurately reflect the required fields for decision summaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sync Record Storage with Privacy Guardrails</name>
  <files>src/lib/sync/storage.ts</files>
  <action>
    Implement local JSON-based storage for ingested threads.
    - Export `saveSyncRecords(connectorId: string, sourceId: string, records: SyncRecord[])`.
    - Storage path: `process.env.OPENCLAW_ROOT/workspace/.openclaw/records/[connectorId]/[sourceId].json`.
    - CRITICAL PRIVACY GUARDRAIL: Iterate over `records`, and if `payload.text` exists, pass it through `redactSensitiveData` from `src/lib/redaction.ts` BEFORE saving.
    - Implement `loadSyncRecords` to retrieve them for the summarizer.
  </action>
  <verify>Create a mock record, call saveSyncRecords, verify the file exists and sensitive data is redacted.</verify>
  <done>Thread records are securely persisted on disk with PII redacted.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Engine to Storage</name>
  <files>src/lib/sync/engine.ts</files>
  <action>
    Update `ConnectorSyncAdapter` usage (specifically where `adapter.upsertRecords` is called in `runIncrementalSync`) or update the adapter itself to ensure that ingested batches are routed to `saveSyncRecords`.
    Since `upsertRecords` is per-adapter, modify `src/lib/sync/engine.ts` to explicitly call `saveSyncRecords` after a successful `adapter.upsertRecords` call, capturing the raw material for Phase 03.
  </action>
  <verify>bun run build (Ensure no TS errors after modifying engine.ts)</verify>
  <done>Background sync automatically saves privacy-scrubbed records to local storage.</done>
</task>

</tasks>

<verification>
- `src/lib/types/decisions.ts` has the expected fields, notably `citation`.
- Sync storage applies redaction before writing to the local filesystem.
</verification>

<success_criteria>
- The system is capable of storing sanitized thread data.
- The data types for decisions are ready for the LLM extraction engine.
</success_criteria>

<output>
After completion, create `.planning/phases/03-decision-summaries-with-provenance/03-01-SUMMARY.md`
</output>
