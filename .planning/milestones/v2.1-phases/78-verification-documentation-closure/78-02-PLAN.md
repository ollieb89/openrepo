---
phase: 78-verification-documentation-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "74-VERIFICATION.md Required Artifacts table references paths that exist on disk"
    - "TaskCard.tsx artifact row path reads packages/dashboard/src/components/tasks/TaskCard.tsx"
    - "TaskBoard.tsx artifact row path reads packages/dashboard/src/components/tasks/TaskBoard.tsx"
  artifacts:
    - path: ".planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md"
      provides: "Phase 74 verification report with correct artifact paths"
      contains: "components/tasks/TaskCard.tsx"
  key_links:
    - from: ".planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md"
      to: "packages/dashboard/src/components/tasks/TaskCard.tsx"
      via: "Required Artifacts table path reference"
      pattern: "tasks/TaskCard\\.tsx"
---

<objective>
Fix two wrong file paths in the Required Artifacts table of 74-VERIFICATION.md.

Purpose: The verification report claims `mission-control/TaskCard.tsx` and `mission-control/TaskBoard.tsx` as verified artifacts, but those paths do not exist. The actual files are under `tasks/`. A future consumer of this report who navigates to the claimed paths will find nothing.

Output: 74-VERIFICATION.md with corrected paths (`tasks/` subdirectory) in the Required Artifacts table, committed to git.
</objective>

<execution_context>
@/home/ob/.claude/get-shit-done/workflows/execute-plan.md
@/home/ob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix artifact paths in 74-VERIFICATION.md</name>
  <files>.planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md</files>
  <action>
    Edit lines 61-62 in the Required Artifacts table. Two path strings reference the wrong subdirectory:

    Line 61 — change:
      `packages/dashboard/src/components/mission-control/TaskCard.tsx`
    to:
      `packages/dashboard/src/components/tasks/TaskCard.tsx`

    Line 62 — change:
      `packages/dashboard/src/components/mission-control/TaskBoard.tsx`
    to:
      `packages/dashboard/src/components/tasks/TaskBoard.tsx`

    No other changes. The rest of the file (frontmatter, observable truths, key links, requirements coverage, evidence, deferred checklist) is correct and must not be modified.

    After editing, verify both actual paths exist on disk:
    - `packages/dashboard/src/components/tasks/TaskCard.tsx`
    - `packages/dashboard/src/components/tasks/TaskBoard.tsx`
  </action>
  <verify>
    <automated>grep -n "tasks/TaskCard.tsx\|tasks/TaskBoard.tsx" .planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md | grep -c "tasks/" | grep -q "^2$" && echo "PASS: both paths corrected" || echo "FAIL: paths not corrected"</automated>
  </verify>
  <done>Lines 61-62 of 74-VERIFICATION.md reference `tasks/TaskCard.tsx` and `tasks/TaskBoard.tsx`. The string `mission-control/TaskCard` and `mission-control/TaskBoard` no longer appear in the file. Both actual files exist at the stated paths.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the path correction</name>
  <files>.planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md</files>
  <action>
    Stage and commit the single-file change using gsd-tools:

    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
      "docs(78-02): fix artifact paths in 74-VERIFICATION.md (mission-control → tasks)" \
      --files .planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md
    ```

    The commit message describes the exact change: wrong subdirectory name corrected in Required Artifacts table.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "74-VERIFICATION.md" && echo "PASS: committed" || echo "FAIL: not committed"</automated>
  </verify>
  <done>Git log shows a commit touching 74-VERIFICATION.md. `git status` shows no unstaged changes to the file.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Confirm wrong paths are gone: `grep "mission-control/Task" .planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md` returns no output.
2. Confirm correct paths are present: `grep "tasks/TaskCard.tsx" .planning/phases/74-dashboard-streaming-ui/74-VERIFICATION.md` returns 1 match on line 61; `grep "tasks/TaskBoard.tsx"` returns 1 match on line 62.
3. Confirm actual files exist: `ls packages/dashboard/src/components/tasks/TaskCard.tsx packages/dashboard/src/components/tasks/TaskBoard.tsx` exits 0.
4. Confirm commit is in log: `git log --oneline -3` shows the correction commit.
</verification>

<success_criteria>
- 74-VERIFICATION.md Required Artifacts table rows for TaskCard.tsx and TaskBoard.tsx reference `packages/dashboard/src/components/tasks/` (not `mission-control/`)
- No other content in 74-VERIFICATION.md is changed
- Change is committed to git
- The 78-VERIFICATION.md gap (path error in artifact table) is closed
</success_criteria>

<output>
After completion, create `.planning/phases/78-verification-documentation-closure/78-02-SUMMARY.md`
</output>
