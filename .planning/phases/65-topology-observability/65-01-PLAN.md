---
phase: 65-topology-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/package.json
  - packages/dashboard/src/lib/types/topology.ts
  - packages/dashboard/src/app/api/topology/route.ts
  - packages/dashboard/src/app/api/topology/changelog/route.ts
  - packages/dashboard/src/lib/hooks/useTopology.ts
  - packages/dashboard/src/components/layout/Sidebar.tsx
  - packages/dashboard/tests/topology/api.test.ts
  - packages/dashboard/tests/topology/transform.test.ts
  - packages/dashboard/tests/topology/diff.test.ts
  - packages/dashboard/tests/topology/timeline.test.ts
  - packages/dashboard/tests/topology/confidence.test.ts
  - packages/dashboard/tests/topology/proposals.test.ts
autonomous: true
requirements: [TOBS-01, TOBS-02, TOBS-03, TOBS-04, TOBS-05, TOBS-06]

must_haves:
  truths:
    - "API route /api/topology returns approved and pending proposal data as JSON for a given project"
    - "API route /api/topology/changelog returns correction history entries as JSON for a given project"
    - "SWR hooks provide topology and changelog data to components with loading/error states"
    - "Sidebar shows Topology nav link that navigates to /topology"
    - "Wave 0 test stubs exist for all 6 TOBS requirements and run without errors"
  artifacts:
    - path: "packages/dashboard/src/lib/types/topology.ts"
      provides: "TypeScript interfaces matching Python topology models"
      exports: ["TopologyNode", "TopologyEdge", "TopologyGraph", "RubricScore", "TopologyProposal", "ProposalSet", "TopologyDiff", "ChangelogEntry"]
    - path: "packages/dashboard/src/app/api/topology/route.ts"
      provides: "GET endpoint returning approved + pending topology data"
      exports: ["GET"]
    - path: "packages/dashboard/src/app/api/topology/changelog/route.ts"
      provides: "GET endpoint returning correction changelog entries"
      exports: ["GET"]
    - path: "packages/dashboard/src/lib/hooks/useTopology.ts"
      provides: "SWR hooks for topology and changelog data"
      exports: ["useTopology", "useTopologyChangelog"]
    - path: "packages/dashboard/tests/topology/api.test.ts"
      provides: "API route unit test stubs"
    - path: "packages/dashboard/tests/topology/transform.test.ts"
      provides: "TopologyGraph-to-ReactFlow transform test stubs"
    - path: "packages/dashboard/tests/topology/diff.test.ts"
      provides: "Diff highlight classification test stubs"
    - path: "packages/dashboard/tests/topology/timeline.test.ts"
      provides: "Changelog sort and time-travel state test stubs"
    - path: "packages/dashboard/tests/topology/confidence.test.ts"
      provides: "Confidence score transformation test stubs"
    - path: "packages/dashboard/tests/topology/proposals.test.ts"
      provides: "ProposalSet parsing test stubs"
  key_links:
    - from: "packages/dashboard/src/lib/hooks/useTopology.ts"
      to: "/api/topology"
      via: "SWR fetcher"
      pattern: "fetch.*api/topology"
    - from: "packages/dashboard/src/app/api/topology/route.ts"
      to: "filesystem"
      via: "fs/promises readFile"
      pattern: "readFile.*topology"
---

<objective>
Install React Flow dependencies, define TypeScript interfaces matching the Python topology data model, create API routes that read topology files from disk, build SWR hooks for data fetching, add Topology to the sidebar navigation, and create Wave 0 test stubs for all 6 TOBS requirements.

Purpose: Establishes the complete data layer so that all downstream visualization components (plans 02 and 03) can consume topology data without touching API or type concerns. Wave 0 test stubs ensure Nyquist compliance throughout execution.
Output: Types, 2 API routes, 2 SWR hooks, sidebar nav entry, reactflow + dagre installed, 6 test stub files.
</objective>

<execution_context>
@/home/ob/.claude/get-shit-done/workflows/execute-plan.md
@/home/ob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-topology-observability/65-CONTEXT.md
@.planning/phases/65-topology-observability/65-RESEARCH.md
@.planning/phases/65-topology-observability/65-VALIDATION.md

@packages/dashboard/package.json
@packages/dashboard/vitest.config.ts
@packages/dashboard/src/app/api/metrics/route.ts
@packages/dashboard/src/lib/hooks
@packages/dashboard/src/components/layout/Sidebar.tsx
@packages/dashboard/src/lib/openclaw.ts
@packages/dashboard/src/lib/auth-middleware.ts
@packages/dashboard/tests

<interfaces>
<!-- Python topology model serialization format (from research) -->

From packages/orchestration/src/openclaw/topology/models.py:
- TopologyNode: { id, level (1|2|3), intent, risk_level, resource_constraints?, estimated_load? }
- TopologyEdge: { from_role, to_role, edge_type }
- TopologyGraph: { project_id, proposal_id?, version, created_at, metadata?, nodes[], edges[] }

From packages/orchestration/src/openclaw/topology/proposal_models.py:
- RubricScore: { complexity, coordination_overhead, risk_containment, time_to_first_output, cost_estimate, preference_fit, overall_confidence, key_differentiators[] }
- TopologyProposal: { archetype, topology, delegation_boundaries, coordination_model, risk_assessment, justification, rubric_score? }
- ProposalSet: { proposals[], assumptions[], outcome }

From packages/orchestration/src/openclaw/topology/diff.py:
- TopologyDiff: { added_nodes[], removed_nodes[], modified_nodes[], added_edges[], removed_edges[], modified_edges[], summary, annotations? }

From packages/orchestration/src/openclaw/topology/storage.py:
- ChangelogEntry: { timestamp, correction_type, diff?, annotations? }
- File locations: {OPENCLAW_ROOT}/workspace/.openclaw/{project_id}/topology/{current.json, pending-proposals.json, changelog.json}

Established API pattern (from metrics/route.ts):
- withAuth(handler) wrapper
- searchParams.get('project') || getActiveProjectId()
- async fs reads with try/catch for missing files
- NextResponse.json() return
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for all TOBS requirements</name>
  <files>packages/dashboard/tests/topology/api.test.ts, packages/dashboard/tests/topology/transform.test.ts, packages/dashboard/tests/topology/diff.test.ts, packages/dashboard/tests/topology/timeline.test.ts, packages/dashboard/tests/topology/confidence.test.ts, packages/dashboard/tests/topology/proposals.test.ts</files>
  <action>
Create 6 test stub files under `packages/dashboard/tests/topology/`. Each file should import `describe`, `it`, `expect` from 'vitest' and contain placeholder tests that will be filled in as the corresponding production code is created.

1. `api.test.ts` (TOBS-01, TOBS-02):
   - `describe('topology API routes')` with stubs:
   - `it('GET /api/topology returns approved and proposals for a project')` — stub: expect(true).toBe(true) with TODO comment to test actual route handler once created
   - `it('GET /api/topology returns null approved when no current.json exists')`
   - `it('GET /api/topology/changelog returns changelog entries')`
   - `it('GET /api/topology/changelog returns empty array when no changelog.json')`

2. `transform.test.ts` (TOBS-01, TOBS-02):
   - `describe('toFlowElements')` with stubs:
   - `it('converts TopologyGraph nodes to React Flow nodes with dagre positions')`
   - `it('converts TopologyGraph edges to React Flow edges with edge_type styling')`
   - `it('handles empty graph gracefully')`
   - `it('applies diff highlights to nodes and edges')`

3. `diff.test.ts` (TOBS-03):
   - `describe('computeDiffHighlights')` with stubs:
   - `it('marks added nodes as added in highlight map')`
   - `it('marks removed nodes as removed in highlight map')`
   - `it('marks modified nodes as modified in highlight map')`
   - `it('returns empty maps for empty diff')`

4. `timeline.test.ts` (TOBS-04):
   - `describe('CorrectionTimeline data')` with stubs:
   - `it('sorts changelog entries chronologically by timestamp')`
   - `it('extracts diff summary from changelog entry')`
   - `it('identifies correction type from entry')`

5. `confidence.test.ts` (TOBS-05):
   - `describe('confidence chart data transformation')` with stubs:
   - `it('extracts per-archetype confidence scores from changelog')`
   - `it('builds chart data array from rubric_scores annotations')`
   - `it('skips entries without rubric_scores')`
   - `it('returns empty array for empty changelog')`

6. `proposals.test.ts` (TOBS-06):
   - `describe('ProposalSet parsing')` with stubs:
   - `it('accesses individual archetype proposals from ProposalSet')`
   - `it('extracts rubric_score from each proposal')`
   - `it('identifies highest confidence proposal')`
   - `it('handles null ProposalSet gracefully')`

Each stub test should pass (use `expect(true).toBe(true)` as placeholder) with a `// TODO: implement with real assertions when production code exists` comment. This ensures `pnpm test` passes immediately and stubs are ready for Plan 02/03 to fill in.
  </action>
  <verify>
    <automated>cd /home/ob/Development/Tools/openrepo/packages/dashboard && pnpm test -- tests/topology/ 2>&1 | tail -15</automated>
  </verify>
  <done>All 6 test files exist under tests/topology/, each with describe blocks and placeholder it() stubs. `pnpm test -- tests/topology/` runs and passes with all stubs green.</done>
</task>

<task type="auto">
  <name>Task 1: Install dependencies and create TypeScript types</name>
  <files>packages/dashboard/package.json, packages/dashboard/src/lib/types/topology.ts</files>
  <action>
1. Install React Flow and dagre:
   cd packages/dashboard && pnpm add @xyflow/react @dagrejs/dagre

2. Create `src/lib/types/topology.ts` with TypeScript interfaces matching the Python topology data model exactly. Include ALL types needed by the entire phase:

- TopologyNode: id (string), level (1|2|3), intent (string), risk_level ('low'|'medium'|'high'), resource_constraints? (Record<string, unknown>), estimated_load? (number)
- TopologyEdge: from_role (string), to_role (string), edge_type ('delegation'|'coordination'|'review_gate'|'information_flow'|'escalation')
- TopologyGraph: project_id (string), proposal_id? (string), version (number), created_at (string), metadata? (Record<string, unknown>), nodes (TopologyNode[]), edges (TopologyEdge[])
- RubricScore: complexity (number 0-10), coordination_overhead, risk_containment, time_to_first_output, cost_estimate, preference_fit, overall_confidence, key_differentiators (string[])
- TopologyProposal: archetype ('lean'|'balanced'|'robust'), topology (TopologyGraph), delegation_boundaries (string), coordination_model (string), risk_assessment (string), justification (string), rubric_score? (RubricScore)
- ProposalSet: proposals (TopologyProposal[]), assumptions (string[]), outcome (string)
- TopologyDiff: added_nodes (TopologyNode[]), removed_nodes (TopologyNode[]), modified_nodes (Array<{id: string; changes: Record<string, {old: unknown; new: unknown}>}>), added_edges (TopologyEdge[]), removed_edges (TopologyEdge[]), modified_edges (Array<{from_role: string; to_role: string; old_edge_type: string; new_edge_type: string}>), summary (string), annotations? (Record<string, unknown>)
- ChangelogEntry: timestamp (string), correction_type ('initial'|'soft'|'hard'), diff? (TopologyDiff), annotations? with optional approved_archetype, pushback_note, rubric_scores (Record<string, RubricScore>)
- TopologyApiResponse: approved (TopologyGraph | null), proposals (ProposalSet | null), projectId (string)
- ChangelogApiResponse: changelog (ChangelogEntry[]), projectId (string)

Export ALL types. These are the contracts for the entire phase.
  </action>
  <verify>
    <automated>cd /home/ob/Development/Tools/openrepo/packages/dashboard && npx tsc --noEmit src/lib/types/topology.ts 2>&1 | head -20</automated>
  </verify>
  <done>@xyflow/react and @dagrejs/dagre in package.json dependencies. topology.ts exports all 11 interfaces with correct field types matching Python model serialization.</done>
</task>

<task type="auto">
  <name>Task 2: API routes, SWR hooks, and sidebar navigation</name>
  <files>packages/dashboard/src/app/api/topology/route.ts, packages/dashboard/src/app/api/topology/changelog/route.ts, packages/dashboard/src/lib/hooks/useTopology.ts, packages/dashboard/src/components/layout/Sidebar.tsx</files>
  <action>
1. Create `src/app/api/topology/route.ts` following the metrics/route.ts pattern exactly:
   - Import withAuth from @/lib/auth-middleware, getActiveProjectId from @/lib/openclaw
   - Build topology directory path: `path.join(OPENCLAW_ROOT, 'workspace', '.openclaw', projectId, 'topology')` where OPENCLAW_ROOT comes from process.env.OPENCLAW_ROOT || the same default as other routes (import from @/lib/openclaw if available, else use the established pattern)
   - Read `current.json` (approved topology) — try/catch, return null if missing
   - Read `pending-proposals.json` (proposal set) — try/catch, return null if missing
   - Return `{ approved, proposals, projectId }` as TopologyApiResponse
   - Export GET = withAuth(handler)

2. Create `src/app/api/topology/changelog/route.ts`:
   - Same pattern as above
   - Read `changelog.json` — try/catch, return [] if missing
   - Return `{ changelog, projectId }` as ChangelogApiResponse
   - Export GET = withAuth(handler)

3. Create `src/lib/hooks/useTopology.ts`:
   - useTopology(projectId: string | null) — SWR hook hitting /api/topology?project={projectId}, refreshInterval 30000, returns { topology: TopologyApiResponse | null, isLoading, error, refresh }
   - useTopologyChangelog(projectId: string | null) — SWR hook hitting /api/topology/changelog?project={projectId}, returns { changelog: ChangelogEntry[], isLoading, error }
   - Use the same fetcher pattern as existing hooks (check src/lib/hooks/ for the established pattern — if useAuthenticatedFetch exists use it, otherwise plain fetch)

4. Add Topology to Sidebar.tsx navItems array:
   - Insert before Metrics in the navItems array (topology is structural, metrics is operational — topology should come first logically)
   - href: '/topology', label: 'Topology'
   - Use a network/graph icon from lucide-react (e.g., Network or GitBranch) OR an inline SVG with a simple hierarchy/graph shape. Per user decision, use lucide-react since it is already in deps.
  </action>
  <verify>
    <automated>cd /home/ob/Development/Tools/openrepo/packages/dashboard && npx tsc --noEmit 2>&1 | tail -5 && pnpm test -- tests/topology/ 2>&1 | tail -5</automated>
  </verify>
  <done>Two API routes return topology data (with null/empty graceful fallback for missing files). Two SWR hooks consume those routes. Sidebar shows Topology link navigating to /topology. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `pnpm list @xyflow/react @dagrejs/dagre` shows both installed
- TypeScript compiles with no errors
- API routes handle missing topology directory gracefully (return null/[])
- Sidebar renders Topology link
- `pnpm test -- tests/topology/` runs green with all 6 test stub files passing
</verification>

<success_criteria>
- @xyflow/react and @dagrejs/dagre installed in dashboard
- All topology TypeScript interfaces exported from types/topology.ts
- GET /api/topology returns { approved, proposals, projectId } reading from filesystem
- GET /api/topology/changelog returns { changelog, projectId } reading from filesystem
- useTopology and useTopologyChangelog hooks available for components
- Sidebar shows Topology navigation link
- TypeScript compiles cleanly
- All 6 Wave 0 test stub files created and passing
</success_criteria>

<output>
After completion, create `.planning/phases/65-topology-observability/65-01-SUMMARY.md`
</output>
