---
phase: 07-phase4-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_phase4.py
  - scripts/test_redaction.cjs
autonomous: true
requirements:
  - DSH-01
  - DSH-02
  - DSH-03
  - DSH-04
  - SEC-02

must_haves:
  truths:
    - "verify_phase4.py runs end-to-end and exits with a clear PASS/FAIL per requirement"
    - "Dashboard dev server starts on port 6987 and is probed by the script"
    - "DSH-01 checks: Next.js 16 + Tailwind 4 in package.json, TypeScript compiles, dashboard root responds HTTP 200"
    - "DSH-02 checks: /api/swarm returns 200 with agents+metrics+state OR documents Zod schema mismatch as Critical gap"
    - "DSH-03 checks: /api/logs/[agent] returns 200 (container running) or 404 (graceful), not 500"
    - "DSH-04 checks: metrics object from /api/swarm has all 7 required fields OR documents gap if Zod blocks it"
    - "SEC-02 checks: synthetic secrets tested against redaction patterns — implemented patterns pass, missing categories documented"
    - "test_redaction.cjs exercises redactSensitiveData() with synthetic secrets via Node.js"
    - "Script captures evidence text for each check to feed into VERIFICATION.md"
  artifacts:
    - path: "scripts/verify_phase4.py"
      provides: "Consolidated Phase 4 verification harness covering DSH-01 through DSH-04 and SEC-02"
      min_lines: 250
    - path: "scripts/test_redaction.cjs"
      provides: "Node.js helper that calls redactSensitiveData() with synthetic secrets and reports coverage"
      min_lines: 40
  key_links:
    - from: "scripts/verify_phase4.py"
      to: "workspace/occc/package.json"
      via: "JSON parse for next/tailwind version"
      pattern: "package.json"
    - from: "scripts/verify_phase4.py"
      to: "http://localhost:6987/api/swarm"
      via: "HTTP GET with requests library"
      pattern: "api/swarm"
    - from: "scripts/verify_phase4.py"
      to: "http://localhost:6987/api/swarm/stream"
      via: "SSE stream probe with timeout"
      pattern: "api/swarm/stream"
    - from: "scripts/verify_phase4.py"
      to: "http://localhost:6987/api/logs"
      via: "HTTP GET to test log endpoint"
      pattern: "api/logs"
    - from: "scripts/test_redaction.cjs"
      to: "workspace/occc/src/lib/redaction.ts"
      via: "require() of compiled redaction module"
      pattern: "redactSensitiveData"
---

<objective>
Build and run a consolidated Phase 4 verification script that exercises all five requirements (DSH-01, DSH-02, DSH-03, DSH-04, SEC-02) through live runtime testing against the occc dashboard.

Purpose: Produce repeatable, scriptable evidence that Phase 4 deliverables work or document precise gaps with severity. Per CONTEXT.md locked decision: this phase documents gaps, it does NOT fix them inline.
Output: `scripts/verify_phase4.py` and `scripts/test_redaction.cjs` that produce structured PASS/FAIL/GAP output per requirement.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase4-verification/07-RESEARCH.md
@scripts/verify_phase3.py
@workspace/occc/package.json
@workspace/occc/src/lib/redaction.ts
@workspace/occc/src/lib/jarvis.ts
@workspace/occc/src/lib/docker.ts
@workspace/occc/src/lib/metrics.ts
@workspace/occc/src/app/api/swarm/route.ts
@workspace/occc/src/app/api/swarm/stream/route.ts
@workspace/occc/src/app/api/logs/[agent]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_redaction.cjs Node.js helper for SEC-02</name>
  <files>scripts/test_redaction.cjs</files>
  <action>
Create `scripts/test_redaction.cjs` — a CommonJS Node.js script that tests the redactSensitiveData() function from the occc dashboard with synthetic secrets.

The script must:

1. Load the compiled redaction module. The Next.js build output may not be available, so use a two-strategy approach:
   - Strategy A: Try to require the compiled `.next/server/` output
   - Strategy B: If A fails, read `workspace/occc/src/lib/redaction.ts` as text, extract the REDACTION_PATTERNS regex array and the redactSensitiveData function logic, and replicate it in plain JS inline. The patterns are pure regex — no external deps.

2. Test these synthetic secrets (from CONTEXT.md requirements — must cover API keys, auth tokens, credentials, emails, generic secrets):
   ```
   "sk-test12345678901234567890abcdef"       → OPENAI_KEY
   "AKIAIOSFODNN7EXAMPLETEST1"               → AWS_KEY
   "AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKfm"   → GOOGLE_KEY
   "ghp_abcdefghijklmnopqrstuvwxyz1234567"   → GITHUB_TOKEN
   "xoxb-123456789012-123456789012-testtoken" → SLACK_TOKEN
   "Authorization: Bearer eyJhbGciOiJIUzI1NiJ9.test" → AUTH_HEADER
   "x-api-key: my-secret-api-key-9999"       → API_KEY_HEADER
   "test@example.com"                         → EMAIL
   "PASSWORD=mysecretpassword123"             → GENERIC_SECRET
   ```

3. Test these MISSING categories (expected to FAIL — document as gaps per CONTEXT.md):
   ```
   "/home/ollie/.ssh/id_rsa"                 → HOST_PATH
   "192.168.1.100"                            → IP_ADDRESS
   "968134ac3afe"                             → CONTAINER_ID (12-char hex)
   ```

4. Output JSON to stdout:
   ```json
   {
     "implemented": [{"name": "OPENAI_KEY", "input": "...", "redacted": true/false}],
     "missing": [{"name": "HOST_PATH", "input": "...", "redacted": true/false}],
     "summary": {"total": 12, "passed": N, "failed": M}
   }
   ```

5. Exit 0 always (verification script interprets the JSON, not the exit code).

Use `npm run dev` NOT required for this — it reads the TS source file directly and replicates the regex patterns.
  </action>
  <verify>
    <automated>node /home/ollie/.openclaw/scripts/test_redaction.cjs | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Redaction test: {d[\"summary\"][\"passed\"]}/{d[\"summary\"][\"total\"]} passed')"</automated>
    <manual>Review JSON output shows implemented patterns pass and missing categories are flagged</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>test_redaction.cjs runs with Node.js, exercises all synthetic secrets against redaction patterns, outputs structured JSON with pass/fail per pattern and summary counts</done>
</task>

<task type="auto">
  <name>Task 2: Create verify_phase4.py verification script</name>
  <files>scripts/verify_phase4.py</files>
  <action>
Create `scripts/verify_phase4.py` following the established pattern from `scripts/verify_phase3.py` (Colors class, print_section, print_check helpers, find_project_root, structured sections, exit code 0/1).

**CRITICAL:** Per CONTEXT.md locked decision — this script documents gaps, it does NOT fix them. If /api/swarm returns 500 due to Zod schema mismatch, that is a documented Critical gap, NOT a reason to modify the dashboard code.

The script must have these sections:

**Preamble: Start Dashboard Dev Server**
- Run `npm run dev` in `workspace/occc/` as a background subprocess (stdout/stderr to PIPE)
- Wait up to 30 seconds for readiness by polling `http://localhost:6987/api/swarm` — accept both 200 and 500 as "server is up" (500 means Zod error but server is running)
- If server does not start: FAIL all runtime checks, still run static checks
- Always kill the server in a finally block. Use `lsof -ti:6987 | xargs kill -9` as belt-and-suspenders cleanup.

**Section 1: DSH-01 — Dashboard Deployment**
Static checks (no server needed):
- Read `workspace/occc/package.json`: assert `next` version contains "16." → PASS/FAIL
- Assert `tailwindcss` in devDependencies starts with "^4" or "4." → PASS/FAIL
- Run `node_modules/.bin/tsc --noEmit` in `workspace/occc/`: assert exit 0 → PASS/FAIL (capture stderr on failure)
- Assert all required source files exist: `src/app/page.tsx`, `src/components/AgentHierarchy.tsx`, `src/components/AgentDetail.tsx`, `src/components/LogStream.tsx`, `src/components/GlobalMetrics.tsx`

Runtime checks (server must be up):
- HTTP GET `http://localhost:6987/` — assert status 200, content contains "html" → PASS/FAIL
- HTTP GET `http://localhost:6987/api/swarm` — record status code and response body. If 200: PASS. If 500: document as GAP with response body (Zod schema mismatch).

**Section 2: DSH-02 — Real-Time Monitoring**
- If `/api/swarm` returned 200 in DSH-01: parse JSON, assert `agents` key exists, assert `state` key exists → PASS
- If `/api/swarm` returned 500: mark as GAP (CRITICAL) — "Zod schema mismatch prevents state.json parsing. version field is integer (actual) vs string (expected), protocol field missing, metadata.created_at missing."
- Probe `/api/swarm/stream` with SSE: open stream with `Accept: text/event-stream`, read up to 3 lines within 5 seconds, assert `Content-Type` contains `text/event-stream` → PASS/FAIL
- Code inspection: grep `workspace/occc/src/hooks/useSwarmState.ts` for `refreshInterval` to confirm polling interval → INFO with value found

**Section 3: DSH-03 — Live Log Feeds**
- HTTP GET `http://localhost:6987/api/logs/pumplai_pm` with timeout 5s
- Accept: 200 (container running, stream alive) → PASS; 404 (container not found, graceful error) → PASS with note; 500 → GAP (docker socket or implementation error)
- Code inspection: grep `workspace/occc/src/lib/docker.ts` for `redactSensitiveData` import/call → PASS/FAIL (confirms redaction wired server-side)

**Section 4: DSH-04 — Global Metrics Visualization**
- If `/api/swarm` returned 200 in DSH-01: parse `metrics` object, assert all 7 fields present: `totalByTier`, `active`, `idle`, `errored`, `totalTasks`, `completedTasks`, `failedTasks` → PASS/FAIL per field
- If `/api/swarm` returned 500: mark as GAP (same Zod issue as DSH-02)
- Code inspection: assert `workspace/occc/src/components/GlobalMetrics.tsx` exists and contains metric rendering logic → PASS/FAIL

**Section 5: SEC-02 — Redaction Logic**
- Run `node scripts/test_redaction.cjs` as subprocess, capture stdout, parse JSON
- For each item in `implemented`: if `redacted == true` → PASS, else → FAIL
- For each item in `missing`: document as GAP (MAJOR) — "CONTEXT.md requires redaction of host filesystem paths, IP addresses, container IDs but these patterns are not implemented in redaction.ts"
- Code inspection: grep `workspace/occc/src/lib/docker.ts` for `redactSensitiveData` to confirm it is called server-side before SSE emission → PASS/FAIL

**Evidence Collection:**
- Store all check results in a list of dicts: `{"section": "DSH-01", "check": "description", "status": "PASS|FAIL|GAP", "evidence": "actual output text", "severity": "Critical|Major|Minor|null"}`
- Write this collected evidence to `/tmp/phase4_verification_evidence.json` for consumption by Plan 02
- Print summary table at the end: requirement ID | checks passed/total | gaps found

**Final Summary:**
- Print per-requirement PASS/FAIL/PARTIAL status
- Print total gaps found with severity breakdown
- Exit 0 if script ran to completion (even with gaps — gaps are expected and documented)
- Exit 1 only if script itself crashes (import error, server timeout, etc.)

Use `requests` library for HTTP calls. If not installed, fall back to `urllib.request` from stdlib.
  </action>
  <verify>
    <automated>python3 /home/ollie/.openclaw/scripts/verify_phase4.py; echo "EXIT_CODE=$?"</automated>
    <manual>Review terminal output shows structured PASS/FAIL/GAP for all five requirements, evidence JSON written to /tmp, server cleaned up</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>verify_phase4.py exists, starts the dashboard, probes all five requirements with HTTP/SSE/code-inspection checks, documents gaps with severity, writes evidence JSON to /tmp, and exits 0</done>
</task>

</tasks>

<verification>
- `scripts/verify_phase4.py` exists and runs to completion
- `scripts/test_redaction.cjs` exists and produces valid JSON output
- All five requirements (DSH-01, DSH-02, DSH-03, DSH-04, SEC-02) have structured checks
- Known gaps (Zod schema mismatch, missing redaction categories) are documented, not fixed
- Evidence JSON written to `/tmp/phase4_verification_evidence.json`
- Dashboard dev server is properly cleaned up after script runs
- Script follows established verify_phase3.py pattern (Colors, print_section, print_check)
</verification>

<success_criteria>
- verify_phase4.py runs end-to-end without crashing
- test_redaction.cjs produces valid JSON with synthetic secret test results
- Per-requirement PASS/FAIL/GAP output is clear and actionable
- Evidence is captured for VERIFICATION.md consumption in Plan 02
- No hardcoded fixes to dashboard code — this is pure verification
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase4-verification/07-01-SUMMARY.md`
</output>
