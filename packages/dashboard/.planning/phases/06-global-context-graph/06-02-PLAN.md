---
phase: 06-global-context-graph
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/lib/sync/graph.ts, src/app/api/graph/ripple-effects/route.ts]
autonomous: true
requirements: [REAS-04]
must_haves:
  truths:
    - "Recursive queries correctly identify transitive dependencies (A -> B -> C)"
    - "Query handles cyclic relationships without infinite loops"
    - "API endpoint provides a flat list of all items affected by a node"
  artifacts:
    - path: "src/app/api/graph/ripple-effects/route.ts"
      provides: "HTTP access to transitive dependency data"
  key_links:
    - from: "src/app/api/graph/ripple-effects/route.ts"
      to: "src/lib/sync/graph.ts"
      via: "findRippleEffects call"
---

<objective>
Implement the logic to traverse the context graph recursively, enabling "ripple effect" analysis where changes to one item can be traced through its dependencies across project boundaries.

Purpose: Identifies downstream impacts of decisions or issue changes.
Output: Recursive traversal service and Ripple Effects API.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-context-graph/06-RESEARCH.md
@.planning/phases/06-global-context-graph/06-01-SUMMARY.md
@src/lib/sync/graph.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Recursive Traversal Service</name>
  <files>src/lib/sync/graph.ts</files>
  <action>
    Implement `findRippleEffects(nodeId: string, maxDepth: number = 5)` in `src/lib/sync/graph.ts`.
    Use a Recursive CTE (`WITH RECURSIVE`) to find all reachable nodes.
    Safety measures:
    - Use `UNION` instead of `UNION ALL` to prevent infinite loops in cyclic graphs.
    - Include a `depth` column in the CTE and limit it to `maxDepth`.
    - Join with `vector_cache` to return node metadata (content, entity_type) along with IDs.
  </action>
  <verify>
    Create a test script that seeds a chain (A -> B -> C) and a cycle (A -> B -> A) and verifies the output of `findRippleEffects('A')`.
  </verify>
  <done>
    Graph service can perform multi-hop transitive dependency lookups.
  </done>
</task>

<task type="auto">
  <name>Create Ripple Effects API</name>
  <files>src/app/api/graph/ripple-effects/route.ts</files>
  <action>
    Create a new GET endpoint `src/app/api/graph/ripple-effects/route.ts`.
    It should accept an `id` query parameter.
    Workflow:
    - Call `graph.findRippleEffects(id)`.
    - Return the results as a JSON array.
    - Handle cases where the ID is missing or not found.
  </action>
  <verify>
    Test with `curl`:
    `curl "http://localhost:3000/api/graph/ripple-effects?id={some-id}"`
  </verify>
  <done>
    Transitive dependencies are accessible via API.
  </done>
</task>

</tasks>

<verification>
Verify recursive lookup correctly identifies transitive chains and terminates on cycles.
</verification>

<success_criteria>
- Multi-hop dependencies (A -> B -> C) are correctly identified.
- Cyclic graphs do not cause errors or infinite loops.
- API returns comprehensive metadata for all reachable nodes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-context-graph/06-02-SUMMARY.md`
</output>
