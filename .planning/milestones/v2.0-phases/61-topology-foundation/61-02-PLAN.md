---
phase: 61-topology-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/orchestration/src/openclaw/topology/diff.py
  - packages/orchestration/src/openclaw/topology/classifier.py
  - packages/orchestration/tests/test_topology_diff.py
  - packages/orchestration/tests/test_topology_classifier.py
autonomous: true
requirements: [TOPO-04, TOPO-05]

must_haves:
  truths:
    - "System generates a human-readable structural diff between two topology versions showing added/removed/modified nodes and edges"
    - "System classifies a topology as Lean, Balanced, or Robust based on structural shape with explanation and trait annotations"
  artifacts:
    - path: "packages/orchestration/src/openclaw/topology/diff.py"
      provides: "TopologyDiff, topology_diff(), format_diff()"
      contains: "def topology_diff"
    - path: "packages/orchestration/src/openclaw/topology/classifier.py"
      provides: "ArchetypeClassifier with pattern-matching classification"
      contains: "class ArchetypeClassifier"
    - path: "packages/orchestration/tests/test_topology_diff.py"
      provides: "21 tests for diff engine"
      contains: "TestNodeChanges"
    - path: "packages/orchestration/tests/test_topology_classifier.py"
      provides: "19 tests for archetype classification"
      contains: "TestLeanArchetype"
  key_links:
    - from: "packages/orchestration/src/openclaw/topology/diff.py"
      to: "packages/orchestration/src/openclaw/topology/models.py"
      via: "diff operates on TopologyGraph instances"
      pattern: "from openclaw\\.topology\\.models import"
    - from: "packages/orchestration/src/openclaw/topology/classifier.py"
      to: "packages/orchestration/src/openclaw/topology/models.py"
      via: "classifier reads TopologyGraph edges and nodes"
      pattern: "from openclaw\\.topology\\.models import"
---

<objective>
Verify topology diff engine and archetype classifier against TOPO-04 and TOPO-05 requirements.

Purpose: These modules were built during Phases 62-65. This plan formally validates the existing implementation satisfies diff and classification requirements.
Output: All TOPO-04 and TOPO-05 requirements verified green via existing test suite; any gaps patched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-topology-foundation/61-CONTEXT.md
@.planning/phases/61-topology-foundation/61-RESEARCH.md

@packages/orchestration/src/openclaw/topology/diff.py
@packages/orchestration/src/openclaw/topology/classifier.py
@packages/orchestration/tests/test_topology_diff.py
@packages/orchestration/tests/test_topology_classifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate structural diff engine (TOPO-04)</name>
  <files>packages/orchestration/src/openclaw/topology/diff.py, packages/orchestration/tests/test_topology_diff.py</files>
  <action>
Run the existing diff test suite and verify TOPO-04:

1. **Node changes:** Run TestNodeChanges — confirm diff detects added nodes, removed nodes, and modified nodes (risk_level change, intent change, multiple field changes on same node).

2. **Edge changes:** Run TestEdgeChanges — confirm diff detects added edges, removed edges, and modified edge types. Confirm edges are matched by endpoint pair (from_role, to_role) only — a change in edge_type is a modification, not add+remove (per Phase 62 decision).

3. **Human-readable output:** Run TestFormatDiff and TestSummary — confirm `format_diff()` returns a human-readable string with sections for added/removed/modified nodes and edges, and skips empty sections.

4. **Serialization:** Run TestSerializability — confirm diff `to_dict()` produces a JSON-serializable dict and the `annotations` field is present and mutable (for Phase 64 enrichment).

If any test fails, inspect and fix. The diff algorithm is custom (not deepdiff) per Claude's discretion.
  </action>
  <verify>
    <automated>uv run pytest packages/orchestration/tests/test_topology_diff.py -v -x</automated>
  </verify>
  <done>All 21 diff tests pass. System detects added/removed/modified nodes and edges, produces human-readable format_diff() output, and serializes diff entries with mutable annotations field. TOPO-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Validate archetype classifier (TOPO-05)</name>
  <files>packages/orchestration/src/openclaw/topology/classifier.py, packages/orchestration/tests/test_topology_classifier.py</files>
  <action>
Run the existing classifier test suite and verify TOPO-05:

1. **Result structure:** Run TestArchetypeResultStructure — confirm ArchetypeResult has archetype (one of "lean"/"balanced"/"robust"), confidence (0.0-1.0), explanation (non-empty string), and traits (list of strings).

2. **Lean classification:** Run TestLeanArchetype — confirm linear chains and flat delegations with no review gates classify as Lean, and the trait includes "linear" or "flat".

3. **Robust classification:** Run TestRobustArchetype — confirm topologies with review gates + escalation, or multiple coordination paths, classify as Robust, and traits include "review-heavy" and/or "fallback-roles".

4. **Balanced classification:** Run TestBalancedArchetype — confirm trees with coordination edges or single review gates (without escalation) classify as Balanced (the explicit catch-all per Phase 62 decision), and traits include "coordination-linked".

5. **Determinism:** Run TestDeterminism — confirm same input always produces same archetype.

6. **Edge cases:** Run TestEdgeCases — confirm single-node, empty-graph, and information-flow-only graphs all classify without error, getting nearest archetype with explanation.

If any test fails, inspect and fix. Classification uses pattern-matching (not hard thresholds) per CONTEXT decision.
  </action>
  <verify>
    <automated>uv run pytest packages/orchestration/tests/test_topology_classifier.py -v -x</automated>
  </verify>
  <done>All 19 classifier tests pass. System classifies topologies as Lean/Balanced/Robust based on structural shape with explanation, confidence, and trait annotations. Classification is deterministic and handles edge cases. TOPO-05 satisfied.</done>
</task>

</tasks>

<verification>
All TOPO-04 and TOPO-05 requirements verified by running:
```bash
uv run pytest packages/orchestration/tests/test_topology_diff.py packages/orchestration/tests/test_topology_classifier.py -v
```
Full topology suite gate:
```bash
uv run pytest packages/orchestration/tests/test_topology_models.py packages/orchestration/tests/test_topology_diff.py packages/orchestration/tests/test_topology_classifier.py -v
```
</verification>

<success_criteria>
- All 21 tests in test_topology_diff.py pass
- All 19 tests in test_topology_classifier.py pass (total 40 tests in this plan)
- Diff engine detects added/removed/modified nodes and edges with human-readable output
- Classifier returns Lean/Balanced/Robust with explanation, confidence, and traits
- Classification is deterministic for same input
- Edge cases (empty graph, single node) handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/61-topology-foundation/61-02-SUMMARY.md`
</output>
