# Plan 54-01: Autonomy Framework Formulation

Define the initial data structures and logic hooks to enable L3 agent autonomy.

## Proposed Changes

### Core (v1.6)

#### [MODIFY] [config.py](file:///home/ollie/Development/Tools/heretic/src/heretic_tools/config.py)
- Add `AutonomyConfig` pydantic model.
- Define default `AUTONOMY_DEFAULTS` (max_retries=3, min_confidence=0.7).

#### [MODIFY] [spawn.py](file:///home/ollie/Development/Tools/heretic/src/heretic_tools/spawn.py)
- Update L3 container startup to inject `autonomy_config` into the environment or SOUL.

#### [MODIFY] [pool.py](file:///home/ollie/Development/Tools/heretic/src/heretic_tools/pool.py)
- Implement `analyze_failure()` hook to determine if an agent can self-retry based on exit code and logs.

## Verification Plan

### Automated Tests
- `pytest tests/test_autonomy.py`: Test the failure analysis logic with mocked exit codes.

### Manual Verification
1. Disrupt an L3 task (e.g., kill process) and verify it triggers an autonomous retry if within budget.
