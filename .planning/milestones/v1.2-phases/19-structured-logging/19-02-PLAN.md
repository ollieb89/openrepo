---
phase: 19-structured-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - skills/spawn_specialist/spawn.py
  - skills/spawn_specialist/pool.py
  - orchestration/snapshot.py
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "spawn.py emits structured JSON logs for container lifecycle events with task_id and project_id in every entry"
    - "pool.py emits structured JSON logs for semaphore acquisition, retry logic, and container monitoring with task_id in every entry"
    - "snapshot.py emits structured JSON logs for git operations with task_id in every entry"
    - "Grepping structured logs for a task ID returns entries from state_engine, spawn, pool, and snapshot components"
    - "No print() calls remain in orchestration-layer Python files (state_engine, spawn, pool, snapshot) except monitor.py CLI output"
  artifacts:
    - path: "skills/spawn_specialist/spawn.py"
      provides: "Structured logging for container spawning and cleanup"
      contains: "get_logger"
    - path: "skills/spawn_specialist/pool.py"
      provides: "Structured logging for pool management and container monitoring"
      contains: "get_logger"
    - path: "orchestration/snapshot.py"
      provides: "Structured logging for git snapshot operations"
      contains: "get_logger"
  key_links:
    - from: "skills/spawn_specialist/spawn.py"
      to: "orchestration/logging.py"
      via: "import get_logger"
      pattern: "from orchestration.logging import get_logger"
    - from: "skills/spawn_specialist/pool.py"
      to: "orchestration/logging.py"
      via: "import get_logger"
      pattern: "from orchestration.logging import get_logger"
    - from: "orchestration/snapshot.py"
      to: "orchestration/logging.py"
      via: "import get_logger"
      pattern: "from .logging import get_logger"
---

<objective>
Instrument all remaining orchestration components (spawn, pool, snapshot) with structured JSON logging, completing the OBS-01 requirement across the full orchestration layer.

Purpose: After Plan 01 established the logging foundation and instrumented state_engine, this plan replaces all remaining `print()` calls in the orchestration layer with structured JSON logging. This ensures every component that touches a task emits machine-readable log lines with task_id, enabling operators to trace a task's lifecycle across all components with a single grep.

Output: All orchestration-layer `print()` calls replaced with structured logging. Complete task traceability across components.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-structured-logging/19-01-SUMMARY.md
@orchestration/logging.py
@skills/spawn_specialist/spawn.py
@skills/spawn_specialist/pool.py
@orchestration/snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument spawn.py and snapshot.py with structured logging</name>
  <files>skills/spawn_specialist/spawn.py, orchestration/snapshot.py</files>
  <action>
**spawn.py — Replace all print() calls with structured logger:**

Add at module level (after the existing sys.path and import block):
```python
from orchestration.logging import get_logger
logger = get_logger("spawn")
```

Replace these specific print() calls:

1. `print(f"[spawn] Removing existing container: {container_name}")` becomes:
   `logger.info("Removing existing container", extra={"task_id": task_id, "project_id": project_id, "container_name": container_name})`

2. `print(f"[spawn] Spawning L3 container: {container_name} (project: {project_id})")` becomes:
   `logger.info("Spawning L3 container", extra={"task_id": task_id, "project_id": project_id, "container_name": container_name, "skill": skill_hint})`

3. `print(f"[spawn] Task: {task_id}, Skill: {skill_hint}, GPU: {requires_gpu}")` — DELETE this line (info merged into the previous log line's extra fields). Add `"gpu": requires_gpu` to the previous log line's extra dict.

4. `print(f"[cleanup] Removed container: {container.name}")` becomes:
   `logger.info("Container removed", extra={"container_name": container.name})`

5. `print(f"[cleanup] Container already removed: {container.name}")` becomes:
   `logger.debug("Container already removed", extra={"container_name": container.name})`

6. `print(f"[cleanup] Error removing container: {e}")` becomes:
   `logger.error("Error removing container", extra={"container_name": container.name, "error": str(e)})`

7. The `print(f"Container spawned: {container.id}")` at the bottom in `__main__` block can stay as-is (CLI output, not orchestration logging).

**snapshot.py — Replace all print() calls with structured logger:**

Add at module level (after imports):
```python
from .logging import get_logger
logger = get_logger("snapshot")
```

Replace these specific print() calls:

1. `print(f"[snapshot] Stashed uncommitted changes for {branch_name}")` becomes:
   `logger.info("Stashed uncommitted changes", extra={"task_id": task_id, "branch": branch_name})`

2. `print(f"WARNING: Could not detect default branch for {workspace}, falling back to 'main'")` becomes:
   `logger.warning("Could not detect default branch, falling back to main", extra={"workspace": str(workspace)})`
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "
import ast, sys
# Check spawn.py has no print() except in __main__
with open('skills/spawn_specialist/spawn.py') as f:
    source = f.read()
tree = ast.parse(source)
main_block_line = None
for node in ast.walk(tree):
    if isinstance(node, ast.If) and isinstance(node.test, ast.Compare):
        if any(isinstance(c, ast.Constant) and c.value == '__main__' for c in [node.test.left] + node.test.comparators):
            main_block_line = node.lineno
prints_outside_main = []
for node in ast.walk(tree):
    if isinstance(node, ast.Call) and isinstance(node.func, ast.Name) and node.func.id == 'print':
        if main_block_line is None or node.lineno < main_block_line:
            prints_outside_main.append(node.lineno)
assert not prints_outside_main, f'print() found outside __main__ at lines: {prints_outside_main}'
# Check snapshot.py has no print()
with open('orchestration/snapshot.py') as f:
    source2 = f.read()
tree2 = ast.parse(source2)
prints = [n.lineno for n in ast.walk(tree2) if isinstance(n, ast.Call) and isinstance(n.func, ast.Name) and n.func.id == 'print']
assert not prints, f'print() found in snapshot.py at lines: {prints}'
# Check imports exist
assert 'from orchestration.logging import get_logger' in source, 'spawn.py missing get_logger import'
assert 'from .logging import get_logger' in source2, 'snapshot.py missing get_logger import'
print('PASS: spawn.py and snapshot.py use structured logging, no stray print()')
"
    </automated>
  </verify>
  <done>spawn.py and snapshot.py use structured JSON logging for all operational messages. No print() calls remain outside __main__ blocks. task_id and project_id included in relevant log entries.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument pool.py with structured logging</name>
  <files>skills/spawn_specialist/pool.py</files>
  <action>
**pool.py — Replace all print() calls with structured logger:**

Add at module level (after the existing import block, before the class definition):
```python
from orchestration.logging import get_logger
logger = get_logger("pool")
```

Replace these specific print() calls in L3ContainerPool:

1. `print(f"[pool] Acquired slot for task: {task_id}")` becomes:
   `logger.info("Acquired pool slot", extra={"task_id": task_id})`

2. `print(f"[pool] Task {task_id} failed, retrying once...")` becomes:
   `logger.warning("Task failed, retrying", extra={"task_id": task_id, "retry_count": 1})`

3. `print(f"[pool] Task {task_id} final result: {result['status']}")` becomes:
   `logger.info("Task final result", extra={"task_id": task_id, "status": result["status"]})`

4. `print(f"[pool] Spawned container for task {task_id}: {container.name}")` becomes:
   `logger.info("Container spawned", extra={"task_id": task_id, "container_name": container.name})`

5. `print(f"[pool] Monitoring container for task {task_id} (timeout: {timeout_seconds}s)")` becomes:
   `logger.info("Monitoring container", extra={"task_id": task_id, "timeout": timeout_seconds})`

6. `print(f"[pool] Task {task_id} timed out after {timeout_seconds}s")` becomes:
   `logger.error("Task timed out", extra={"task_id": task_id, "timeout": timeout_seconds})`

7. `print(f"[pool] Error killing container on timeout: {e}")` becomes:
   `logger.error("Error killing container on timeout", extra={"task_id": task_id, "error": str(e)})`

8. `print(f"[pool] Error executing task {task_id}: {e}")` becomes:
   `logger.error("Task execution error", extra={"task_id": task_id, "error": str(e)})`

9. `print(f"[pool] Container for task {task_id} exited with code {exit_code}")` becomes:
   `logger.info("Container exited", extra={"task_id": task_id, "exit_code": exit_code, "status": status})`

10. `print(f"[pool] Container timeout for task {task_id}")` becomes:
    `logger.error("Container timeout", extra={"task_id": task_id})`

11. `print(f"[pool] Log streaming error: {e}")` becomes:
    `logger.debug("Log streaming ended", extra={"task_id": task_id, "error": str(e)})`

12. `print(f"[L3-{task_id}] {decoded}")` — this is L3 container stdout relay. Replace with:
    `logger.debug("L3 output", extra={"task_id": task_id, "output": decoded})`

13. The `print(f"\nResult: {result}")` in `__main__` block can stay as-is (CLI test output).
  </action>
  <verify>
    <automated>cd /home/ollie/.openclaw && python3 -c "
import ast
with open('skills/spawn_specialist/pool.py') as f:
    source = f.read()
tree = ast.parse(source)
main_block_line = None
for node in ast.walk(tree):
    if isinstance(node, ast.If) and isinstance(node.test, ast.Compare):
        if any(isinstance(c, ast.Constant) and c.value == '__main__' for c in [node.test.left] + node.test.comparators):
            main_block_line = node.lineno
prints_outside_main = []
for node in ast.walk(tree):
    if isinstance(node, ast.Call) and isinstance(node.func, ast.Name) and node.func.id == 'print':
        if main_block_line is None or node.lineno < main_block_line:
            prints_outside_main.append(node.lineno)
assert not prints_outside_main, f'print() found outside __main__ at lines: {prints_outside_main}'
assert 'from orchestration.logging import get_logger' in source, 'pool.py missing get_logger import'
print('PASS: pool.py uses structured logging, no stray print()')
"
    </automated>
  </verify>
  <done>pool.py uses structured JSON logging for all pool management operations. No print() calls remain outside __main__ block. task_id included in every log entry for full task traceability.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'print(' orchestration/state_engine.py orchestration/snapshot.py skills/spawn_specialist/spawn.py skills/spawn_specialist/pool.py` returns zero matches outside `__main__` blocks
2. `grep -rn 'get_logger' orchestration/state_engine.py orchestration/snapshot.py skills/spawn_specialist/spawn.py skills/spawn_specialist/pool.py` shows all four files import and use get_logger
3. All log output is valid JSON on stderr with timestamp, level, component, message fields
4. Task ID appears in log entries from all components that process tasks
5. Setting `OPENCLAW_LOG_LEVEL=WARNING` suppresses INFO/DEBUG output from all components
</verification>

<success_criteria>
- All orchestration-layer print() calls replaced with structured logger (except monitor.py CLI UI and __main__ test output)
- spawn.py, pool.py, snapshot.py all import and use get_logger
- task_id present in every task-related log entry across all components
- A grep for any task ID returns structured entries from every component that touched it
- Log level filtering works (setting WARNING suppresses INFO/DEBUG)
</success_criteria>

<output>
After completion, create `.planning/phases/19-structured-logging/19-02-SUMMARY.md`
</output>
