---
phase: 61-topology-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/orchestration/src/openclaw/topology/models.py
  - packages/orchestration/src/openclaw/topology/storage.py
  - packages/orchestration/tests/test_topology_models.py
autonomous: true
requirements: [TOPO-01, TOPO-02, TOPO-03, TOPO-06]

must_haves:
  truths:
    - "TopologyGraph represents nodes (roles) and edges (delegation/coordination relationships) as an explicit graph object"
    - "A topology round-trips through JSON serialize/deserialize with zero data loss"
    - "Each topology carries version number and created_at timestamp associated with a project_id"
    - "Topology files are stored under a dedicated topology/ directory completely separate from workspace-state.json"
  artifacts:
    - path: "packages/orchestration/src/openclaw/topology/models.py"
      provides: "TopologyGraph, TopologyNode, TopologyEdge, EdgeType dataclasses"
      contains: "class TopologyGraph"
    - path: "packages/orchestration/src/openclaw/topology/storage.py"
      provides: "save_topology, load_topology, append_changelog, load_changelog with fcntl locking"
      contains: "def save_topology"
    - path: "packages/orchestration/tests/test_topology_models.py"
      provides: "Unit tests for models, serialization, storage, and file isolation"
      contains: "test_graph_with_all_edge_types"
  key_links:
    - from: "packages/orchestration/src/openclaw/topology/models.py"
      to: "packages/orchestration/src/openclaw/topology/storage.py"
      via: "storage imports TopologyGraph for save/load"
      pattern: "from openclaw\\.topology\\.models import TopologyGraph"
    - from: "packages/orchestration/src/openclaw/topology/storage.py"
      to: "workspace/.openclaw/{project_id}/topology/"
      via: "_topology_dir() creates isolated directory"
      pattern: "_topology_dir"
---

<objective>
Verify topology data model, serialization, versioning, and file isolation against TOPO-01, TOPO-02, TOPO-03, and TOPO-06 requirements.

Purpose: Phase 61 code was built forward during Phases 62-65. This plan formally validates the existing implementation satisfies the foundation requirements rather than building net-new code.
Output: All 4 requirements verified green via existing test suite; any gaps patched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-topology-foundation/61-CONTEXT.md
@.planning/phases/61-topology-foundation/61-RESEARCH.md

@packages/orchestration/src/openclaw/topology/models.py
@packages/orchestration/src/openclaw/topology/storage.py
@packages/orchestration/tests/test_topology_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate data model and serialization (TOPO-01, TOPO-02, TOPO-03)</name>
  <files>packages/orchestration/src/openclaw/topology/models.py, packages/orchestration/tests/test_topology_models.py</files>
  <action>
Run the existing test suite for topology models and verify each requirement:

1. **TOPO-01 (graph representation):** Run `test_graph_with_all_edge_types_roundtrip` and `test_edge_type_values` — confirm TopologyGraph has nodes (TopologyNode with id, level, intent, risk_level, resource_constraints, estimated_load) and edges (TopologyEdge with from_role, to_role, EdgeType). Confirm all 5 EdgeType values exist: delegation, coordination, review_gate, information_flow, escalation.

2. **TOPO-02 (JSON round-trip):** Run `test_from_json_roundtrip`, `test_to_json_produces_valid_json`, `test_empty_graph_roundtrip` — confirm `to_json()` produces valid JSON and `from_json()` restores identical data (equality check on `to_dict()` output).

3. **TOPO-03 (version and timestamp):** Run `test_graph_created_at_auto_set` and `test_graph_created_at_preserved_if_provided` — confirm TopologyGraph has `version` (int), `created_at` (ISO 8601 string auto-set in __post_init__), and `project_id` (string).

If any test fails, inspect the failure and fix the implementation or test to satisfy the requirement. Do NOT add new fields or change the data model unless a requirement is genuinely unmet.

Note: All code exists already (built during Phases 62-65). This is verification, not construction.
  </action>
  <verify>
    <automated>uv run pytest packages/orchestration/tests/test_topology_models.py -k "test_graph_with_all_edge_types or test_edge_type_values or roundtrip or test_to_json or created_at or version" -v -x</automated>
  </verify>
  <done>All TOPO-01, TOPO-02, TOPO-03 tests pass. TopologyGraph represents nodes and typed edges as an explicit graph, round-trips through JSON with zero data loss, and carries version/timestamp/project_id.</done>
</task>

<task type="auto">
  <name>Task 2: Validate file storage isolation (TOPO-06)</name>
  <files>packages/orchestration/src/openclaw/topology/storage.py, packages/orchestration/tests/test_topology_models.py</files>
  <action>
Run the existing storage tests and verify TOPO-06:

1. Run `test_save_and_load_topology`, `test_load_topology_missing_returns_none`, `test_topology_dir_created_automatically`, `test_changelog_append_and_load`, `test_bak_created_on_save`.

2. Verify the isolation invariant by confirming:
   - `storage.py` does NOT import from `state_engine.py` (no JarvisState dependency)
   - `_topology_dir()` resolves to `workspace/.openclaw/{project_id}/topology/` — completely separate from `workspace-state.json`
   - `save_topology` uses its own fcntl lock scope on `current.json.tmp`, not the workspace-state lock
   - `append_changelog` uses a dedicated `.lock` sidecar file for its RMW cycle

3. Run a quick grep to confirm no `JarvisState` import exists in the topology package: `grep -r "JarvisState" packages/orchestration/src/openclaw/topology/` should return empty.

If the isolation invariant is violated, fix it. If all checks pass, this requirement is satisfied.
  </action>
  <verify>
    <automated>uv run pytest packages/orchestration/tests/test_topology_models.py -k "test_save_and_load or test_load_topology_missing or test_topology_dir or test_changelog or test_bak" -v -x</automated>
  </verify>
  <done>All storage tests pass. Topology files are stored under a dedicated topology/ directory with independent fcntl locks. No JarvisState dependency exists in the topology package. TOPO-06 isolation invariant holds.</done>
</task>

</tasks>

<verification>
All TOPO-01, TOPO-02, TOPO-03, TOPO-06 requirements verified by running the existing test suite:
```bash
uv run pytest packages/orchestration/tests/test_topology_models.py -v
```
Plus isolation grep:
```bash
grep -r "JarvisState" packages/orchestration/src/openclaw/topology/
```
</verification>

<success_criteria>
- All 15 tests in test_topology_models.py pass
- TopologyGraph has nodes, edges, version, created_at, project_id fields
- JSON round-trip produces identical to_dict() output
- Topology storage is completely isolated from workspace-state.json
- No JarvisState imports in topology package
</success_criteria>

<output>
After completion, create `.planning/phases/61-topology-foundation/61-01-SUMMARY.md`
</output>
