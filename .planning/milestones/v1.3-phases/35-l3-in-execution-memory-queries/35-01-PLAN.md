---
phase: 35-l3-in-execution-memory-queries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/l3_specialist/agent/SOUL.md
  - tests/test_l3_memory_query.py
autonomous: true
requirements:
  - RET-05

must_haves:
  truths:
    - "L3 SOUL template documents how to query memU during execution with a copy-pasteable curl example"
    - "L3 curl POST to /retrieve returns memory items from a mock memU endpoint"
    - "L3 task continues normally when memU is unreachable — silent degradation"
    - "L3 task continues normally when memU returns empty results"
    - "MEMU_API_URL env var is used as the base URL in the SOUL curl example (not a hardcoded hostname)"
  artifacts:
    - path: "agents/l3_specialist/agent/SOUL.md"
      provides: "Memory Queries documentation section for L3 agents"
      contains: "## Memory Queries"
    - path: "tests/test_l3_memory_query.py"
      provides: "Integration tests for L3 in-execution memory queries"
      min_lines: 60
  key_links:
    - from: "agents/l3_specialist/agent/SOUL.md"
      to: "docker/memory/memory_service/routers/retrieve.py"
      via: "curl POST to /retrieve endpoint documented in SOUL"
      pattern: "MEMU_API_URL.*retrieve"
    - from: "tests/test_l3_memory_query.py"
      to: "agents/l3_specialist/agent/SOUL.md"
      via: "test validates the exact curl command pattern documented in SOUL"
      pattern: "curl.*retrieve"
---

<objective>
Add a Memory Queries section to the L3 SOUL template documenting how agents can query memU mid-execution, and write integration tests proving the curl pattern works against a mock HTTP endpoint.

Purpose: Enables L3 agents to fetch task-specific context on demand during execution — not just the static memories injected at spawn time. This closes RET-05, the last retrieval requirement.

Output: Updated SOUL.md with Memory Queries section + new test file validating success, empty-result, and failure paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-l3-in-execution-memory-queries/35-RESEARCH.md
@agents/l3_specialist/agent/SOUL.md
@docker/memory/memory_service/routers/retrieve.py
@orchestration/memory_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Memory Queries section to L3 SOUL template</name>
  <files>agents/l3_specialist/agent/SOUL.md</files>
  <action>
Append a new `## Memory Queries` section at the END of `agents/l3_specialist/agent/SOUL.md` (after the existing `## Security & Isolation` section). This placement follows the pattern established by spawn.py which appends memory context at the end of the base SOUL.

The section must contain:

1. **When to query guidance**: "Consider querying memU when you encounter an unfamiliar pattern, hit an error, or need project conventions" — per locked decision from CONTEXT.md.

2. **Copy-pasteable curl example** with these exact characteristics (all per locked decisions):
   - Uses `${MEMU_API_URL}` env var as base URL (NOT a hardcoded hostname)
   - POST to `/retrieve` endpoint
   - Payload: `{"queries": [{"role": "user", "content": "${QUERY}"}], "where": {"user_id": "${OPENCLAW_PROJECT}"}}`
   - `--max-time 5` timeout flag (locked: 5-second timeout)
   - `-s` silent flag
   - `2>/dev/null || echo "[]"` for silent degradation on network failure
   - jq extraction that handles both list `[...]` and dict `{"items": [...]}` response shapes: `jq -r '(if type == "array" then . else .items // [] end)[] | .resource_url // empty'` with `2>/dev/null || true` to prevent jq parse failure from aborting the task

3. **Scoping note**: Query uses `OPENCLAW_PROJECT` for project isolation via `where.user_id` — project-scoped only, no global fallback (per REQUIREMENTS.md out-of-scope: cross-project contamination).

4. **Advisory-only note**: "If memU is unreachable or returns no results, continue task execution — memory queries are advisory only."

5. **Agent context suggestion**: Agents can include task type from `$SKILL_HINT` in their natural language query string for more relevant results (e.g., "common errors in ${SKILL_HINT} tasks").

Do NOT modify any existing sections. Only append the new section.
  </action>
  <verify>
    <automated>grep -c "## Memory Queries" agents/l3_specialist/agent/SOUL.md | grep -q "1" && grep -q "MEMU_API_URL" agents/l3_specialist/agent/SOUL.md && grep -q "max-time 5" agents/l3_specialist/agent/SOUL.md && echo "PASS" || echo "FAIL"</automated>
    <manual>Read SOUL.md and confirm the new section is at the end, after Security & Isolation</manual>
  </verify>
  <done>SOUL.md has a Memory Queries section at the end with a copy-pasteable curl example using MEMU_API_URL, 5s timeout, project scoping via OPENCLAW_PROJECT, jq extraction handling both response shapes, and silent degradation on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for L3 in-execution memory queries</name>
  <files>tests/test_l3_memory_query.py</files>
  <action>
Create `tests/test_l3_memory_query.py` with a mock HTTP server (stdlib `http.server.BaseHTTPRequestHandler` in a background thread) and tests that validate RET-05 by running the exact curl command pattern from the SOUL template against the mock.

**Mock server setup** (use as a pytest fixture with `yield` for guaranteed cleanup):
- `MockMemuHandler(BaseHTTPRequestHandler)` — handles POST, stores last request body on server instance, returns configurable mock response, suppresses log output
- Bind to `("127.0.0.1", 0)` — OS assigns a free port (avoids "Address already in use" between test runs)
- Run in a daemon thread via `server.serve_forever()`
- Fixture yields `(server, port)`, then calls `server.shutdown()` and `server.server_close()` in teardown

**Test cases** (all use `subprocess.run` with `bash -c` to execute curl, matching the SOUL example pattern):

1. `test_l3_curl_retrieves_memories` — Mock returns `[{"resource_url": "Use asyncio for concurrent tasks", "category": "l3_outcome"}]`. Run the SOUL curl command. Assert: stdout contains the mock response, mock server received POST to `/retrieve`, request body has `where.user_id` matching the test project ID.

2. `test_l3_curl_empty_result_continues` — Mock returns `[]`. Run the SOUL curl command. Assert: subprocess exits 0, stdout is parseable as empty JSON list.

3. `test_l3_curl_graceful_on_unreachable` — Do NOT start mock server (use a port where nothing is listening). Run the SOUL curl command with `MEMU_API_URL=http://127.0.0.1:<unused_port>`. Assert: subprocess exits 0 (because of `|| echo "[]"`), stdout contains `[]`.

4. `test_l3_curl_dict_response_shape` — Mock returns `{"items": [{"resource_url": "Dict shape memory", "category": "review_decision"}], "total": 1}`. Run the SOUL curl command including jq extraction. Assert: stdout contains "Dict shape memory".

5. `test_memu_api_url_env_used` — Mock returns a known response. Run curl with `MEMU_API_URL` env var set to mock URL. Assert: mock received the request (proving the env var was used, not a hardcoded URL).

Each test must set `MEMU_API_URL` and `OPENCLAW_PROJECT` in the subprocess environment. Use `timeout=10` on `subprocess.run` to prevent hangs. The curl command in tests should match the exact pattern documented in the SOUL template.

Do NOT use httpx, requests, or any external dependency — only stdlib (http.server, subprocess, json, threading).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_l3_memory_query.py -v</automated>
  </verify>
  <done>5 tests pass: successful retrieval, empty result, unreachable server, dict response shape, and env var usage — all using the exact curl pattern from the SOUL template against a mock HTTP server.</done>
</task>

</tasks>

<verification>
1. `grep "## Memory Queries" agents/l3_specialist/agent/SOUL.md` — section exists
2. `python3 -m pytest tests/test_l3_memory_query.py -v` — all 5 tests pass
3. `python3 -m pytest tests/ -v` — full suite passes (existing 58 tests + 5 new = 63)
4. SOUL curl example uses `${MEMU_API_URL}` (not hardcoded hostname), `--max-time 5`, and `|| echo "[]"` for graceful degradation
</verification>

<success_criteria>
- RET-05 satisfied: L3 containers have documented capability and tested pattern for mid-execution memU queries
- SOUL.md Memory Queries section appended after Security & Isolation with copy-pasteable curl example
- 5 new tests in test_l3_memory_query.py covering success, empty, unreachable, dict shape, and env var paths
- Full test suite (63 tests) passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/35-l3-in-execution-memory-queries/35-01-SUMMARY.md`
</output>
