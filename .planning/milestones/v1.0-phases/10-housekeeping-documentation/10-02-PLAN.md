---
phase: 10-housekeeping-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/occc/src/lib/redaction.ts
  - agents/pumplai_pm/agent/SOUL.md
  - orchestration/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "redactWithReport() and RedactionResult are fully removed from redaction.ts with zero remaining references"
    - "redactSensitiveData() still works correctly (it is the active function)"
    - "pumplai_pm SOUL.md Scope line references /home/ollie/.openclaw/workspace"
    - "orchestration/__init__.py exports all snapshot.py public symbols"
    - "No other agent configs have stale filesystem path references"
    - "No other orchestration modules are missing from __all__"
  artifacts:
    - path: "workspace/occc/src/lib/redaction.ts"
      provides: "Clean redaction module with only active exports"
      contains: "redactSensitiveData"
    - path: "agents/pumplai_pm/agent/SOUL.md"
      provides: "Corrected workspace path for PumplAI_PM"
      contains: "/home/ollie/.openclaw/workspace"
    - path: "orchestration/__init__.py"
      provides: "Complete public API including snapshot symbols"
      contains: "capture_semantic_snapshot"
  key_links:
    - from: "workspace/occc/src/lib/docker.ts"
      to: "workspace/occc/src/lib/redaction.ts"
      via: "import { redactSensitiveData }"
      pattern: "redactSensitiveData"
    - from: "orchestration/__init__.py"
      to: "orchestration/snapshot.py"
      via: "from .snapshot import"
      pattern: "from \\.snapshot import"
---

<objective>
Remove unused `redactWithReport()` function and `RedactionResult` type, fix the pumplai_pm workspace path, and add missing snapshot.py exports to orchestration/__init__.py.

Purpose: Close the three code-level tech debt items from the v1.0 milestone audit. Each is a mechanical fix confirmed by the Phase 10 research.

Output: Three cleaned-up files with commits grouped by type (one for redactWithReport removal, one for path fixes, one for export fixes) per user decision.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Research with exact line numbers, code examples, and verification commands
@.planning/phases/10-housekeeping-documentation/10-RESEARCH.md

# Files to modify
@workspace/occc/src/lib/redaction.ts
@orchestration/__init__.py
@agents/pumplai_pm/agent/SOUL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redactWithReport() and RedactionResult from redaction.ts</name>
  <files>workspace/occc/src/lib/redaction.ts</files>
  <action>
Remove the unused `redactWithReport()` function and its `RedactionResult` interface from `workspace/occc/src/lib/redaction.ts`:

1. **Trace call graph first** — grep across `workspace/occc/src/` for any imports of `redactWithReport` or `RedactionResult`. Research confirms zero consumers, but verify before deleting.

2. **Remove `RedactionResult` interface** (lines ~19-22):
   ```typescript
   // DELETE this entire interface
   export interface RedactionResult {
     redacted: string;
     report: { pattern: string; count: number }[];
   }
   ```

3. **Remove `redactWithReport()` function** (lines ~147-167):
   - Delete the JSDoc comment, function signature, and entire function body
   - This includes the export — the function is exported inline

4. **Keep everything else intact:**
   - `RedactionPattern` interface (lines ~12-17) — used by REDACTION_PATTERNS
   - `REDACTION_PATTERNS` constant (lines ~27-125) — used by both functions
   - `redactSensitiveData()` function (lines ~131-141) — actively imported by `docker.ts`

5. **Verify after removal:**
   - `grep -rn "redactWithReport\|RedactionResult" workspace/occc/src/` returns empty
   - The remaining `redactSensitiveData` function and its imports are untouched

Commit: `fix(10): remove unused redactWithReport and RedactionResult (closes TD-03)`
  </action>
  <verify>
    <automated>grep -rn "redactWithReport\|RedactionResult" /home/ollie/.openclaw/workspace/occc/src/ ; echo "exit: $?"</automated>
    <manual>grep returns no matches; redactSensitiveData still present in redaction.ts</manual>
  </verify>
  <done>redactWithReport() and RedactionResult are fully removed from redaction.ts. Zero references remain across the codebase. redactSensitiveData() is untouched and still imported by docker.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix pumplai_pm workspace path and add snapshot.py exports</name>
  <files>agents/pumplai_pm/agent/SOUL.md, orchestration/__init__.py</files>
  <action>
Two independent fixes, committed separately per user decision (one commit for path fixes, one for export fixes):

**Fix 1 — pumplai_pm workspace path:**

1. Edit `agents/pumplai_pm/agent/SOUL.md` line 6:
   - Change: `Scope: Primary authority over the /home/ollie/Development/Projects/pumplai workspace.`
   - To: `Scope: Primary authority over the /home/ollie/.openclaw/workspace workspace.`

2. **Audit all agent configs** for stale filesystem path references (per locked decision):
   - `agents/clawdia_prime/agent/config.json` — check for filesystem paths
   - `agents/pumplai_pm/agent/config.json` — check for filesystem paths
   - `agents/l3_specialist/config.json` — check for filesystem paths
   - Research already confirmed all other configs are clean; verify this is still true
   - Do NOT change IDENTITY.md domain descriptions (e.g. "PumplAI Ecosystem") — those are intentional, not path references

3. Fix any additional stale paths found. Log structural issues as tech debt if found.

Commit: `fix(10): correct pumplai_pm workspace path in SOUL.md`

**Fix 2 — snapshot.py exports in orchestration/__init__.py:**

1. Edit `orchestration/__init__.py` to add snapshot.py imports and __all__ entries:
   ```python
   from .state_engine import JarvisState
   from .config import STATE_FILE, LOCK_TIMEOUT, POLL_INTERVAL, SNAPSHOT_DIR
   from .init import initialize_workspace, verify_workspace
   from .snapshot import (
       create_staging_branch,
       capture_semantic_snapshot,
       l2_review_diff,
       l2_merge_staging,
       l2_reject_staging,
       cleanup_old_snapshots,
       GitOperationError,
   )

   __all__ = [
       'JarvisState',
       'STATE_FILE', 'LOCK_TIMEOUT', 'POLL_INTERVAL', 'SNAPSHOT_DIR',
       'initialize_workspace', 'verify_workspace',
       'create_staging_branch', 'capture_semantic_snapshot',
       'l2_review_diff', 'l2_merge_staging', 'l2_reject_staging',
       'cleanup_old_snapshots', 'GitOperationError',
   ]
   ```

2. **Audit all orchestration modules** (per locked decision):
   - `state_engine.py` → JarvisState — already exported ✓
   - `config.py` → STATE_FILE etc. — already exported ✓
   - `init.py` → initialize_workspace, verify_workspace — already exported ✓
   - `snapshot.py` → 7 symbols — adding now
   - `monitor.py` → CLI entry point, NOT a library module — do NOT add to __all__
   - Verify no other .py files exist in orchestration/ that should be exported

3. Fix any additional missing exports found. Log structural issues as tech debt if found.

Commit: `fix(10): add snapshot.py exports to orchestration/__init__.py`
  </action>
  <verify>
    <automated>python3 -c "from orchestration import capture_semantic_snapshot, GitOperationError; print('OK')" && grep "/home/ollie/.openclaw/workspace" /home/ollie/.openclaw/agents/pumplai_pm/agent/SOUL.md</automated>
    <manual>snapshot symbols importable from orchestration package; SOUL.md has correct workspace path</manual>
  </verify>
  <done>pumplai_pm SOUL.md references correct workspace path. orchestration/__init__.py exports all 7 snapshot.py public symbols. Agent config audit and orchestration export audit both clean. Two separate commits (paths, exports).</done>
</task>

</tasks>

<verification>
1. `grep -rn "redactWithReport\|RedactionResult" workspace/occc/src/` returns empty
2. `grep "redactSensitiveData" workspace/occc/src/lib/redaction.ts` still returns the function (not accidentally deleted)
3. `grep "/home/ollie/.openclaw/workspace" agents/pumplai_pm/agent/SOUL.md` returns the Scope line
4. `python3 -c "from orchestration import capture_semantic_snapshot, GitOperationError"` succeeds
5. `python3 -c "from orchestration import __all__; print(len(__all__))"` returns 14 (7 existing + 7 new)
6. Three separate commits: redactWithReport removal, path fix, export fix
</verification>

<success_criteria>
- redactWithReport() and RedactionResult fully removed, zero codebase references
- redactSensitiveData() untouched and functional
- pumplai_pm SOUL.md Scope references `/home/ollie/.openclaw/workspace`
- orchestration/__init__.py exports all snapshot.py public symbols in __all__
- No other stale paths or missing exports found in audits
- Three commits grouped by type per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/10-housekeeping-documentation/10-02-SUMMARY.md`
</output>
